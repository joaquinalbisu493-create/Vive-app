# SCHEMA.md — Esquema real de base de datos (Supabase)

> ⚠️ Este archivo describe lo que está REALMENTE en Supabase hoy.
> No es un diseño aspiracional — si algo cambia en la base, este archivo se actualiza el mismo día.
> Última actualización: 28 de agosto 2026 — 🔴 **la videollamada no dejaba entrar a nadie, y no era la cuenta de Daily**. Se activó la cuenta nueva (dominio `veraapp`, key nueva en `DAILY_API_KEY` el 28/08) y los 4 chequeos dieron OK: autentica, crea sala privada por API, acuña meeting token y `/meetings` es consultable. Pero el bloqueante real era otro: `create-meeting-room` creaba la sala `privacy: 'private'` y devolvía la URL **pelada**, y a una sala privada de Daily no se entra sin **meeting token** (o con `enable_knocking` y un owner adentro admitiendo a mano). No se había detectado porque en el plan gratuito fallaba antes, en la creación misma — el camino de entrada nunca se pudo probar. Ahora el token se acuña por participante en cada llamada (el coach entra como owner) y nunca se guarda. Segundo hallazgo: **`SessionsScreen` abría la URL pelada sin llamar a la función**, un camino de entrada entero que no pasaba por acá. Tercero: la URL guardada trae el dominio de la cuenta VIEJA en 43 filas, así que la función dejó de confiar en ella y las corrige sola. Ver `bookings.meeting_url`. Antes, 26 de agosto 2026 — 🔴 **una disputa GANADA se quedaba para siempre en la lista de plata a recuperar** (`scripts/add-dispute-resolution.sql`, **CORRIDO el 26/08/2026**; `paypal-webhook` **v18** deployada y verificada). Nuevas `bookings.dispute_resolved_at` y `dispute_outcome`, y `reversiones_despues_de_pagar` deja de contar las disputas ya cerradas. La lógica cuelga de `status`, cuyo enum se pudo citar de la documentación de PayPal, y **no** de `outcome_code`, que no — ese se guarda crudo. Ver `bookings`. Antes, 25 de agosto 2026 — 🔴 **dos defectos de la regla espejo que ROMPEN escrituras de todos los días EN PRODUCCIÓN AHORA MISMO** (`scripts/fix-payout-rails-trigger.sql`, ✅ **CORRIDO y VERIFICADO el 26/08/2026**: `method` devuelve `is_nullable = YES`, y el `update` de `price_usd` que antes abortaba con `record "new" has no field "coach_id"` ahora pasa **y deja la derivada bien calculada** — el coach de prueba quedó con `price_usd = 50` y `accepts_international = false`, que es lo correcto porque tiene los dos rieles en `false`. ⚠️ Sin probar queda **la rama del DELETE** (`old.coach_id`), el otro caso que rompía: borrar una fila de cobro y, por cascada, borrar un coach). Estaban vivos porque `add-payout-rails.sql` sí está corrido: se confirmó contra la base el 25/08, contra lo que decía este mismo archivo. (1) `sync_accepts_international()` resolvía el coach con `coalesce(new.coach_id, old.coach_id, new.id, old.id)`, y eso **no es defensivo**: PL/pgSQL resuelve cada campo contra la forma REAL del registro, así que `new.coach_id` sobre una fila de `coaches` no da null — tira `record "new" has no field "coach_id"` y **aborta la sentencia**. Con `trg_sync_intl_on_price` colgado de `coaches`, ningún coach podía guardar su `price_usd`; y `new` en un DELETE no está asignado, así que tampoco se podía borrar una fila de cobro ni, por cascada, un coach. (2) `coach_payout_accounts.method` seguía NOT NULL sin default después de que la pantalla dejara de mandarla: **nadie podía guardar sus datos de cobro**. Los dos fallaban del lado del cliente como "no se pudo guardar". Y un tercero solo de UI: el input del precio en dólares vivía adentro de `{accepts_international && …}` y esa columna ahora **se deriva de tener precio**, o sea que el coach sin precio no veía nunca el campo que se lo habría puesto — un circuito cerrado. Ver `coach_payout_accounts`. Antes, mismo día — 🔴 **dónde estaba quien reservó** (`scripts/add-observacion-pais.sql`, **CORRIDO y VERIFICADO el 25/08/2026**): `bookings.user_tz_observed` + `user_observation_source` + `user_observed_at`. El sistema **no sabía** cuáles operaciones eran internacionales: lo inferí­a del RIEL de pago, y el riel lo elige el usuario. Se guarda la **observación cruda** —ni el país, que ya es una lectura, ni `es_internacional`, que es una conclusión— y la clasificación se deriva en la vista `clasificacion_de_operaciones`, **el único lugar que va a cambiar cuando el contador conteste**. Mismo día — **registro de operaciones de dinero** (`scripts/add-operaciones-de-dinero.sql`, **CORRIDO**): no hizo falta tabla nueva —`admin_audit_log` ya existía y ya era append-only— sino una forma acordada para los montos dentro de `details` y la vista `operaciones_de_dinero`. Mismo día — 🔴 **prueba de que la sesión ocurrió** (`scripts/add-session-attendance.sql`, **CORRIDO**; función y cron deployados): nueva tabla `session_attendance` y la edge function `session-attendance` (cron cada hora) que trae de Daily quién entró a la videollamada, cuándo y cuánto duró. Hasta hoy **no existía ninguna prueba de que una sesión hubiera pasado**, y sobre una videollamada la disputa que llega es "el servicio no se prestó". ⚠️ **Es metadato, nunca contenido**, y hay que declararlo en la política de privacidad — va con los placeholders que esperan al abogado. Mismo día — 🔴 **contracargos** (`scripts/add-chargebacks.sql`, **CORRIDO y VERIFICADO el 25/08/2026**; `mp-webhook` v26 y `paypal-webhook` v16 deployadas, las dos con `verify_jwt = false`, y los **7 eventos suscritos** en el webhook de producción): `payment_status` acepta **`'contracargo'`**, que hasta hoy se guardaba como `'reembolsado'` — **en los datos eran indistinguibles**, así que no se podía contar cuántos hubo. Nuevas `disputed_at` y `dispute_reason` (una disputa abierta **no** toca `payment_status`: la plata todavía no se movió), y la vista `reversiones_despues_de_pagar`, que es la consulta que **nadie hacía**: reservas donde la plata volvió después de habérsela transferido al coach. Mismo día — 🔴 **regla espejo** (`scripts/add-payout-rails.sql`, ⚠️ **PENDIENTE DE CORRER**): el coach pasa de elegir UN método de cobro a **declarar qué rieles acepta**, y a sus clientes se les ofrecen solo esos. El CBU sale como opción del exterior, el costo de entrega deja de descontársele al coach (lo absorbe VIVE) y `accepts_international` pasa a ser **derivada por trigger** con su `update` revocado. Ver `docs/decisiones-pagos.md`. Antes, mismo día — 🔴 **el riel de PayPal está EN PRODUCCIÓN** (`PAYPAL_MODE = live` desde el 25/08 00:22), con el webhook de producción registrado y Payouts aprobado en la cuenta live. ✅ **Probado de punta a punta con plata real el 25/08**: cobro de USD 1,00 (captura `2BV40052U0373070A`, `aprobado` + `confirmada`) y reembolso al primer intento tres minutos después. Se hizo con USD 1 bajando el CHECK de `price_usd` un rato y **restaurándolo enseguida**. ⚠️ El checkout live abre **Safari** y no la app de PayPal: PayPal no reclama la ruta del checkout como universal link, así que obliga a loguearse a mano — no afecta la acreditación, que es server-side. Antes de cambiar el modo hubo que dejar en `'reembolsado'` las reservas de prueba pagadas en sandbox: sus capturas no existen en producción, y un reembolso posterior al cambio habría ido derecho al dead-letter. Antes, 24 de agosto 2026 — **el riel de PayPal quedó completo en la base**: se corrieron `scripts/add-paypal-rail.sql` y `scripts/add-paypal-refund-cron.sql`, los dos que figuraban como pendientes desde el 19 y el 20 de agosto. `bookings.charged_amount` **ya estaba creada** (el registro estaba desactualizado, no la base), pero el CHECK de `price_usd >= 20`, la rama de PayPal en `expire_unpaid_checkouts()` y el cron de reembolsos sí faltaban — y sin ese último, toda reserva de PayPal cancelada se quedaba marcada `'reembolso_pendiente'` para siempre, sin devolverle la plata a nadie y sin ningún error visible. Nuevo `scripts/verificar-riel-paypal.sql` (solo lectura, 8 chequeos) para que el estado real no vuelva a depender de lo que diga el changelog. Antes, 19 de agosto 2026 — **zonas horarias**: la tardanza de una cancelación la decide ahora el trigger y no el teléfono (`scripts/add-late-cancel-server-side.sql`, **CORRIDO**), porque el cálculo del cliente usaba la zona del dispositivo y le negaba el reembolso a quien cancelaba en tiempo desde afuera de Argentina — y además la columna era falsificable contra la API. Del lado del cliente todo el cálculo de instantes pasó a `lib/time.ts`, que convierte por **nombre de zona** y no por offset fijo; `_shared/guarantee.ts` hace lo mismo del lado del servidor. Mismo día: **pagos a coaches del riel internacional**: `bookings.paid_out_at` + `payout_reference` y la policy `bookings_select_admin` (`scripts/add-coach-payouts.sql`, **CORRIDO y VERIFICADO**), más la comisión calculada también en `usdt-create-payment` (antes toda reserva internacional se quedaba con el default 20 de `platform_fee_pct`, que en ese riel es lo único que dice cuánto se le debe al coach). De paso: **el panel nunca tuvo policy de SELECT sobre `bookings`**, así que la pestaña de Reembolsos de la 104 solo veía las reservas propias del admin. Ver la sección "Pagos v1". Antes, 18 de agosto 2026 — **Cobro en USDT (TRC20)** funcionando de punta a punta, verificado con un pago real. Mismo día: **`coach_payout_accounts` + `coaches.accepts_international`** (`scripts/add-coach-international.sql`, **CORRIDO y VERIFICADO**): opt-in del coach a sesiones del exterior y sus datos de cobro, en tabla aparte porque `coaches` se lee público con la anon key. Mismo día: **`bookings.payment_provider`** (`scripts/add-payment-provider.sql`, **CORRIDO y VERIFICADO**): guarda para el día que haya un segundo rail de cobro; sin ella, la primera reserva cobrada fuera de Mercado Pago y cancelada se iba al dead-letter de reembolsos en silencio. Ver la sección "Pagos v1". Antes, 16 de agosto 2026 — **el trigger de alta ahora escribe `avatar_url`** (`scripts/add-social-profile-data.sql`, **CORRIDO y VERIFICADO el 16/08/2026** contra un alta real con Google: `name = 'Steam Steam'` y `avatar_url` con la URL de `lh3.googleusercontent.com`). Google manda nombre y foto en `raw_user_meta_data` desde el primer login, pero la foto se quedaba en `auth.users` y no llegaba a `profiles` — y **todas** las pantallas leen `profiles.avatar_url`, ninguna lee la metadata: quien se registraba con Google veía sus iniciales teniendo la foto disponible. Incluye backfill de las cuentas sociales ya creadas (solo las que tenían `avatar_url` en NULL). Ver la subsección "Trigger de alta" en `profiles`. Antes, 13 de agosto 2026 — dos cambios, los dos **corridos y verificados el mismo día**. (1) **Bloqueo de usuarios** (`blocked_users` + `are_blocked()` + triggers en `messages` y `bookings`, `scripts/add-user-blocking.sql`): cierra el requisito que faltaba de la guideline 1.2 de Apple, que era un bloqueador de publicación. (2) **`profiles.age_confirmed`** (`scripts/add-age-confirmation.sql`): constancia de la declaración de mayoría de edad que T&C §3.1 afirmaba y nunca se pedía. Ver las secciones `blocked_users` y `profiles`. Antes, 9 de agosto — **primer pago real contra MP, y dos cosas que estaban muertas en producción sin avisar.** (1) `mp-webhook` nunca escribió un `payment_status`: leía el pago con el token de plataforma, que en marketplace no puede leer pagos del vendedor → 502 en cada notificación. (2) El cron de reembolsos venía dando 401 cada 5 minutos desde el 24/07 porque el Vault tenía guardado el placeholder `<PEGAR_SERVICE_ROLE_KEY>` sin reemplazar. Las dos corregidas y verificadas con un pago de $1 cobrado y reembolsado end-to-end. Ver sección "Pagos v1". Antes, 7 de agosto: **INSERT de `reviews` endurecido** (`scripts/harden-reviews-insert.sql`, **CORRIDO 07/08/2026**): una reseña ahora exige una sesión propia y `completada` con ese mismo coach, verificado por RLS y no solo por la pantalla. Ver la sección `reviews`. Antes, 6 de agosto: tabla `session_notes` (notas de sesión del coach, privada+compartida, anti-fuga #4, **`scripts/add-session-notes.sql` CORRIDO — incluye el `WITH CHECK` endurecido, verificado en `pg_policy`**) y tabla `reports` (moderación, reportar usuarios/coaches, `scripts/add-reports.sql` corrido). Antes, mismo día: auditoría del pipeline de pagos MP: se documenta `bookings.refund_attempts` (dead-letter de reembolsos, `scripts/add-refund-attempts.sql` corrido), el refresh automático del token del coach (`getFreshCoachToken`), y se corrige la nota de "edge functions scaffolds" (ya tienen lógica real). Ver sección "Pagos v1".
>
> 29 de julio 2026 — se documenta `mood_entries` (existía en Supabase pero no estaba en este archivo — gap detectado al hacer que `app/diario.tsx` la lea). Sin migración nueva: la tabla y sus datos ya existían, solo se agregó un consumidor.
>
> 23 de julio 2026 — `user_habits` (nueva tabla): rutina de hábitos = prácticas VITA elegidas por el usuario para la sección "Hábitos de hoy" de Progreso; el "hecho hoy" se deriva de `resource_completions` (atado al uso real). `app/diario.tsx` ahora también escribe en `resource_completions` (era la única de las 10 herramientas que no lo hacía). **Falta correr `scripts/add-user-habits.sql` en Supabase.**
>
> 18 de julio 2026 — `resource_events` instrumentado (view/play/complete/coach_profile_visit) + 2 funciones `SECURITY DEFINER` nuevas para que el coach lea sus stats agregadas (`get_my_resource_counts`, `get_my_resource_stats_month`), alimentando el tab F4 "Tus recursos". `resource_reminders` ya tiene RF1-RF3 completos (motor, UI, pantalla), documentado en CHANGELOG. Cerrado el hueco 05/07→13/07: pagos v1 y el trigger de reembolso estaban marcados "pendiente de correr" pero ya estaban corridos en prod (corregido); "puertas de Conexiones" resultó ser lógica de frontend pura contra `coach_topics` (ya documentado), sin migración propia — no había nada que documentar ahí.

## Realtime (postgres_changes)

🔴 **Cuatro tablas están publicadas en `supabase_realtime`: `messages`, `bookings`, `salas` y `notifications`** (`scripts/habilitar-realtime.sql`, ✅ **CORRIDO y VERIFICADO el 28/08/2026**), las cuatro con `REPLICA IDENTITY FULL`.

📝 **Hasta ese día la publicación estaba VACÍA** y ninguna de las ocho suscripciones de la app recibía un solo evento — el chat no se actualizaba solo (`SalaScreen` no hace polling de mensajes), los puntitos de las dos barras no se movían, y el badge de Reservas del coach no se apagaba al aceptar. El código del cliente siempre estuvo bien: escuchaba un canal por el que no pasaba nada.

⚠️ **Publicar no saltea RLS.** Realtime evalúa las políticas del usuario suscripto antes de entregarle una fila. Lo que se habilita es el transporte, no el permiso.

⚠️ **`REPLICA IDENTITY FULL` no es opcional acá**: con la de por defecto (la PK) un UPDATE viaja sin los valores viejos, y los filtros del cliente (`coach_id=eq.…`) se evalúan contra una fila incompleta. Para los INSERT da igual; para los UPDATE es la diferencia entre que ande siempre y que ande a veces.

## Tablas y relaciones

### ⚠️ Permiso sobrante en TODAS las tablas — `TRUNCATE`, `REFERENCES`, `TRIGGER`
Detectado el 03/09/2026 verificando `add-user-consents.sql`. Supabase concede esos tres a `authenticated` y `anon` sobre **todo `public`** por default, y el barrido confirmó que están en todas las tablas y vistas —`mood_entries`, `journal_entries`, `messages`, `profiles`, `bookings`, `coach_payout_accounts` incluidas—. 🔴 **`TRUNCATE` no lo filtra la RLS**: las policies se evalúan sobre select/insert/update/delete, así que toda la protección por filas del proyecto no dice nada sobre vaciar una tabla entera. ✅ **Hoy no es explotable desde la app** —PostgREST no expone TRUNCATE por HTTP— pero deja de ser teórico el día que exista una RPC con SQL dinámico. Arreglado por `scripts/revoke-truncate-trigger-references.sql` — ✅ **CORRIDO y VERIFICADO el 03/09/2026**: el barrido devuelve **0 filas**, y el control confirma que no se llevó puesto nada (`mood_entries`, `journal_entries`, `gratitude_entries` y `messages` siguen con DELETE/INSERT/SELECT/UPDATE, `bookings` con DELETE/INSERT/SELECT, y `user_consents` con SELECT y nada más). El script toca además `alter default privileges` para que la próxima tabla no nazca igual — sin esa segunda parte el problema vuelve, que es exactamente lo que le pasó a `user_consents`.

⚠️ **Sigue siendo por rol creador.** El `alter default privileges` cubre lo que cree `postgres`, que es el rol del SQL editor del dashboard. Si algún día una migración corre con otro rol, la tabla nueva puede volver a nacer con los tres permisos — la verificación del script lo detecta.

### `profiles`
- 🔴 **`anon` podía leer TODAS las columnas del perfil de los coaches** (encontrado el 08/09/2026 verificando si hacía falta una policy pública para la página web del coach: **ya existía y era más ancha de lo que debía**). Con la anon key —que viaja DENTRO de la app publicada, o sea que es pública por definición— cualquiera leía `email`, `push_token`, `birth_date`, `nationality`, `is_admin` y el resto, de los 32 coaches. 🟢 **Lo que sí estaba bien:** la RLS filtra por `role = 'coach'`, así que los perfiles de usuarios finales no se veían (0 filas con `role='user'`), y lo sensible está cerrado (`mood_entries`, `journal_entries`, `messages`, `bookings`, `session_notes` devuelven `[]`; `coach_payout_accounts` tiene el SELECT revocado). **Era un problema de COLUMNAS, no de filas**, y por eso se arregla con grants y no con una policy: la RLS dice qué filas, los grants dicen qué columnas. `scripts/restrict-anon-profiles-columns.sql` deja a `anon` con **`id`, `name`, `avatar_url`, `gender`** y nada más — las cuatro que usa el catálogo (`coachesCache`, `search3`, `ProfesionalScreen`). ✅ **CORRIDO y VERIFICADO el 08/09/2026 con la anon key**, que es lo que tiene un atacante: `email`, `push_token` y `select=*` devuelven **42501 permission denied**, y la consulta real de `coachesCache` —con su `profiles!inner(...)` y todos sus filtros— sigue dando **200 con los perfiles embebidos completos**. `gender` se queda a propósito: se muestra en el perfil público y es filtro de búsqueda. 🔴 **Lo que el script NO arregla y hay que decidir aparte: qué ve `authenticated`.** Si un usuario logueado también lee el mail de todos los coaches, alcanza con registrarse para scrapear el roster — el mismo agujero una puerta más adentro. La verificación 3 del script lo deja a la vista.
- `id` (uuid, PK) — coincide con `auth.users.id`, pero **YA NO hay FK contra `auth.users`** (se dropeó el 06/08/2026, ver "Baja de cuenta" abajo). La integridad la sostiene el trigger de alta; la fila puede sobrevivir a la cuenta de auth.
- `email`, `name`, `role` (coach | user), `avatar_url`, `birth_date`, `gender`, `nationality`
- `accepted_terms` (bool), `push_token`, `created_at`
  - 📌 **`push_token` ya NO se lee desde el cliente** (A4 fase 1, 09/09/2026). El envío de push se movió a la edge function `send-push` (service role + validación de relación); el cliente solo hace el `.update` de su propio token en `registerForPushNotifications`.
  - 🟢 **`email` y `push_token` REVOCADOS a `authenticated`** (A4 fase 3, ✅ CORRIDO y VERIFICADO el 09/09/2026, `scripts/restrict-authenticated-profiles-columns.sql`). Se le sacó a `authenticated` el SELECT a nivel tabla y se re-granteó el de las **15 columnas** restantes (todas menos `email` y `push_token`). Antes, cualquiera con una cuenta podía leer el mail y el token de los 34 coaches — el mismo agujero de `anon` (205), una puerta adentro. Verificado con JWT real: `coaches→profiles.email` y `push_token` → **42501**; catálogo y fila propia → OK. ⚠️ Efecto lateral bueno: `authenticated` pasó de grant de tabla a grant de columnas, así que una columna sensible NUEVA ya no queda legible sola (cierra la asimetría de la 207 para `profiles`). 🟡 `is_admin`/`birth_date`/`nationality` siguen visibles a `authenticated` — no se pueden revocar sin una vista pública (los lee la fila PROPIA en `EditProfileScreen`); queda como mejora para Andre.
- `accepted_terms_at` (timestamptz, nullable) y `accepted_terms_version` (text, nullable) — **cuándo** y **qué texto** aceptó. `accepted_terms` a secas dice que aceptó, no qué leyó: alcanza mientras los documentos no cambien nunca, y van a cambiar (hoy son borrador con 12 placeholders, y §20 prevé expresamente modificarlos). Sin esto, invocar §20 o §10 contra alguien deja la aceptación existente pero inoponible en su contenido. `accepted_terms_version` guarda `LEGAL_VERSION` de `constants/legal.ts`: **sha256 (12 hex) del contenido de T&C + Política**, derivado del texto por `sync-legal.mjs` y no mantenido a mano — un número manual se olvida justo al editar el documento, que es cuando importa. Entran solo esos dos documentos: arrepentimiento y baja de cuenta son informativos, no se aceptan, y moverían la versión invalidando aceptaciones por un cambio que no toca lo aceptado. Se escribe en los tres caminos de alta vía `acceptanceFields()` en `AuthContext`, que **nunca escribe `false` ni `null`** para que una llamada parcial no pise una aceptación anterior. ⚠️ **NO backfilleadas**: las cuentas previas aceptaron de verdad pero no se registró ni cuándo ni qué versión, y poner una fecha o un hash inventado sería fabricar la constancia. `scripts/add-terms-version.sql` — **CORRIDO y VERIFICADO el 13/08/2026**: `information_schema.columns` devuelve las 2 filas (`timestamptz` / `YES` y `text` / `YES`).
- `age_confirmed` (boolean, NOT NULL DEFAULT false) — el Usuario declaró tener 18 años o más al crear la cuenta (T&C §3.1). Antes del 13/08/2026 la cláusula afirmaba una declaración que **nunca se pedía**: `birth_date` es opcional para el usuario, obligatoria solo para el coach, y en ningún caso se validaba que fueran 18. Se escribe en los tres caminos de alta (email, Google, Apple) desde `RegisterScreen`, y desde `CoachLoginScreen` por la aceptación implícita de esa pantalla. ⚠️ **NO backfilleada**: las cuentas previas quedan en `false` porque efectivamente no declararon nada — poner `true` sería fabricar una constancia inexistente. Del lado coach hay además un chequeo duro contra `birth_date` en `CoachApplicationScreen` (`ageFromIso() < 18` corta la postulación), que es el único punto del alta con una fecha real. `scripts/add-age-confirmation.sql` — **CORRIDO y VERIFICADO el 13/08/2026**: `information_schema.columns` devuelve `age_confirmed` / `boolean` / `is_nullable = NO` / `column_default = false`.
- `deleted_at` (timestamptz, nullable) — no nulo = **lápida**: el usuario se dio de baja, la fila quedó vaciada de datos personales (`name = 'Usuario eliminado'`, el resto en NULL) y la cuenta de `auth.users` ya no existe.
- ⚠️ **`email` es NOT NULL** (verificado en la primera baja real, 06/08/2026: `email: null` hacía fallar la anonimización). Por eso la lápida escribe un placeholder opaco `deleted-<uuid>@vita.invalid` en vez de NULL — un literal fijo chocaría contra el UNIQUE en la segunda baja, y `.invalid` es un TLD reservado que nunca resuelve. Efecto secundario deseable: **libera el email original** para que la persona pueda registrarse de nuevo.
- Usuarios y coaches viven en la misma tabla, diferenciados por `role`

#### Baja de cuenta (`delete-account`, 06/08/2026)
Apple exige (guideline **5.1.1(v)**) que toda app con registro permita borrar la cuenta desde adentro; sin esto la app se rechaza en iOS. **Modelo: borrado + anonimización, NO cascade.**
- **Se borra** el contenido personal: `journal_entries`, `gratitude_entries`, `mood_entries`, `mood_suggestions`, `user_habits`, `user_quiz_answers`, `resource_reminders`, `resource_completions`, `saved_resources`, `pinned_resources`, `resource_saves`, `resource_feedback`, `favorite_coaches`, `notifications`, y el avatar del bucket.
- **Se conserva anonimizado** lo que pertenece también a un tercero o hay que retener: `bookings` (respaldo fiscal + historial del coach), `reviews` (reputación del coach — si se borraran, alguien podría darse de baja para eliminar una reseña negativa), `messages` y `salas`. Todo apunta a la lápida y muestra "Usuario eliminado". `session_notes` también se conservan (⚠️ a confirmar con abogado, ver changelog).
- **Sesiones futuras**: se cancelan y disparan el reembolso vía `trg_mark_refund_on_cancel`. No se marca `cancelled_late` a propósito — darse de baja no es cancelar tarde. Un **coach con sesiones agendadas no puede darse de baja**: se le pide cancelarlas antes, para que cada cancelación avise y reembolse.
- **3 FKs cambiadas** (`scripts/add-account-deletion.sql`, **corrido y verificado el 06/08/2026**). Sin esto, un `deleteUser` a secas rompía cosas:
  1. `profiles.id → auth.users` era **CASCADE** → borraba la lápida y reventaba las FKs NO ACTION de `reviews`/`messages`/`salas`. **FK dropeada.**
  2. `bookings.user_id → auth.users` era **CASCADE** → **borraba todas las reservas**. **Repuntada a `profiles(id)` NO ACTION.**
  3. `analytics_events.user_id` era **NO ACTION** → **bloqueaba** el borrado con error de FK. **Ahora SET NULL** (conserva la métrica, pierde la identidad).
- Lo que sigue colgando de `auth.users` con CASCADE es todo contenido personal y está bien que se vaya solo: `favorite_coaches`, `gratitude_entries`, `mood_entries`, `mood_suggestions`, `resource_completions`, `user_events`, `user_habits`.
- **`user_events` SÍ existe en prod** (cuelga de `auth.users`), contra lo que concluyó la sesión 79. El código no la usa: la analítica va por `analytics_events` vía `registrarEvento`.
- `email_verified_at` (timestamptz, nullable) — **cuándo la persona probó tener acceso a su casilla**, por código OTP en el alta de coach. NULL = nunca (todas las filas viejas y todos los usuarios finales, que no pasan por ese paso). Agregada 31/08/2026 (`scripts/add-email-verified-at.sql`, **CORRIDA el 31/08/2026**).
  - **La escribe `screens/VerificarMailScreen.tsx`**, que sirve a dos caminos con `modo`: `alta` (registro de coach — abandonar cierra la sesión) y `gate` (usuario que va a reservar — abandonar solo vuelve). La regla de cuándo hace falta vive en `lib/emailVerificado.ts`, con tests.
  - 📝 **A quien entró con Google o Apple no se le pide**: el proveedor ya entregó el mail verificado. Se detecta por `app_metadata.provider`/`providers`.
  - 🔴 **`necesitaVerificarMail` falla ABIERTO**: ante cualquier error del select devuelve `false` y deja pasar. Si la columna no existe todavía —este script sin correr— un gate que se activara con ese error dejaría a **todo el mundo sin poder reservar** por un problema de esquema.
  - ⚠️ **NO es `auth.users.email_confirmed_at`.** El proyecto tiene **"Confirm email" APAGADO**, así que Supabase auto-confirma a todos en el alta: esa columna viene llena siempre y no distingue nada. Y prenderlo es del proyecto entero — se llevaría puesto el registro de usuarios, que a propósito no se quiere frenar con un muro de mail. Por eso la verificación del coach se hace por OTP desde la app (`screens/CoachVerificationScreen.tsx`) y el resultado se anota acá.
  - 📝 Para qué sirve: `resetPassword` manda un mail, así que una dirección mal tipeada deja una cuenta **irrecuperable y sin aviso**; y la moderación de coaches puede ver si la casilla desde la que se postula alguien se comprobó o se dio por buena.
- `avatar_url` (text, nullable) — columna vieja, sin uso hasta el 01/07/2026. Ahora es la foto de perfil real de cualquier `profiles` (coach o usuario), subida como archivo a Supabase Storage (bucket `avatars`, path `{auth.uid()}/avatar.jpg`, upsert siempre true). Coaches suben desde `CoachProfileScreen.tsx`, usuarios desde `EditProfileScreen.tsx` (mismo patrón). Se muestra en: `ProfesionalScreen.tsx` y `app/search3.tsx` (coach visto por un usuario), `ProfileOwnScreen.tsx` y el avatar del top bar en `app/(tabs)/index.tsx` (perfil propio), y donde cualquiera de los dos roles ve a la otra parte — `SalaScreen.tsx` (chat, ambos avatares), `CoachReservasScreen.tsx` (usuario que reservó), `CoachChatsScreen.tsx` y `SessionsScreen.tsx` (listas de chat). Todos con fallback a iniciales/ícono genérico si `avatar_url` es null. Requiere `scripts/add-avatar-upload.sql` (bucket + RLS de storage + política de UPDATE en `profiles`), corrido en Supabase el 02/07/2026.

#### ⚠️ La sesión anónima NO es una cuenta (01/09/2026)
Las 8 pantallas de herramientas llaman a `ensureAnonSession()` al montarse (`supabase.auth.signInAnonymously()`) para poder escribir en `resource_completions`. Hasta el 01/09/2026 **nada en el repo distinguía esa sesión de una real** (`grep is_anonymous` no daba un solo resultado) y `AuthContext` hacía `isLoggedIn = !!user`, con tres consecuencias:
- 🔴 `volcarPendiente` escribía las respuestas del onboarding bajo el id **anónimo** y las marcaba como volcadas; al registrarse de verdad, `signUpWithEmail` crea un id distinto y las respuestas quedaban varadas en una fila fantasma. **Es el mismo bug que se arregló el 31/08, entrando por otra puerta.**
- 🔴 `onboarding_registro` se emitía para la sesión anónima, inflando la conversión con gente que nunca se registró.
- 🔴 Los guards son `if (!user) requestAuth()`, así que quien abría una herramienta **cruzaba el muro de la cuenta**. El changelog decía "Booking requiere sesión real (no anónima)" desde hacía tiempo; ese guard no existía en el código.

**Corrección**: `esSesionAnonima()` en `context/AuthContext.tsx` — para todo lo que no sea anotar el uso de una herramienta, una sesión anónima **es** no tener cuenta, y `user` queda en null. Las herramientas no se ven afectadas porque toman el id de `ensureAnonSession()` directamente, no del contexto.

✅ **CONFIRMADO contra la base el 01/09/2026: `select count(*) from auth.users where is_anonymous = true;` da CERO.** `signInAnonymously()` nunca funcionó — cada apertura de una herramienta hacía un round-trip que fallaba y el `.catch(() => {})` de las ocho pantallas se lo comía en silencio.
- ⚠️ **Lo que NO significa**: las herramientas sí registraban completaciones para quien tenía cuenta. `ensureAnonSession()` devolvía el id de la sesión existente cuando la había, y solo intentaba el alta anónima si no. Lo que nunca se registró es el uso **sin cuenta**.
- **`ensureAnonSession()` se eliminó** y lo reemplaza `usuarioActualId(): Promise<string | null>`, que no intenta ningún alta. Ya no hace falta: lo único que la sesión anónima habilitaba era medir sin cuenta, y eso ahora lo cubre `lib/analytics.ts` con un id de dispositivo. `recordCompletion` acepta `null`: **emite el evento siempre** y escribe la fila solo si hay cuenta (`user_id` es FK).
- ⚠️ **Si alguna vez se quieren altas anónimas de verdad, no alcanza con prender el switch del panel**: el trigger de abajo escribe `profiles.email`, que es NOT NULL, y un usuario anónimo no tiene email.

#### Trigger de alta (`public.handle_new_user`, sobre `auth.users`)
Es lo único que crea filas en `profiles` — no hay ningún INSERT a `profiles` en el código de la app. Corre en la **misma transacción** que la inserción en `auth.users`: si falla, no se crea ninguna de las dos y el registro devuelve `Database error saving new user`. Corolario útil para debuggear: **si existe la fila de `auth.users`, el trigger no falló.**
- Escribe `id`, `email`, `name`, `role` (siempre `'user'`) y, desde el 16/08/2026, `avatar_url`.
- `name`: `coalesce(name, full_name, 'Usuario')` sobre `raw_user_meta_data`. `avatar_url`: `coalesce(avatar_url, picture)`. Dos claves para lo mismo a propósito — Supabase escribe la foto de Google como `avatar_url`, pero el claim OIDC crudo se llama `picture` y según el proveedor viene uno, el otro o los dos. Todos con `nullif(…, '')`: estos campos llegan como string vacío —no NULL— cuando el proveedor no los da, y `''` no lo corta un `coalesce`.
- `avatar_url` guarda la **URL pública completa**, no un path de Storage (ver `getPublicUrl` en `EditProfileScreen`/`CoachProfileScreen`), así que una URL de `googleusercontent` entra sin adaptar nada del lado de la app.
- ⚠️ **`accepted_terms` y `age_confirmed` NO los escribe el trigger** — quedan en `false` y los persiste `markAccepted()` desde el cliente, después del alta.
- 📝 **`delete-account` también se usa para las altas de coach abandonadas** (31/08/2026, `hooks/useCerrarSesionAlSalir.ts`). Cancelar la verificación o la postulación **borra la cuenta**, no solo cierra sesión: si quedara, tendría `role = 'user'` y ninguna fila en `coaches`, y al reintentar con el mismo mail el guard de `CoachLoginScreen` la leería como un usuario final que quiere postularse — dejando a la persona bloqueada de su propia dirección por haber cancelado. Como la función reescribe el email a `deleted-<uuid>@vita.invalid`, la dirección queda libre. ⚠️ Solo se borra si hay marca de alta en curso (`lib/altaCoach.ts`), que se pone únicamente para cuentas que ese flujo acaba de crear.
- 🔴 **Nunca borres usuarios desde el panel de Authentication.** Borra `auth.users` y **no** toca `profiles` (la FK entre las dos se dropeó el 06/08). La fila huérfana conserva el email real, que es UNIQUE, y el trigger no puede insertar la fila nueva: registrarse otra vez con ese mail falla con `Database error saving new user`, un mensaje que no dice nada del verdadero motivo. La baja desde la app (`delete-account`) no tiene el problema porque reescribe el email como `deleted-<uuid>@vita.invalid`. Si hay que limpiar cuentas de test a mano, borrá **las dos** filas.

### `coaches`
- `id` (uuid, PK) ⚠️ — **NO es lo mismo que `profiles.id`**
- `profile_id` (uuid, FK → `profiles.id`) — el dato que conecta con el resto del sistema
- **`slug`** (text, NOT NULL, UNIQUE) — el identificador del profesional en una URL pública: `vitaapp.com.ar/c/sofia-herrera`. `scripts/add-coach-slug.sql` — ✅ **CORRIDO y VERIFICADO el 08/09/2026**: 34 coaches, 34 con slug, 34 distintos; los acentos resueltos sin `unaccent`; y la consulta real de la página pública (buscar por slug + `profiles!inner`) anda **sin sesión**. Se deriva del nombre con `public.slugify()` (sin `unaccent`: la extensión puede no estar y `translate` cubre el español) y lo pone el trigger **`trg_coaches_set_slug`**, que es **`before insert` y NADA MÁS**. 🔴 **No sigue al nombre a propósito**: un link ya compartido por WhatsApp tiene que seguir andando el mes que viene, y si el slug siguiera al nombre cada corrección de tipeo rompería todos los links repartidos. Los choques se resuelven con sufijo numérico (`sofia-herrera-2`). 🟢 **El coach no lo puede escribir y no hizo falta hacer nada**: `lock-privileged-columns.sql` dejó la tabla con `revoke update` + grants columna por columna, así que **toda columna nueva de `coaches` nace de solo lectura** — está bien que así sea, porque un slug editable es una invitación a hacerse pasar por otro. 📌 La ruta lleva prefijo (`/c/<slug>`) para que un slug como `terminos` no choque con las páginas que ya viven en el dominio (`docs/hosting.md`). ⚠️ **Asimetría a tener presente**: el SELECT de `anon` sobre `profiles` ahora es **por columnas** y sobre `coaches` sigue siendo **por tabla**, así que una columna sensible nueva en `coaches` no queda cerrada sola.
- `specialty`, `bio`, `price_per_session`, `nationality`, `verified`, `created_at`
  - `bio` (text, nullable): presentación breve del coach. Desde 05/07/2026 es **editable por el coach** (sección "Presentación" en `CoachProfileScreen`, editor inline máx. 400 chars, mismo patrón que el editor de precio) y **visible en el perfil público** (`ProfesionalScreen`, arriba de los chips de temas). Antes existía la columna pero no había UI para escribirla ni se mostraba a los usuarios.
- `application_video_url` (text, nullable) — link al video de presentación que el coach pega al postularse (YouTube, Drive, etc.). Artefacto de revisión de la postulación, no se muestra a usuarios.
- **`application_status`** (text, NOT NULL DEFAULT `'pendiente'`, CHECK IN `pendiente`/`aprobada`/`rechazada`), **`application_notes`** (text, nullable — motivo del rechazo, **le llega al coach**), **`application_reviewed_at`** (timestamptz, nullable). Agregadas 15/08/2026 (`scripts/add-application-status-and-audit.sql`).
  - ⚠️ **No confundir con `verified`, son preguntas distintas y por eso son dos columnas.** `verified` es *"¿aparece en el catálogo?"* (`coachesCache` filtra por ella); `application_status` es *"¿en qué estado está la revisión?"*. Se separan porque despublicar a un coach aprobado tras un reporte lo deja en `verified=false` / `application_status='aprobada'`, que es la verdad. Con una sola columna, **revocar y rechazar serían el mismo acto** y se perdería por qué está afuera del catálogo. Antes de esto, `verified=false` significaba a la vez "nadie la miró" y "la miramos y no", así que rechazar dejaba la fila igual que antes de rechazarla y la postulación volvía a la cola para siempre — por eso el panel solo podía aprobar.
  - **Backfill:** `verified=true` → `'aprobada'`. `verified=false` quedó en `'pendiente'` y **no** en `'rechazada'`: nadie los rechazó, no había con qué. Mismo criterio que `age_confirmed` — fabricar una constancia que no existió es peor que no tenerla.
  - Las tres son **no escribibles por el cliente**, gratis: al haber revocado el privilegio de tabla completa y otorgado columnas nombradas (puntos 20 y 21), una columna nueva no queda otorgada.
  - **`trg_reset_application_on_edit`** (BEFORE UPDATE): si la fila está en `'rechazada'`, cualquier update la devuelve a `'pendiente'` y limpia `application_reviewed_at`. `application_notes` **se conserva** — es el contexto que necesita quien revisa la segunda vuelta. Va por trigger porque el coach no puede (ni debe) escribir `application_status`. ⚠️ Se dispara con **cualquier** update y no solo con el de las columnas revisadas: la versión angosta dejaba varada para siempre a una persona rechazada por sus temas, que viven en `coach_topics` —otra tabla— y cuya corrección no tocaría ninguna columna vigilada. El peor caso de la versión ancha es una postulación que vuelve a revisión de más: visible y barato.
- `video_url` (text, nullable) — URL pública del video de perfil real, subido como archivo desde `CoachProfileScreen.tsx` a Supabase Storage (bucket `coach-videos`). Visible para cualquier usuario en `ProfesionalScreen.tsx`. Distinta de `application_video_url` a propósito: un reproductor nativo (`expo-video`) necesita una URL de archivo directa, no un link de YouTube/Drive. Agregada el 30/06/2026 (`scripts/add-coach-video-upload.sql`).
- `instant_booking` (boolean, NOT NULL DEFAULT false) — modalidad de reserva del coach, editable desde el switch "Modalidad de reserva" en `CoachProfileScreen.tsx`. Si es `true`, las reservas nuevas de ese coach nacen con `bookings.status = 'confirmada'` directo (sin pasar por `'pendiente'` ni por aceptación manual). Agregada el 01/07/2026 (`scripts/add-coach-instant-booking.sql`, corrido en Supabase el 02/07/2026).
- `availability_status` (text, NOT NULL DEFAULT 'activo') — CHECK IN ('activo', 'en_pausa'). Controla si el coach aparece en búsquedas. 'en_pausa' lo oculta del catálogo sin afectar sesiones existentes. Editable desde el toggle "Disponible / En pausa" en `CoachProfileScreen.tsx`. `coachesCache.ts` filtra por `availability_status = 'activo'`. Agregada el 07/07/2026 (`scripts/add-coaches-availability-status.sql`).
- ⚠️ **Dato sucio encontrado 16/07/2026**: hay filas en `coaches` cuyo `profiles.role` vinculado es `'user'`, no `'coach'` (cuentas de test/dev con fila de coach creada sin cambiar de rol). No rompen nada por sí solas, pero cualquier query que dependa de `coaches → profiles` bajo RLS (la política de `profiles` solo expone perfiles con `role = 'coach'`) las deja invisibles en silencio — ver el hallazgo documentado en `coach_resources` más abajo. Si vas a elegir un coach "cualquiera" para un seed/test, filtrá `profiles.role = 'coach'`.
  - 🔴 **27/08/2026 — resuelta la causa raíz, no solo el síntoma.** El "dato sucio" de arriba no era solo de seeds de test: `admin-actions` (`set_coach_verified`) aprobaba postulaciones escribiendo `coaches.verified = true` pero **nunca tocaba `profiles.role`** — y `role` es lo que `AuthRedirect`/`app/index.tsx` usa para enrutar a `(coach)` vs `(tabs)`. Un coach real, aprobado por el camino real de la app, se logueaba y cae para siempre en la app de USUARIO, sin ningún error. Nadie lo había pisado porque **todos** los coaches "que funcionan" hasta hoy se sembraron por SQL directo con `role='coach'` ya puesto a mano — ninguno pasó nunca por una aprobación real de punta a punta hasta que Joaquín hizo la primera. Arreglado: `set_coach_verified` ahora también hace `profiles.update({role:'coach'})` cuando `verified=true` (deployado, `admin-actions` v20). No se toca al revocar (`verified=false`) — revocar saca del catálogo, no convierte de nuevo en usuario final, mismo criterio que ya usa esta función para no reescribir `application_status` al revocar.

### `coaches.has_matricula` (03/09/2026)
Derivada de `coach_credentials`: `true` si tiene al menos una credencial `kind = 'matricula'` con `status = 'verificada'`. `scripts/add-coach-has-matricula.sql` — ✅ **CORRIDO y VERIFICADO el 03/09/2026**: la columna existe, `authenticated` tiene **solo SELECT** (la lee el catálogo, no la escribe el coach), y el backfill coincide con las credenciales verificadas — **0 filas de diferencia**. La mantiene `trg_sync_matricula`, y su `update` está **revocado** — como flag podría contradecir a los datos y el catálogo anunciaría una matrícula que nadie verificó, que es el daño exacto que viene a evitar. Mismo criterio que `accepts_international`.
- **Por qué**: `docs/encuadre-salud-y-responsabilidad.md` §2. La Ley 23.277 reserva diagnóstico y tratamiento a quien tiene matrícula, y la plataforma muestra coaches y psicólogos/as bajo el mismo rótulo. El 01/09 se agregó la distinción al **perfil**, pero se puede reservar sin entrar nunca ahí — el catálogo y el buscador no sabían nada. Esta columna es lo que les permite saberlo sin consultar credenciales por cada tarjeta.
- **El trigger va sin `of` en columnas** a propósito: verificar una matrícula la prende, y rechazarla o editarla la apaga — `trg_reset_credential_on_edit` devuelve la fila a `pendiente`, y ese reset tiene que apagar la marca del catálogo.
- 🔴 **De paso apareció un defecto peor en `app/search3.tsx`.** `inferType()` decidía "Psicólogo" con `specialty.includes('psicolog')` sobre un **campo de texto libre que escribe el propio profesional**, y había un filtro por ese tipo. O sea que cualquiera que escribiera "psicología positiva" en su presentación aparecía bajo el filtro Psicólogo — y quien filtra por Psicólogo es justamente quien busca terapia. La app hacía una afirmación profesional por su cuenta, desde una fuente que nadie verifica. Ahora la palabra clave **no alcanza**: hace falta además `has_matricula`. ⚠️ Sigue sin ser perfecto —`has_matricula` no dice de qué profesión es— y cerrarlo pide un campo estructurado de profesión, que es decisión de producto.

### `coach_credentials`
Títulos, matrículas y certificaciones de un profesional. `scripts/add-coach-credentials.sql` — ✅ **CORRIDO y VERIFICADO el 28/08/2026**, los 8 chequeos contra la base: `cols_update = 6` y **`puede_escribir_status = false`** (el coach NO puede tocar su propio `status`, o sea que la verificación no es decorativa), **`bucket_publico = false`**, `fugas_en_vista = 0` (la vista no expone `file_path`, `review_notes` ni `status`), `tipos_ok = true`, `rls_activa = true`, `policies = 4`, `trigger_reset = 1`. La consulta está en el bloque de verificación del script. `admin-actions` **v22 deployada** con las tres acciones nuevas.
- 📝 **El primer intento del script abortó**, y a propósito: el paso que extiende el CHECK de `notifications.type` anclaba en `]::text[]`, un sufijo que este Postgres no imprime. El guard frenó la corrida entera en vez de dejar el CHECK borrado. Corregido anclando en `::text]`, que aparece una sola vez porque solo el último elemento del array está pegado al corchete. De paso quedó confirmado que `postulacion_aprobada`/`postulacion_rechazada` **sí están** en el CHECK, cosa que este archivo no listaba.

- `id` (uuid, PK), `coach_id` (uuid → **`coaches.id`**, no `profile_id`), `kind` (CHECK IN `titulo`/`matricula`/`certificacion`), `title`, `institution`, `year` (smallint), `registration_number` (la matrícula), `file_path`, `status` (CHECK IN `pendiente`/`verificada`/`rechazada`, default `pendiente`), `review_notes` (le llega al coach), `reviewed_at`, `created_at`.
- 🔴 **EL DOCUMENTO NO ES CONTENIDO PÚBLICO.** El coach sube el archivo, un admin lo mira, y lo que se publica es el dato en texto con la marca de verificado. Tres motivos: (1) un diploma lleva nombre completo, muchas veces DNI y firma — publicarlo expone datos personales del profesional a cualquier visitante; (2) una imagen es un canal de texto que **ningún regex puede moderar**, o sea que saltea `contactInfoGuard` poniendo el WhatsApp en una esquina; (3) un JPEG no prueba nada, y mostrarlo sería que Vita preste credibilidad a algo que no chequeó. Lo que verifica de verdad a un psicólogo en Argentina es la **matrícula**, que es un número consultable.
- 🔴 **`revoke update` + `grant update` por columnas nombradas.** RLS dice qué FILAS, no qué columnas: sin esto el coach podría hacer `update ... set status='verificada'` sobre su propia fila y auto-verificarse. Mismo mecanismo que ya usa `coaches` para `verified`. **La verificación que importa**: `information_schema.column_privileges` tiene que devolver 6 columnas para `authenticated`, sin `status`.
- 🔴 **`trg_reset_credential_on_edit`**: editar cualquier campo revisado devuelve la fila a `pendiente`. Sin esto el ataque es de un paso: cargar "Coach de hábitos", esperar la verificación, editarla a "Lic. en Psicología". Mismo criterio que `trg_reset_application_on_edit`.
- **`coach_credentials_public`** (vista) — es **vista y no policy** porque RLS filtra filas y no columnas: una policy pública sobre la tabla dejaría `file_path` y `review_notes` legibles con la anon key. Expone solo las columnas seguras de las filas `verificada`. La lee `ProfesionalScreen` vía `lib/coachCredentials.ts`.
- **Bucket `coach-credentials`** — 🔴 **el primero PRIVADO del proyecto** (los cinco anteriores son públicos), 10MB, jpeg/png/webp/pdf. `getPublicUrl` —el patrón que se copia en toda la app— **acá no sirve**. El admin lo mira con una URL firmada de 5 minutos que emite `admin-actions`, y **abrirlo queda auditado** en `admin_audit_log`: es un documento de identidad y tiene que constar quién lo vio. A propósito no hay policy de storage para admins — la única puerta es la edge function con service role.
- `notifications.type` suma `credencial_verificada` y `credencial_rechazada`. El script **no asume** el nombre ni el contenido del CHECK: los lee de `pg_constraint` y aborta si la forma no es la esperada. **Desde el 29/08/2026 también `recurso_publicado` y `recurso_rechazado`** (`add-resource-status-notification.sql`) — los inserta el trigger `trg_notify_resource_status` de `coach_resources` (ver esa sección). Ese script sí recrea el CHECK entero con la lista explícita en vez de parchear el string, justamente por el problema del anclaje que se documenta arriba.
- ⚠️ **Falta el párrafo en la política de privacidad**: se recibe y se conserva un documento de identidad profesional. Va con los placeholders que esperan al abogado.

### `user_consents` (03/09/2026)
Consentimiento **expreso y específico** para tratar datos sensibles (Ley 25.326 art. 7). `scripts/add-user-consents.sql` — ✅ **CORRIDO y VERIFICADO el 03/09/2026**: la tabla y la vista existen (401 con la anon key contra el 404 de una tabla inventada), `authenticated` devuelve **solo SELECT**, y `pg_policy` devuelve una sola fila (`user_consents_select_own`, cmd = `r`). El porqué completo está en `docs/consentimiento-datos-sensibles.md`.

- `id` (uuid, PK), `user_id` (uuid → `auth.users`, **on delete cascade**), `consent_type` (CHECK IN `datos_sensibles_bienestar`), `granted` (bool), `granted_at`, `revoked_at`, `policy_version` (el `LEGAL_VERSION` de `constants/legal.ts`), `created_at`. Índice `user_consents_lookup (user_id, consent_type, created_at desc)`.
- 🔴 **La primera corrida dejó un hueco, y quedó anotado porque es instructivo.** El `revoke` decía `insert, update, delete` y la verificación devolvió igual **SELECT + TRUNCATE + REFERENCES + TRIGGER**: Supabase los concede por default sobre todo `public`. **`TRUNCATE` es el que importa porque la RLS NO lo filtra** — las policies se evalúan sobre select/insert/update/delete, así que `user_consents_select_own` no protegía nada contra vaciar la tabla entera. Se revocó aparte y la verificación quedó limpia. 📌 El hueco es de **todo el proyecto**: ver `scripts/revoke-truncate-trigger-references.sql`.
- 🔴 **EL TITULAR NO PUEDE ESCRIBIR SU PROPIA FILA.** `insert`/`update`/`delete` revocados para `authenticated`; RLS con una sola policy, `user_consents_select_own`. Se escribe **únicamente** desde la edge function `user-consent`, que verifica el JWT y deja que la base ponga la fecha con su reloj. **Es la corrección del defecto que este mismo archivo documenta sobre `profiles.accepted_terms`**: esas cuatro columnas las escribe el cliente, o sea que son falsificables por su titular. El Decreto 1558/2001 admite el consentimiento por un medio no escrito pero exige asegurar **autoría e integridad de la declaración**, y una fila que el titular puede fechar como quiera no asegura ninguna de las dos. La verificación que importa: `information_schema.role_table_grants` tiene que devolver **solo SELECT** para `authenticated`.
- **Se INSERTA una fila por acto, no se actualiza la anterior.** La historia (otorgado → revocado → otorgado) es lo que se prueba; un `update` la borraría. Por eso es tabla y no dos columnas en `profiles`.
- **`user_consents_current`** (vista, `security_invoker = true`) — el último acto por usuario y tipo, con `distinct on`. Hereda la RLS de la tabla. Es lo que lee la app; sin ella cada pantalla haría el `distinct on` a mano y alguna se lo iba a olvidar.
- **Qué cubre `datos_sensibles_bienestar`:** ánimo, diario, gratitud **y qué recursos usa la persona**. 🔴 Lo último entró el 03/09/2026 por **TJUE C-184/20**: los datos que por "una operación intelectual de comparación o deducción" revelan información sensible **son** categoría especial. "Escuchaste tres audios de ansiedad" dice lo mismo que un check-in, por deducción en vez de por declaración.
- 📝 **Baja de cuenta, sin resolver:** la tabla **no** se agregó a `PERSONAL_TABLES` de `delete-account` —la constancia de que el tratamiento pasado fue lícito sigue haciendo falta— pero el `on delete cascade` contra `auth.users` la borra igual cuando la function elimina la cuenta. Para conservarla habría que cambiar la FK. Es una pregunta para la consulta legal, no una decisión de implementación.

### `user_quiz_answers`
- `id` (uuid, PK)
- `user_id` (uuid, UNIQUE, FK → `profiles.id` ON DELETE CASCADE)
- `topic` (text, nullable) — respuesta Q1 del quiz: emocion | relaciones | trabajo | salud | proposito
- `professional_type` (text, nullable) — respuesta Q2: coach | psicologo | nutricionista | any
- `budget` (text, nullable) — respuesta Q3: low | mid | high | flex
- `axis` (text, nullable) — **el eje que la persona declaró en el onboarding** (`cuerpo` | `mente` | `alma`, con CHECK; mismo vocabulario que `resource_axes` y `resource_proposals.axes`). Agregada 31/08/2026 (`scripts/add-quiz-declared-axis.sql`, **CORRIDA y VERIFICADA por Andre el 31/08/2026** — columna `text` nullable y el CHECK puesto, confirmado contra la base). Nullable a propósito: `QuizScreen` no pregunta universo, así que en esas filas queda null y el eje se sigue deduciendo del `topic`.
- `created_at`, `updated_at`
- Una fila por usuario (UNIQUE user_id); se hace upsert al re-hacer el quiz.
- RLS: solo el dueño puede leer/escribir su propia fila (authenticated).
- Escrita por `QuizScreen.tsx` al completar el quiz (además de AsyncStorage) y, **desde el 31/08/2026, también por el onboarding guiado** vía `lib/onboardingRespuestas.ts` → `lib/quizPendiente.ts`. Leída por `hooks/useRecommendedResource.ts`, que mapea `topic` → eje (cuerpo/mente/alma) + etiqueta.
- ⚠️ **El onboarding no puede escribir acá en el momento en que pregunta**: `user_id` es FK a `profiles` y en las pantallas 3-4-5 todavía no hay cuenta. Las respuestas se encolan en AsyncStorage (`lib/quizPendiente.ts`, compartida con `QuizScreen`) y `AuthContext` las vuelca en el `onAuthStateChange`, **una sola vez** (flag `volcado` en el propio registro local). La cola **mergea** en vez de pisar —el onboarding solo produce `topic`, el quiz los tres campos— y el upsert manda únicamente las columnas que existen, así que un volcado parcial no borra lo que ya había. Sin ese flag, cada login reescribiría la respuesta del onboarding y **pisaría el quiz que la persona hizo después adentro de la app**, que es más nuevo y más deliberado. Si el upsert falla no se marca, así que se reintenta en el login siguiente.
- ✅ **La contradicción entre las dos taxonomías se resolvió con `axis` (31/08/2026), guardando las dos cosas en vez de sacrificar una.** `hooks/useRecommendedResource.ts` usa el **eje declarado** para decidir QUÉ recomendar —es lo que la persona eligió con su nombre— y el **topic** para decidir CÓMO nombrárselo. Antes había que elegir: o el mapa respetaba el universo y la etiqueta mentía ("algo para tu salud" a quien habló de sexualidad), o respetaba el significado y el recurso salía de un eje que la persona no eligió. El detalle de la divergencia, que sigue existiendo y ahora no molesta: **`topic` y el onboarding agrupan distinto, y en tres casos se contradicen** (`CATEGORIA_A_TOPIC` en `lib/onboardingRespuestas.ts`): `sexualidad` y `vinculos` el onboarding las pone en cuerpo y mente, pero `relaciones` cae en alma; `trabajo` el onboarding lo pone en alma y `trabajo` cae en mente. El mapa va **por significado**, para que la etiqueta de la recomendación diga algo cierto — la consecuencia es que en esos tres el eje del recurso sugerido no coincide con el universo que la persona eligió. El mapa sigue yendo **por significado** a propósito, porque es lo que hace que la etiqueta diga algo cierto — y ya no cuesta nada, porque el eje va aparte.
- ⚠️ **`useRecommendedResource` consulta con `select('*')` y no por nombre de columna**, a propósito: pedir `axis` antes de correr el script devuelve error y `data` en null, o sea que un OTA que llegue antes de la migración dejaría de leer también el `topic` y apagaría la recomendación entera. Con `*` la app anda igual antes y después.
- 🔴 **El eje se valida antes de encolarlo** (`esEje` en `lib/onboardingRespuestas.ts`): `universo` llega por parámetro de ruta, y un valor que el CHECK rechace haría fallar el upsert — y como el volcado no se marca cuando falla, **reintentaría en cada login para siempre**.
- 📝 **Los `temas` elegidos siguen sin columna** y se quedan solo en AsyncStorage. No es que se descarten: es que todavía no hay dónde ponerlos.
- ✅ **`QuizScreen` usa la misma cola desde el 31/08/2026.** Antes su upsert hacía `if (!uid) return;`: quien hacía el quiz sin cuenta perdía las tres respuestas en silencio, y lo único que quedaba era `vive_quiz_topic` en AsyncStorage — una clave que **no leía nadie**, o sea nada. Y el quiz se puede hacer sin cuenta, porque se llega desde Profesionales, que es navegable como anónimo. Ahora hay un solo camino: se encolan siempre en `lib/quizPendiente.ts` y, si ya hay sesión, se vuelcan en el acto. `vive_quiz_topic` se eliminó.
- Agregada el 07/07/2026 (`scripts/add-user-quiz-answers.sql`).

### `salas`
- `id` (uuid, PK)
- `user_id` (uuid, FK → `profiles.id`)
- `coach_id` (uuid, FK → `profiles.id`) — es `coaches.profile_id`, NO `coaches.id`
- `room_url` (text) — generado automáticamente por trigger al insertar: `https://meet.jit.si/vita-<16hex>`
- `user_last_read_at` (timestamptz, nullable) — timestamp de la última vez que el usuario abrió la sala. NULL = nunca abrió. Se actualiza en `SalaScreen.init()` al entrar. Usado para el dot de "novedad" en el tab "Mis salas".
- `coach_last_read_at` (timestamptz, nullable) — ídem para el coach. Mismo mecanismo, actualizado cuando el coach entra a la sala desde su interfaz.
- `coach_archived` (boolean, **NULLABLE a propósito** — `scripts/add-archivar-salas.sql`, ✅ **CORRIDO y VERIFICADO el 27/08/2026** — `boolean` / nullable / sin default, o sea los tres estados vivos) — archivado de la bandeja del COACH, con **tres estados**: `true` lo archivó él, `false` lo sacó de archivados él (y gana sobre la regla automática), `null` nunca opinó y manda la regla. 🔴 Con `not null default false` el tercer estado no existiría y la regla automática **dejaría de correr el día uno**, porque todas las filas nacerían diciendo "el coach quiere verla activa". La regla automática es: el último mensaje es del sistema y tiene más de 30 días. ⚠️ Es del coach, no de la sala: el usuario sigue viendo su conversación normal. El día que él quiera lo mismo, la columna se llama `user_archived` y esta lógica no se toca.
- `created_at`
- Filas existentes al 25/06/2026: backfill aplicado con `UPDATE salas SET user_last_read_at = now(), coach_last_read_at = now()` — arranque limpio sin dots falsos. Filas nuevas nacen con NULL (correcto: si llega un mensaje antes de la primera apertura, el dot muestra).

### `messages`
- `id` (uuid, PK)
- `sala_id` (uuid, FK → `salas.id`)
- `sender_id` (uuid, FK → `profiles.id`)
- `content` (text) — encriptado con XOR+base64 antes de guardar (ver `lib/encryption.ts`)
- `sender_type` (text, default 'user') — CHECK constraint extendido vía ALTER TABLE (23/06/2026). Valores válidos: 'user' | 'coach' | 'system' | 'system_confirmed' | 'system_cancelled'. `system_confirmed` se inserta al confirmar una reserva (fecha, hora y motivo del usuario en segunda línea). `system_cancelled` se inserta al cancelar desde SalaScreen o CoachReservasScreen, y al cancelar conflictos automáticamente. 'system' es fallback para mensajes anteriores al 22/06/2026 (render gris cursiva, sin pill). `sender_id` es NOT NULL incluso en mensajes de sistema — queda el id de quien disparó la acción. El render (pill o texto plano) lo determina `sender_type`, no el contenido.
- `metadata` (jsonb, nullable) — agregada 13/07/2026 (`scripts/recursos-v2-migration.sql`) para la recomendación de recursos por chat. `sender_type='coach'` + `content` con el título/nota como fallback de texto + `metadata = { type: 'resource', resource_id, recommendation_id }`. `SalaScreen` chequea `metadata?.type === 'resource'` para pintar la card embebida en vez de la burbuja de texto normal; `metadata` null o `type` desconocido cae al render de siempre. Sin cambios de RLS — hereda las políticas existentes de `messages`.
- `created_at`
- **`get_last_messages_per_sala(sala_ids uuid[])`** (función, agregada 21/07/2026, `scripts/get-last-messages-per-sala.sql`) — devuelve el último mensaje (`content`, `sender_type`, `metadata`, `created_at`) de cada sala pedida en un solo round-trip (`DISTINCT ON`). Reemplaza el `Promise.all` de una query por sala que tenían `SessionsScreen` (tab Mensajes) y `CoachChatsScreen` (tab Chats) — con varias salas ese N+1 se sentía como lag real apenas la pantalla montaba. **Sin `SECURITY DEFINER`** a propósito: `messages` ya tiene RLS que deja leer al usuario autenticado los mensajes de sus propias salas, así que corriendo como invoker esa misma política sigue aplicando sola.

### `bookings`
- `id` (uuid, PK)
- `user_id` (uuid, FK → `auth.users.id`)
- `coach_id` (uuid, FK → `coaches.id`) ⚠️ — a diferencia de `salas.coach_id`, acá es el PK de `coaches`
- `sala_id` (uuid, FK → `salas.id`)
- `coach_name` (text)
- `coach_specialty` (text)
- `scheduled_date` (date)
- `scheduled_time` (text)
- **`origen`** (text, nullable, CHECK IN `link`/`app`/`catalogo`) — por dónde entró la reserva. La escribe `web-book` con `'link'`; **toda reserva que nace ahí viene del link público del coach por definición**, porque esa función solo la llama `/c/<slug>`. `scripts/add-booking-origen.sql` — ✅ **CORRIDO el 09/09/2026**. 🔴 **No se puede reconstruir**: una reserva sin marca es indistinguible de una del catálogo, así que se agregó antes de abrir el checkout. Para qué sirve: (1) cobrarle distinto al coach por los clientes que trae él —la comisión del link, todavía sin decidir— y (2) contestar *"¿cuántas reservas trajo el canal del coach?"*, que es la pregunta que decide si esa estrategia de lanzamiento sirvió. 📌 **Texto acotado y no booleano** a propósito: `es_del_link` alcanzaría hoy y obligaría a otra columna mañana (ya se ve venir el coach reservando a nombre de alguien). ⚠️ **`null` significa "no sabemos", NO "app"**: las filas viejas quedan en null y llamarlas `'app'` sería afirmar algo que nadie verificó — la app se marcará el día que se agregue a `BookingScreen_Confirm`.
- **`payer_fingerprint`** (text, nullable) — `sha256(sal + payer.id de Mercado Pago)`. La escribe `mp-webhook` al acreditar, y **solo ahí**: es el único momento en que el objeto del pago pasa por nosotros, así que **lo que entre sin esto queda ciego para siempre**. `scripts/add-payer-fingerprint.sql` — ✅ **CORRIDO y PROBADO CON PLATA REAL el 09/09/2026**, con `PAYER_FINGERPRINT_SALT` creada y `mp-webhook` en **v33**. 🔴 **Y se probó con el caso exacto que motiva la columna, que apareció solo**: 3 reservas, **1 pagador, 2 cuentas de la app distintas** — Andre probó el checkout web con su mail y con un alias, pagando las dos veces con la misma cuenta de MP. Es la señal del "lavado" de un cliente a una cuenta nueva, reproducida sin querer. La consulta que la encuentra está en el script (verificación 4 bis). 🔴 **Para qué**: al coach se le va a cobrar menos por los clientes que trae él, y eso crea el incentivo de pedirle a un cliente que VIVE le consiguió que se haga una cuenta nueva con su link. Con mail nuevo no hay forma de notarlo — salvo por el medio de pago, porque la misma persona casi siempre paga con la misma cuenta de MP. 📌 **Es una huella y no el id**: lo que hay que responder es *"¿es el mismo pagador?"*, y para eso alcanza comparar; quien acceda a la base no se lleva identificadores de MP de nadie. La sal vive en `PAYER_FINGERPRINT_SALT` (variable de la edge function) — si cambia, las huellas viejas dejan de ser comparables con las nuevas. ⚠️ **Nunca hace fallar la acreditación**: sin sal, sin `payer.id` o con el hash roto queda en null y el pago sigue. 🔴 **Y hay que declararla antes de USARLA**: detectar que dos cuentas son la misma persona es una finalidad NUEVA bajo la Ley 25.326, así que va en la política de privacidad **antes del primer pago** — mismo criterio que `session_attendance`. Guardar la columna desde ya es correcto; negarle una tarifa a alguien con ella, recién cuando esté declarado.
- `amount` (integer) — 🔴 **NO es de confianza tal como lo inserta el cliente.** `BookingScreen_Confirm` lo escribe desde un parámetro de ruta, así que hasta el 26/08/2026 **el monto que se cobraba era el que mandaba el cliente**: `mp-create-payment` lo usaba como `unit_price` y como base de la comisión sin compararlo contra nada. Sin parámetro se cobraban $4500 (un default del mockup); con el parámetro modificado, cualquier precio. Los rieles internacionales nunca tuvieron el agujero — `usdt-create-payment` y `paypal-create-payment` derivan el precio de `coaches.price_usd` desde el primer día. Desde el 26/08 `mp-create-payment` hace lo mismo con `coaches.price_per_session`, **corrige la fila si llegó distinta** y loguea la corrección; y el cliente relee el precio al insertar. **No hay CHECK ni trigger que lo valide en la base**: la garantía son las tres funciones de cobro.
- `status` (text)
- `room_url` (text, nullable) — redundante, el room_url canónico está en `salas` (Jitsi, legacy)
- `duration_minutes` (integer, nullable) — duración de la sesión en minutos, copiado de `coach_weekly_pattern.slot_duration_minutes` al crear el booking. Agregada 02/07/2026 (`scripts/add-duration-minutes-meeting-url.sql`, corrida en Supabase el 07/07/2026).
- `meeting_url` (text, nullable) — la sala de Daily.co, **pelada**. La genera `create-meeting-room` al confirmar la sesión. Agregada 02/07/2026 (mismo script, corrida el 07/07/2026). 🔴 **No sirve para entrar y no hay que abrirla**: la sala es `privacy: 'private'`, y a una sala privada de Daily se entra con un **meeting token** —de un solo participante, vencido con la sesión— que se acuña en cada llamada a la función y viaja como `?t=` en la URL que ella devuelve. Lo que se guarda acá es la sala, que es estable; el pase de entrada no se guarda en ningún lado. Es también lo que cruza `session_attendance`. ⚠️ **El dominio es de la CUENTA**: desde el 28/08/2026 la cuenta de Daily es otra y el dominio es `veraapp.daily.co` — las 43 filas anteriores apuntan a `vive-app.daily.co`, que ya no es nuestro. No hizo falta limpiarlas: `create-meeting-room` **no toma la URL guardada como verdad**, le pregunta a Daily y corrige la fila la primera vez que alguien entra.
- `user_message` (text, nullable) — mensaje opcional que el usuario le escribe al coach antes de reservar
- `cancelled_by` (text, nullable) — quién canceló: `'usuario'` | `'coach'`. Null si no se canceló.
- `cancelled_late` (boolean, nullable) — `true` si el coach canceló con <24hs de anticipación; `false` si ≥24hs; `null` si canceló el usuario (no aplica) o si no se canceló.
- RLS para usuario: policy `users_cancel_own_booking` — permite UPDATE solo si `user_id = auth.uid()` y solo hacia `status='cancelada'`. Los coaches actualizan vía `coaches_can_update_own_bookings` (política preexistente).
- `created_at`

### `coach_availability`
- `id` (uuid, PK)
- `coach_id` (uuid, FK → `coaches.id`) ⚠️ — usa el PK de `coaches`, no `profile_id`
- `date` (date) — fecha puntual para la que el coach habilita horarios
- `time` (text) — horario en formato "9:00", "10:00", etc.
- `blocked` (boolean, NOT NULL DEFAULT false) — si es `true`, el slot no aparece para usuarios aunque exista; el coach puede reactivarlo desde CoachAvailabilityScreen (ícono de candado naranja).
- `created_at` (timestamptz)
- UNIQUE(coach_id, date, time) — un coach no puede tener el mismo slot dos veces
- RLS: SELECT abierto (anyone_can_view_availability) · ALL solo para el coach dueño (coaches_manage_own_availability, WITH CHECK `coach_id IN (SELECT id FROM coaches WHERE profile_id = auth.uid())`)

### `coach_availability_status` (VISTA)
- `coach_id` (= `coaches.id`), `status` (text: `'this_week'` | `'responds_24h'` | `NULL`). Reemplaza el cálculo cliente de disponibilidad que hacía `lib/coachAvailability.ts` (ahora lee esta vista en vez de calcular). `'this_week'` = tiene al menos un slot en `coach_availability` de los próximos 7 días, `blocked=false`, sin booking `confirmada`/`pendiente` que lo ocupe (compara `scheduled_date`/`scheduled_time` normalizando el padding de hora). Si no hay slot libre esta semana pero `coaches.availability_status = 'activo'`, cae a `'responds_24h'`. Si ninguna, `NULL`.
- `security_invoker = false` (corre como owner + GRANT SELECT a `anon`/`authenticated`) — mismo patrón que `coach_trending_stats`/`coach_rebooking_stats`. Necesario porque la subquery contra `bookings` con `security_invoker=true` corría con el RLS del usuario que consulta y solo veía sus propias reservas, marcando como libre un slot que en realidad ya estaba tomado por otro usuario (bug encontrado y corregido antes de correr en prod).
- Agregada 16/07/2026 (`scripts/add-coach-availability-view.sql`, **corrida y verificada el 16/07/2026** por Andre). El estado `responds_24h` todavía no se muestra en el deck de Conexiones (badge pendiente).

### `coach_weekly_pattern`
- `id` (uuid, PK)
- `coach_id` (uuid, FK → `coaches.id`) ⚠️ — mismo FK que `coach_availability`, no `profile_id`
- `day_of_week` (int) — 1=Lunes … 7=Domingo (CHECK BETWEEN 1 AND 7)
- `start_time` (text) — formato "H:MM" (ej: "9:00", "13:30"), igual que `coach_availability.time`
- `end_time` (text) — ídem; el end NO se incluye como slot de inicio en la generación
- `slot_duration_minutes` (int, default 60) — duración de cada turno generado
- `created_at` (timestamptz)
- RLS: SELECT abierto (anyone_can_view_pattern) · ALL solo para el coach dueño (coaches_manage_own_pattern, WITH CHECK `coach_id IN (SELECT id FROM coaches WHERE profile_id = auth.uid())`)
- Generación: `lib/availabilityGenerator.ts → generateWeeklySlots()` pobla `coach_availability` para los próximos 56 días con upsert ignorando duplicados

### ⚠️ Supuesto transversal: el profesional está en Argentina

No es un campo ni un flag — es el **sistema de coordenadas** de varias partes del schema, y conviene tenerlo escrito antes de que deje de ser cierto.

Lo que asume que el profesional atiende desde Argentina:
- **`bookings.scheduled_date` / `scheduled_time`** son texto sin zona y significan **hora de Argentina**. Un profesional en otro país cargando disponibilidad quedaría con turnos interpretados en horario argentino.
- **`coach_payout_accounts.cbu`** valida 22 dígitos (un IBAN no entra).
- **`coaches.price_per_session`** está en pesos.
- El **OAuth de Mercado Pago** asume cuenta argentina — y el split se crea contra el sitio MLA.
- Todo el tratamiento fiscal: VIVE factura su comisión, el profesional factura la sesión. Si el profesional estuviera afuera, VIVE pasaría a **importar** un servicio.

🔴 **De todo eso, lo único que toca el SIGNIFICADO de datos ya guardados es la hora.** El resto es aditivo: sumar IBAN es una fila más en un CHECK, saltear Mercado Pago ya está contemplado (el flujo maneja "coach sin MP"), otra moneda es otra columna. La hora no: cambiar de "esto es hora argentina" a "esto es hora del profesional" exige una zona por profesional, pasarla por cada punto de conversión, y **decidir qué significan las filas viejas**, que quedan ambiguas si nadie lo define antes.

📝 **No se agregó un parámetro de zona "por las dudas"** en `lib/time.ts` ni en `_shared/guarantee.ts`: lo caro no es la firma de la función sino tener la zona disponible en cada punto de uso, así que un default sería código muerto sin tests. Cuando haga falta, se hace entero.

⚠️ **Vocabulario**: hoy "internacional" nombra **dos cosas independientes** — que el **cliente** esté afuera (los rieles de pago, `coaches.accepts_international`, todo lo que existe hoy) y que el **profesional** esté afuera (esto). La primera no implica la segunda, y usar una sola palabra para las dos ya generó confusión una vez.

### Cobro en USDT (TRC20) — `bookings.usdt_amount`, `coaches.price_usd`
Segundo riel de cobro, para usuarios que no pueden pagar con Mercado Pago (probado: MP **rechaza tarjetas emitidas fuera de Argentina**). `scripts/add-usdt-payments.sql`, `add-coach-price-usd.sql`, `add-usdt-cron.sql`, `expire-unpaid-usdt.sql`, `fix-usdt-amount-index.sql` — **todos CORRIDOS, y el circuito completo VERIFICADO con un pago real de 6,28 USDT el 18/08/2026** (tx `ff53bdcb3a7d…`): se acreditó solo y la reserva pasó a `confirmada` sin intervención.
- **`coaches.price_usd`** (integer, CHECK 1..10000) — precio de la sesión internacional. Lo fija el coach; **no se deriva de una cotización** a propósito. ENTERO obligatorio: los centavos son el identificador del pago. Requiere `grant update` de la columna.
- **`bookings.usdt_amount`** (numeric(18,6)) — el monto exacto que identifica la reserva.
- 🔴 **Cómo se reconoce un pago.** En cripto no hay `external_reference`: llega plata a una dirección y hay que decidir a qué reserva corresponde. Cada reserva recibe un **monto único** = `price_usd` + un identificador en los **centavos**. `usdt-check-payments` (cron, cada minuto) lee las transferencias TRC20 hacia la wallet de VIVE en TronGrid y cruza por monto exacto.
- ⚠️ **El identificador son 2 decimales y no más, por una restricción del mundo real**: las billeteras no dejan tipear más (verificado en Belo, que acepta 2). Un identificador en el decimal 5 sería inescribible y el pago nunca se reconocería. Costo: **100 combinaciones por precio** (no globales — el índice es sobre el monto, así que dos precios distintos tienen espacios distintos). Con la ventana de expiración de 60 min eso son ~100 pagos/hora por precio, contra un objetivo de 25-100 por mes. Si alguna vez molesta, la salida es **una dirección de depósito por reserva**, no más decimales.
- 🔴 **Se valida el CONTRATO del token, no el símbolo.** Cualquiera puede desplegar un token llamado "USDT" en Tron y transferirlo gratis; aceptarlo por símbolo permitiría pagar una sesión con algo que no vale nada. Hay un test que fabrica ese token falso.
- **Tres índices, y los tres son garantías de dinero**: `bookings_usdt_payment_id_uniq` (una transferencia acredita UNA reserva, aunque dos corridas del cron se solapen), `bookings_usdt_pending_amount_uniq` (dos reservas esperando pago nunca comparten monto — **excluye las canceladas**, ver abajo), y `bookings_usdt_pending_idx`.
- 🔴 **CUARTA vez del mismo patrón, encontrada el 20/08/2026: `usdt-check-payments` traía las canceladas.** La consulta de pendientes filtraba solo por `payment_status = 'pendiente'`, así que todo checkout abandonado —cancelado por `expire_unpaid_checkouts()`, que deja `payment_status` en `'pendiente'` a propósito— se quedaba en la lista para siempre (había **14** acumuladas). Tres efectos, y el segundo es el grave: (1) una reserva cancelada podía quedar `'aprobado'` si la plata llegaba tarde, sin sesión y fuera del alcance del trigger de reembolso, que dispara en la transición a `'cancelada'` y esa ya había ocurrido; (2) 🔴 como `fix-usdt-amount-index.sql` hizo que el índice de montos excluya las canceladas, **su monto queda libre para una reserva nueva** — con las dos en la lista, un pago tardío del dueño de la cancelada podía acreditarse sobre la reserva de **otra persona**; (3) con el `.limit(200)`, las canceladas acumuladas terminan desplazando a las pendientes reales, que dejan de acreditarse sin error visible. Corregido con `.neq('status','cancelada')` en la consulta **y** en el update (entre una y otro la reserva puede expirar).
- 🔴 **`status` y `payment_status` cuentan historias distintas, y mezclarlas genera fugas.** `expire_unpaid_checkouts()` cancela poniendo `status='cancelada'` y **deja `payment_status='pendiente'`** (correcto: no se pagó). El índice de montos miraba solo `payment_status`, así que la reserva cancelada seguía adentro y **se quedaba con su monto para siempre**. `fix-usdt-amount-index.sql` le suma `status <> 'cancelada'`. Tercer caso del mismo patrón en la misma sesión — ver también el filtro de `payment_provider` en `mp-process-refunds` y el de pago en `CoachReservasScreen`.
- **`expire_unpaid_checkouts()` extendida**: antes solo miraba `preference_id is not null` (Mercado Pago), así que las de USDT **no expiraban nunca**. Ahora cubre los dos rieles, con 60 min para USDT contra 30 de MP — en cripto hay que abrir otra app, buscar saldo y confirmar en la red.
- **Secret `USDT_WALLET_TRC20`** (dirección de cobro de VIVE) y opcional `TRONGRID_API_KEY`. ✅ **ROTADO el 21/08/2026** a una billetera dedicada a VIVE (`TYtm2r…7JQFLs`); antes apuntaba a la dirección personal de Andre, que iba a terminar conteniendo plata de los profesionales.
  - **La dirección se verificó por CHECKSUM, no solo por formato.** Las direcciones de Tron son base58check: el regex de `lib/payout.ts` detecta la forma, pero un error de tipeo puede dar una cadena con forma válida. Se decodificó y se comprobó que los últimos 4 bytes coinciden con el doble SHA-256 del resto, y que el prefijo de red es `0x41` (mainnet). Vale repetirlo ante cualquier dirección nueva — un carácter cambiado pierde los fondos y no rebota.
  - ⚠️ **Rotar tiene una ventana de riesgo de 60 minutos**: la dirección **no se guarda en la reserva** (`usdt-create-payment` la devuelve desde el env en cada llamada), así que quien tuviera un cobro pendiente con la dirección vieja en pantalla mandaría ahí, y el cron ya estaría mirando la nueva. Se rotó ahora justamente porque **sin ninguna reserva internacional esa ventana no existe** — más adelante habría costado un pago perdido. Si alguna vez hay que rotar con tráfico, primero hay que guardar la dirección en la reserva y que el cron mire el conjunto de direcciones con cobros pendientes.
  - ⚠️ **Al rotar hay que REDEPLOYAR** `usdt-create-payment` y `usdt-check-payments`: leen el env al arrancar, y una instancia caliente puede seguir con el valor viejo. La lógica pura vive en `supabase/functions/_shared/usdt.ts`, con 12 tests.

### `session_attendance`
Metadato de participación en la videollamada, traído de Daily. **Prueba de que la sesión ocurrió**, para disputas. `scripts/add-session-attendance.sql` — **CORRIDO el 25/08/2026**.
- `booking_id` (uuid, PK, FK → `bookings.id` ON DELETE CASCADE) · `room_name` · `checked_at` · resumen (`meetings_count`, `participants_count`, `max_simultaneous`, `first_join_at`, `total_seconds`) · `raw` (jsonb)
- 🔴 **`raw` es la evidencia; el resumen es una comodidad.** Se guarda el crudo siempre y el resumen se deriva como **mejor esfuerzo**: si un nombre de campo no es el esperado, el resumen queda en null pero la prueba **igual quedó guardada** y se recalcula sin volver a pedirle nada a Daily. Al revés —derivar y descartar el crudo— un error de nombre haría perder la evidencia en silencio.
- 📝 **`participants_count = 0` NO es un dato faltante**: es la constancia de que **nadie entró**, que es lo que hace falta para el caso inverso — alguien que reclama una sesión que no dio. Por eso la función escribe el cero **solo pasadas 24hs** del horario: antes sería una foto sacada temprano, no una conclusión.
- **Se CONSULTA, no se recibe por webhook.** Daily ofrece las dos formas. La sala ya identifica la reserva (`vive-<booking_id>`), un webhook sería otro endpoint público que asegurar, y sobre todo **consultar es reintentable**: si una corrida falla, la siguiente la levanta. Un webhook perdido se perdió — y esto es justo lo que se va a necesitar dentro de seis meses.
- ⚠️ **La retención de Daily no está verificada.** Por eso el cron corre cada hora y no a demanda: lo que importa es tenerlo en NUESTRA base durante los meses de la ventana de disputa, no en la de ellos.
- 🔴 **Hoy no dice QUIÉN entró.** `create-meeting-room` crea la sala con ventana horaria pero **no emite tokens con identidad**, así que `user_id` y `user_name` vienen nulos. La prueba dice "dos personas estuvieron 47 minutos en la sala de esta reserva, en su horario" — razonable, pero no identifica. La versión completa necesita tokens con `user_id`, y eso toca cómo se entra a la sala.
- **RLS**: solo `is_admin()` puede leer. No se le muestra al coach ni al cliente: es material para resolver una disputa, y exponerlo invita a discutir el dato en vez del hecho.

### `coach_payout_accounts`
Datos de cobro del coach para las **sesiones internacionales** — las únicas donde VIVE cobra y le transfiere. En las de Argentina la plata va directa a su Mercado Pago y esta tabla no interviene. `scripts/add-coach-international.sql` — **CORRIDO y VERIFICADO el 18/08/2026** (columna, políticas, y el insert negativo que debe fallar).
- `coach_id` (uuid, PK, FK → `coaches.id` ON DELETE CASCADE) ⚠️ — `coaches.id`, no `profiles.id`
- `method` (text **NULLABLE** desde el 25/08/2026, CHECK IN `transferencia`/`usdt`/`paypal`), `cbu`, `alias`, `wallet`, `network` (CHECK IN `TRC20`/`ERC20`/`POLYGON`), `paypal_email`, `updated_at` (trigger `trg_touch_coach_payout`)
- 🔴 **`method` dejó de ser obligatoria** (`scripts/fix-payout-rails-trigger.sql`, **CORRIDO el 26/08/2026**, verificación pendiente). Con la regla espejo el coach acepta CERO, UNO o DOS rieles y `method` es singular: no hay valor que represente el estado. Mientras fue NOT NULL sin default, **la pantalla de cobro no podía guardar nada** — el upsert dejó de mandarla y Postgres valida el NOT NULL sobre la tupla propuesta **antes** de resolver el `on conflict`, así que fallaba también para el coach que ya tenía fila. La columna se conserva (la lee un build viejo) y `CoachPayoutScreen` la sigue escribiendo derivada del primer riel aceptado **que tenga su destino cargado** — nunca a secas, porque los tres CHECK viejos por método siguen vivos.
- 🔴 **`accepts_paypal` / `accepts_usdt` — el coach declara qué rieles ACEPTA** (`scripts/add-payout-rails.sql` — ✅ **CORRIDO, confirmado contra la base el 25/08/2026**: las cuatro columnas de rieles responden por REST con la anon key. Este archivo lo daba por "PENDIENTE DE CORRER" y el changelog por "CORRIDO"; **el changelog tenía razón y este archivo estaba desactualizado**. Se preguntó a la base y no a los documentos, que es el único método que sirve — el mismo patrón costó dos semanas y media de reembolsos sin procesar en julio y volvió a aparecer con `bookings.charged_amount` el 24/08). Reemplazan a `method` como fuente de verdad: **regla espejo** (D4 en `docs/decisiones-pagos.md`) — a quien reserva se le ofrecen solo los medios por los que el coach acepta cobrar, y cada reserva se paga por el riel por el que entró. `method` se conserva y la migración es **aditiva a propósito**: un build viejo sigue andando hasta que se despliegue el nuevo. ⚠️ **`transferencia` NO se migra a ningún riel**: el CBU deja de ser opción de cobro del exterior porque no hay riel de entrada en pesos que lo espeje. `cbu`/`alias` se conservan sin uso, reservados por si se agrega la "conversión pasante". Dos CHECK nuevos: aceptar un riel exige tener su destino.
- 🔴 **`coaches.accepts_paypal` / `coaches.accepts_usdt` — los mismos rieles, PÚBLICOS.** `coach_payout_accounts` solo la lee su dueño y el admin (ahí vive el mail y la wallet), pero **el checkout necesita saber qué rieles acepta el coach y lo consulta un cliente cualquiera**. Mismo criterio que `accepts_international`: el hecho es público, el destino no. Las mantiene el trigger `trg_sync_intl_on_payout`, y su `update` está **revocado** para `authenticated`.
- 🔴 **`coaches.accepts_international` pasó de casilla a DATO DERIVADO.** La mantiene `sync_accepts_international()` a partir de tener un riel en dólares aceptado **y** `price_usd` cargado, disparada desde `coach_payout_accounts` y desde `coaches.price_usd`. Su `update` está **revocado**. Motivo: como flag podía contradecir a los datos —activarse sin precio en dólares— y entonces el catálogo lo anunciaba y la pantalla de pago no le podía cobrar. Los seis lectores (catálogo, buscador, perfil público, las dos `*-create-payment` y la pantalla de reserva) **no cambiaron**: siguen leyendo la misma columna.
- **`paypal_email` + el método `paypal`** (`scripts/add-coach-paypal-payout.sql` — **CORRIDO y VERIFICADO el 24/08/2026**: el CHECK acepta los tres métodos, la columna existe, los dos constraints nuevos están y ninguna fila quedó inconsistente). El coach recibe **dólares en su cuenta de PayPal**. Verificado ese día contra la documentación oficial: Argentina figura como *"Send, receive, and withdraw"* en la tabla de países de Payouts, a Latinoamérica se le manda en **USD**, y la comisión es **2% del monto, a cargo del emisor** (el receptor no paga nada). 🔴 **Ese 2% lo absorbe VIVE y NO se le descuenta al coach.** La regla, decidida el 24/08/2026, es **costo fijo se descuenta, costo proporcional se absorbe**: el USD 1,50 de USDT se descuenta porque no escala y castiga al de poco volumen, y un 2% pesa igual para el que hace una sesión que para el que hace veinte. Además elegir PayPal le ahorra a VIVE el tramo de sacar los dólares a Argentina, que en el camino de la transferencia ya paga. ⚠️ Eso **asume que ese tramo cuesta más que 2%**, y todavía no está medido. ⚠️ Para el coach, el dato que no puede quedar implícito: bajar esos dólares a un banco argentino **los convierte a pesos** al cambio de PayPal (la cuenta destino tiene que ser en pesos; los retiros a cuentas locales en dólares se revierten). Sirve para quedarse con dólares, no para evitar la conversión si igual va a pasarlos a pesos.
- 🔴 **Tabla aparte y NO columnas en `coaches` a propósito**: `coaches` se lee con la anon key **sin sesión** (verificado contra el REST API el 18/08/2026), así que un CBU ahí quedaría público para cualquiera que abra la app. Mismo criterio que `coach_mp_accounts`.
- **RLS**: `coach_payout_own` (ALL, dueño vía `coaches.profile_id = auth.uid()`) · `coach_payout_select_admin` (SELECT, `is_admin()` — el panel necesita el CBU para transferir, pero **no escribe**: nadie edita el dato de cobro de otro). `revoke all` para `anon`.
- **Seis CHECK de integridad**, porque un error acá no se deshace: coherencia por método (`usdt` exige wallet + red, `transferencia` exige cbu, `paypal` exige mail) y formato (CBU 22 dígitos; TRC20 `^T[1-9A-HJ-NP-Za-km-z]{33}$` — base58, sin 0/O/I/l; ERC20 y POLYGON `^0x[0-9a-fA-F]{40}$`). **La red se valida CONTRA la dirección**: una dirección EVM es válida en sí misma, y enviarle USDT por la red Tron pierde los fondos definitivamente — no rebota como un CBU mal cargado. Las mismas reglas están duplicadas en `lib/payout.ts` (puro, 24 tests) para mostrar el error mientras se escribe; la base sigue siendo la frontera. ⚠️ **El formato del mail de PayPal es deliberadamente LAXO**, al revés que el de la wallet: un mail equivocado **no pierde los fondos** —PayPal rebota el payout y la plata vuelve al saldo de VIVE—, así que rechazar un mail válido y raro sería el error más caro de los dos.

### `coaches.accepts_international`
- (boolean, NOT NULL default false) — opt-in del coach a atender personas fuera de Argentina. **Público a propósito**: el usuario del exterior tiene que poder filtrar por esto como filtra por precio. **Desde el 19/08/2026 ese filtro existe de verdad**: se lee en `lib/coachesCache.ts` (y de ahí en el filtro "¿Estás fuera de Argentina?" de `app/search3.tsx`) y en `screens/ProfesionalScreen.tsx` (badge + precio en dólares en el footer). Hasta entonces el único consumidor era `BookingScreen_Confirm`, para decidir si dibujaba el botón de USDT — o sea que el dato era público pero invisible, y quien estaba afuera recorría el catálogo entero sin saber quién lo atendía. ⚠️ **En todos los lugares donde se lee va junto con `price_usd != null`**, nunca solo: `usdt-create-payment` rechaza el cobro sin precio en dólares, así que un coach con el flag prendido y sin precio no puede recibir una reserva del exterior. Anunciarlo sería prometer en el catálogo algo que la pantalla de pago no ofrece. No cambia los horarios del coach (atiende en sus mismas franjas, el que se acomoda es el usuario); cambia **el cobro**.
- ⚠️ Requirió `grant update (accepts_international) on public.coaches to authenticated`: `lock-privileged-columns.sql` hace `revoke update` y otorga columna por columna, así que **toda columna nueva de `coaches` es de solo lectura hasta que se agregue a esa lista** — y desde el cliente falla en silencio (0 filas afectadas, sin error).

### `coach_topics`
- `id` (uuid, PK)
- `coach_id` (uuid, FK → `coaches.id`) ⚠️ — mismo criterio que `coach_availability`/`coach_weekly_pattern`, no `profile_id`
- `topic` (text, NOT NULL) — uno de los 32 subtemas definidos en `constants/searchData.ts` (`AXES`), ej. `"Ansiedad"`, `"Pareja"`, `"Sueño"`. Sin CHECK constraint contra esa lista a propósito (mismo criterio que `saved_resources.resource_id`) — la lista vive en el frontend, no vale la pena la rigidez de un enum en la base. Pasó de 28 a 32 el 10/07/2026: se sumaron 4 subtemas al eje emocional (`Ansiedad social`, `Autoestima`, `Duelo` en "Emociones y ánimo"; `Burnout (estrés laboral)` en "Foco, hábitos y trabajo") como parte del sistema de puertas de Conexiones — solo cambió `AXES`, sin migración de base (justo el beneficio de no tener CHECK). ⚠️ Como la base no valida, el string tiene que coincidir EXACTO entre `AXES` y `constants/conexionesDoors.ts` (`DOORS`), o el coach queda huérfano de puerta en silencio; hay un chequeo de partición para eso.
- `created_at` (timestamptz, NOT NULL DEFAULT now())
- UNIQUE(`coach_id`, `topic`)
- RLS: SELECT abierto (`coach_topics_public_read`) — necesario para mostrarlo en el perfil público y para filtrar en la búsqueda sin sesión. ALL solo para el coach dueño (`coach_topics_manage_own`, WITH CHECK `coach_id IN (SELECT id FROM coaches WHERE profile_id = auth.uid())`)
- Agregada 02/07/2026 (`scripts/add-coach-topics.sql`, **aplicada — verificado 11/07/2026 vía `scripts/audit-schema.sql`**). El coach edita su selección en `screens/CoachTopicsScreen.tsx` (`/coach-topics`, desde "Temas que trabajo" en `CoachProfileScreen.tsx`) — guardado con criterio "reemplazar todo": borra todas sus filas y reinserta la selección actual, no hace diff. Se muestra en `ProfesionalScreen.tsx` (reemplazó un array de 5 temas hardcodeado que era igual para cualquier coach) y filtra de verdad en `app/search3.tsx` (antes comparaba el subtema elegido contra `coaches.specialty`, texto libre tipo "Coach de vida" — casi nunca coincidía).

### `coach_rebooking_stats` (VISTA)
- Vista de agregación (no tabla) para el ranking del deck de Conexiones (criterio v1). Una fila por coach: `coach_id` (= `coaches.id`), `completadas_count`, `rebookers_count`, `rebooking_rate`.
- **`rebooking_rate`** = de los usuarios distintos que completaron ≥1 sesión con el coach, qué fracción hizo una reserva posterior (cualquier status; el acto de re-agendar es la señal). `NULL` si `completadas_count < 5` (piso mínimo de muestra → el cliente cae a rating). Todo en usuarios distintos, no filas de booking. Rate global del coach, no por tema.
- **Privacidad:** corre con permisos del owner (`security_invoker = false`) y solo se hace `GRANT SELECT` de la vista a `anon`/`authenticated`, **no** de `bookings`. Así el cliente lee el agregado sin poder ver bookings de terceros (que RLS le bloquea). Expone solo números, nunca filas crudas.
- Se lee en `lib/coachesCache.ts` (join en paralelo con reviews, por `coach_id`) y alimenta `lib/coachDeckRanking.ts`. Usa `bookings.created_at` para "posterior". Vista en vivo; si el volumen la hace lenta, convertir a MATERIALIZED VIEW + cron (patrón `complete_confirmed_sessions()`).
- Creada 10/07/2026 (`scripts/add-coach-rebooking-stats.sql`, corrida en Supabase el 10/07).

### `coach_trending_stats` (VISTA)
- Vista de agregación (no tabla) para el slot **"En tendencia"** del deck de Conexiones (criterio v2 de slots etiquetados). Una fila por coach: `coach_id` (= `coaches.id`), `recent_bookers`.
- **`recent_bookers`** = usuarios DISTINTOS que reservaron a ese coach en los últimos 30 días (por `bookings.created_at`, el acto de reservar), excluyendo `status = 'cancelada'`. Distintos usuarios y no filas para que un mismo usuario reservando varias veces no infle la tendencia.
- **Privacidad:** mismo patrón que `coach_rebooking_stats` — `security_invoker = false` + `GRANT SELECT` solo de la vista a `anon`/`authenticated`, no de `bookings`. Expone solo el número agregado.
- Se lee en `lib/coachesCache.ts` (join en paralelo con reviews/reagendamiento, por `coach_id`) y alimenta el slot `tendencia` de `lib/coachDeckRanking.ts`. Vista en vivo; si el volumen la hace lenta, convertir a MATERIALIZED VIEW + cron.
- Creada 11/07/2026 (`scripts/add-coach-trending-stats.sql`, **aplicada — corrida y verificada 11/07/2026 vía `scripts/audit-schema.sql`**).

### Pagos v1 — MercadoPago Marketplace (`scripts/add-payments-v1.sql`, **corrida** — verificado 18/07/2026 que las columnas y `coach_mp_accounts` ya existen en prod, la nota vieja de "pendiente" había quedado desactualizada)
Modelo: split payments, **Checkout Pro**, cobro al reservar + reembolso automático si el coach rechaza o si la pendiente vence (24hs). Money release del coach demorado hasta post-sesión. Pre-autorización descartada. Ver memoria `project_vive_payments` y `supabase/functions/mp-*`. **Las 5 edge functions tienen lógica real (OAuth con PKCE + state firmado, comisión server-side, split, webhook con firma validada, reembolsos con dead-letter, refresh de token del coach) — ya no son scaffolds; los headers que decían "SCAFFOLD v1" se corrigieron el 06/08/2026.** Credenciales MP en Supabase secrets, nunca en el repo.
- **✅ VALIDADO EN PRODUCCIÓN el 09/08/2026 con 4 pagos reales de $1 ARS.** El flujo completo anda: Checkout Pro → split 20% → `mp-webhook` escribiendo `payment_status` solo → `trg_mark_refund_on_cancel` → `mp-process-refunds` y su cron. En el pago con split (`172923514332`) MP devolvió `fee_details` con `application_fee: 0.2` (comisión VIVE) además del `mercadopago_fee: 0.04`, y `net_received_amount: 0.76` — el reparto cierra exacto.
- **✅ Tramo del 15% también validado, el 19/08/2026** (par Joaquín + Coach Prueba, segunda sesión pagada de verdad, $4.500 ARS): `platform_fee_pct: 15`, `payment_status: 'aprobado'`, `payment_id` real. Los dos intentos anteriores del mismo día (abandonados antes de pagar) ya habían quedado calculados en 15% también — consistente en cada intento.
  - ⚠️ **El split exige que la cuenta MP del coach sea DISTINTA de la dueña de la aplicación** (self-split: MP no deja que quien cobra el `marketplace_fee` sea el vendedor). La app es la cuenta personal de Andre, así que un coach conectado con esa misma cuenta hace fallar la creación de la preferencia apenas se manda `marketplace_fee`. Con el split apagado el síntoma no aparece, lo que lo vuelve fácil de diagnosticar mal.
- **PRIMER PAGO REAL: 09/08/2026** (payment `172908775452`, cobrado y después reembolsado end-to-end). Las dos incógnitas que quedaban marcadas como "pendiente de verificar contra MP" quedaron **resueltas**, y una de las dos era un bug en vivo:
  - (a) **El token de plataforma NO puede leer el pago del coach.** En marketplace el pago es del vendedor: el `GET /v1/payments/{id}` con `MP_ACCESS_TOKEN` devolvía error y `mp-webhook` cortaba en 502, así que **el webhook nunca escribió un solo `payment_status` desde que existe** — el pago se acreditaba en MP y la reserva quedaba en `'pendiente'` para siempre. Corregido: `mp-webhook` ahora resuelve el token del coach vía el `user_id` (collector) de la notificación → `coach_mp_accounts.mp_user_id` → `getFreshCoachToken`. El fallback al token de plataforma quedó solo para cuando MP no manda `user_id`.
  - (b) **El manifest de firma está bien**: las notificaciones v2 validaron a la primera. Los 401 que se veían eran de las **IPN v1 legacy**, que MP firma con un manifest distinto al de la v2. Por cada pago MP pega hasta tres veces en el mismo `notification_url`: la v2 (`?data.id=…&type=payment`), la IPN v1 (`?id=…&topic=payment`) y una o más de `merchant_order`. Se procesa **solo la v2**; el resto se descarta con 200. El descarte tiene que ir **antes** de validar la firma: un 401 le dice a MP que reintente, y con el filtro puesto después, un solo pago generaba 8 reintentos. Las ramas de descarte no leen ni escriben nada, así que ignorar sin verificar firma no abre nada. La v2 con firma inválida sigue devolviendo 401.
- **Las 5 edge functions tienen lógica real** (OAuth con PKCE + state firmado, comisión server-side, split, webhook con firma validada, reembolsos con dead-letter, refresh de token del coach) — ya no son scaffolds; los headers que decían "SCAFFOLD v1" se corrigieron el 06/08/2026.
- **`booking-return`** (edge function, 19/08/2026, `verify_jwt = false`): bounce público para el back_url del checkout. MP exige que `back_urls` sea https — un deep link (`viveapp://booking/result`) directo da `invalid_back_urls` al crear la preferencia. Esta función solo hace un 302 de esa vuelta (https) al deep link real, reenviando los query params por si algún día hacen falta. Mismo patrón que `mp-oauth-callback`/`APP_DEEP_LINK`. **Desde el 21/08/2026 está ACTIVA por default en los dos rieles**: `mp-create-payment` y `paypal-create-payment` caen a `${SUPABASE_URL}/functions/v1/booking-return` si el secret `CHECKOUT_RETURN_URL` no está seteado (antes el default era un `viveapp://`, que al no ser https hacía que no se mandaran `back_urls` — la vuelta quedaba apagada). Para apagarla de nuevo alcanza con poner el secret en cualquier cosa que no empiece con `https://`.

- 🔴 **CÓMO SE ABRE EL CHECKOUT: la app NATIVA de Mercado Pago (21/08/2026, sesión 117).** Cuarta y última vuelta de un problema que costó cuatro sesiones; queda acá entero para que nadie lo reabra sin leerlo.
  - **107** — browser del sistema + `back_urls` https + `WebBrowser.openAuthSessionAsync`: la pantalla de "pago aprobado" de MP **rompe** esa sesión efímera en vez de dejarse interceptar, y Safari terminaba mostrando un checkout nuevo desde cero.
  - **111** — `dismissBrowser()` disparado por el sondeo: funciona solo si el browser sigue siendo el nuestro. Con la app de MP instalada, MP le pasa el control a **su** app nativa y cerrábamos una pestaña que ya no estaba en pantalla.
  - **113** — `<WebView>` embebido + `onShouldStartLoadWithRequest` bloqueando todo lo que no fuera http(s), **justamente para impedir el salto a la app nativa** y no perder el control del cierre. Costo: la persona tenía que loguearse a MP a mano en cada reserva (en iOS lo tapaba a medias `sharedCookiesEnabled`, que **no existe en Android**), y ese login era el paso donde se caían los pagos.
  - **117 (vigente)** — `Linking.openURL(init_point)`: **el salto a la app nativa pasa a ser el objetivo, no el problema.** Ahí la persona ya está logueada, con tarjetas guardadas y biometría. Se eliminó la dependencia de `react-native-webview` en esta pantalla.
  - **Lo que hace que esto no repita 107/111 es que ya no importa controlar el cierre.** Ver el punto siguiente: los efectos de confirmación se mudaron al servidor, así que la app puede morir en cualquier momento sin consecuencias. El sondeo del cliente quedó para una sola cosa — mostrarle el resultado a quien vuelve — y se mide en **tiempo de app EN PANTALLA** (`AppState === 'active'`, 10 min), no en tiempo de reloj: alguien tipeando una tarjeta y un 2FA gasta minutos legítimamente, y un deadline de reloj lo echaría habiendo pagado.
  - `app/booking/result.tsx` (el aterrizaje del deep link) hace `router.back()` si hay algo debajo —la pantalla de confirmación sigue sondeando y va a mandar a `/booking-success` sola— y solo cae a `/(tabs)/mis-salas` si la app arrancó de cero desde el deep link. Antes navegaba siempre a mis-salas, lo que **pisaba** el flujo en curso.

- 🔴 **SIN PAGO ACREDITADO NO QUEDA RESERVA (21/08/2026).** Si el cobro se rechaza, se abandona o simplemente no se acredita, `BookingScreen_Confirm` **cancela la reserva ahí mismo** y no navega a la pantalla de éxito: se queda en la de confirmación con el motivo.
  - **Qué pasaba antes:** la reserva sobrevivía en `'pendiente'` hasta que `expire_unpaid_checkouts()` la barría a los 30 min. En el medio, (a) el horario quedaba **tomado para todo el mundo** —la vista de disponibilidad cuenta `status in ('confirmada','pendiente')`—, y (b) la persona veía la pantalla de éxito, con tilde verde y botón "Ver mi sala", con el texto "Falta confirmar el pago" abajo. El diseño decía que había salido bien y la letra chica decía que no.
  - **Las guardas del update hacen imposible matar una reserva paga:** `status = 'pendiente'` y `payment_status <> 'aprobado'`. Si el pago entra en el mismo instante, o gana él (no se cancela nada) o gana la cancelación y el pago aterriza sobre una reserva cancelada — caso que `mp-webhook` ya resuelve marcándola `'reembolso_pendiente'`. La política RLS que lo permite es `users_cancel_own_booking`, que ya existía y está restringida a `'cancelada'`.
  - **`expire_unpaid_checkouts()` sigue haciendo falta**: cubre a quien nunca vuelve a la app (cerró todo mientras pagaba, o el SO la mató). La cancelación del cliente es el camino rápido, no el único.
  - Desapareció el estado `paymentPending` de `BookingScreen_Success` (título "Falta confirmar el pago", badge "Pago sin confirmar"): ya no se puede llegar a esa pantalla sin el pago hecho.
  - ⚠️ **Consecuencia a mirar: los medios OFFLINE de Mercado Pago** (efectivo, Rapipago, cajero) acreditan horas o días después. Con esta regla, quien elija uno de esos se queda sin reserva y, si después paga, el pago aterriza sobre una reserva cancelada y sale por el circuito de reembolso. Si eso molesta, se corta de raíz excluyéndolos en la preferencia (`payment_methods.excluded_payment_types: ['ticket','atm']` en `mp-create-payment`) — es preferible a prometer un horario que no se está sosteniendo.

- 🔴 **LOS EFECTOS DE CONFIRMACIÓN SON SERVER-SIDE** (`supabase/functions/_shared/booking-effects.ts` → `applyPaidBookingEffects`, 21/08/2026). Confirmar la sesión, avisarle al coach, dejar el mensaje de sistema en la sala y cancelar a los competidores del horario **ya no los hace el cliente**.
  - **Por qué:** hasta la 116 los hacía `BookingScreen_Confirm.applyBookingEffects`, y podía porque el checkout embebido mantenía la app en primer plano todo el pago. Con el salto a la app nativa la app se va a segundo plano y el SO puede matarla: un pago aprobado con la app muerta dejaba **plata cobrada, reserva en 'pendiente' y coach sin enterarse** — el espejo exacto de las 27 reservas fantasma del 09/08 (aquellas eran confirmadas-sin-cobrar; estas serían cobradas-sin-confirmar).
  - **Quién lo llama:** los tres rieles, en el punto donde ganaron la carrera por marcar `payment_status = 'aprobado'` — `mp-webhook`, `paypal-webhook` y `usdt-check-payments`.
  - ⚠️ **La idempotencia la ponen los llamadores, no el helper.** Cada uno reclama la transición con un update condicional + `.select('id')` y solo llama si recibió fila. En `mp-webhook` la guarda es `.neq('payment_status','aprobado')` y **no** `= 'pendiente'`: Checkout Pro recupera pagos rechazados, así que la aprobación puede llegar sobre un `'rechazado'`. Sin ese reclamo, las hasta 3 notificaciones que MP manda por pago producirían 3 push al coach y 3 mensajes de sistema.
  - ⚠️ **El cliente sigue aplicando los efectos en UN caso: cuando no hubo nada que cobrar** (coach sin MP conectado, `initPoint` null). La condición es `if (!initPoint)` y **no** `if (!awaitingPayment)`: con la segunda, un pago aprobado los dispararía de los dos lados.
  - ⚠️ **Secret nuevo `MESSAGE_ENCRYPTION_KEY`**, que tiene que valer **lo mismo** que `EXPO_PUBLIC_ENCRYPTION_KEY` del `.env` de la app. El helper escribe el mensaje de sistema con el mismo XOR+base64 de `lib/encryption.ts`; si las claves no coinciden el mensaje se guarda igual y se lee ilegible en la sala. Sin el secret, el helper cae al mismo default que el cliente (`vive_mvp_key_2026`), así que **solo hace falta setearlo si `EXPO_PUBLIC_ENCRYPTION_KEY` no es el default**.
  - **No crea la sala de Daily** a propósito: `SalaScreen` la crea sola la primera vez que alguien entra (`createOrGetMeetingUrl`), y `create-meeting-room` exige el JWT de un participante — que un webhook no tiene.

- 🔴 **BUG QUE ESTO DESTAPÓ Y ARREGLÓ: el riel de USDT nunca confirmó nada.** `BookingScreen_Confirm` sale por `router.replace('/pago-usdt')` **antes** de `applyBookingEffects`, y `usdt-check-payments` solo escribía `payment_status`. Resultado: la persona pagaba, `UsdtPaymentScreen` le decía "Tu sesión quedó confirmada" y la reserva se quedaba en `'pendiente'` — sin avisarle al coach, sin liberar el horario de los competidores y sin nadie del otro lado. Existía desde que se creó el riel; no se notó porque todavía no hubo un pago real en USDT. Arreglado el 21/08/2026 con la misma llamada a `applyPaidBookingEffects`.
- 🔴 **Los tres rieles tratan la comisión DEL PROCESADOR de forma distinta, y no fue una política unificada — cada uno salió de su restricción.** Está documentado acá para que nadie los "unifique" sin entender por qué difieren.
  - **Mercado Pago: la paga el coach.** La `mercadopago_fee` se descuenta de lo que recibe él (en el pago real del 09/08, sobre $1: `application_fee` 0.2 para VIVE, `mercadopago_fee` 0.04, `net_received_amount` 0.76 al coach — o sea **≈4%**, y el `fee_details` trae `fee_payer: "collector"`, que confirma quién la paga). 📌 **Ese 4% es el único número de esta sección que es una OBSERVACIÓN y no una tarifa publicada**: fue sobre $1, donde un componente fijo distorsionaría el porcentaje; depende del plazo de acreditación de la cuenta; y no se sabe si incluye IVA. Vive en `lib/pricing.ts` como `MP_FEE_PCT_OBSERVED` y se muestra siempre con un "≈". ✅ **Desde el 08/09/2026 la app se lo dice al coach** en "Cómo te pagamos" (`lib/desglosePago.ts`): antes la pantalla le prometía 20% y le llegaba 76%, y el dato solo estaba acá y en los T&C §8.5. **Está bien así porque en ese riel el comerciante es el coach**: conectó su cuenta, vende, y le paga a su propio procesador — como cualquiera que venda con MP. Los T&C §8.5 ya lo dicen.
  - **USDT: no hay procesador, y el identificador AHORA RESTA.** El identificador del pago vive en los centavos. Hasta el 08/09/2026 se **sumaba**, así que el cliente pagaba siempre un poco más que el precio (entre +0,00 y +0,99) y esa diferencia se la quedaba VIVE: **un margen que nadie decidió cobrar**, subproducto del mecanismo de identificación. 🔴 **Y era más grande de lo que decía este archivo**: el sobrante es fijo en dólares, así que como porcentaje crece cuanto más barata la sesión — ~5% en el peor caso sobre el piso de USD 20, contra el "~0,8%" que figuraba acá, calculado sobre un precio alto. ✅ **Desde el 08/09/2026 `uniqueAmount` RESTA** (`uniqueAmount(50, 37)` → 49,63): el cliente nunca paga más que el precio, el coach sigue cobrando sobre el precio entero (`amount`), y el hueco lo absorbe VIVE de su comisión — decisión de Andre. No hizo falta tocar `nonceOf` ni `findPayment`: el identificador se deriva del monto esperado, no del nonce original. Las reservas viejas siguen andando porque el matcheo usa el `usdt_amount` guardado.
  - **PayPal: la paga VIVE.** ⚠️ **Este bullet decía "la paga el cliente", con un gross-up (`charged_amount` = `(precio + 0,30) / 0,946`), y quedó viejo el 20/08/2026**, cuando se eliminó el gross-up entero — ver más abajo. Hoy `paypal-create-payment` cobra el precio pelado (`charged_amount === amount`; el comentario del código dice *"El cliente paga el precio del coach, sin recargo encima"*). Lo que sigue en pie es el fundamento: no se descuenta de la parte del coach. **Acá NO se puede usar el criterio de Mercado Pago** porque el comerciante es VIVE, no el coach: descontárselo a él sería cobrarle 21-22% real mientras el copy le promete 15%.
  - ⚠️ **LO QUE SIGUE QUEDÓ VIEJO EL 25/08/2026: el riel internacional YA NO es plano.** D3 le devolvió la escalera —**25% la primera sesión con cada persona y 20% de ahí en adelante** (`COMMISSION_INTL_FIRST` / `COMMISSION_INTL_RECURRING` en `_shared/commission.ts`)— con un argumento que corrige al de abajo: el 20/15 local no es *"VIVE deja de aportar"*, es que **el primer tramo recupera ADQUISICIÓN y el segundo es RETENCIÓN**, y las dos cosas aplican en cualquier riel. Lo que sí cubre la diferencia entre 25 y 20 es el costo operativo de cobrar y transferir. Se deja el texto original porque el resto sigue siendo la historia de por qué no hay gross-up.
  - ✅ **DECIDIDO E IMPLEMENTADO (20/08/2026): el riel internacional cobra 25% PLANO** (`COMMISSION_INTERNATIONAL` en `_shared/commission.ts`), sin contador por par, **sin importar cómo pagó el cliente ni cómo cobra el coach**. El cliente paga el precio del profesional, sin recargo encima; el costo de procesamiento sale de la comisión.
    - **Por qué no sigue el 20/15:** en Argentina VIVE baja al 15% porque después de la presentación **deja de aportar** — el coach cobra por su propio Mercado Pago y la relación es suya. En el exterior VIVE **cobra, retiene y transfiere en cada sesión, para siempre**.
    - **Por qué 25 y no 20:** es lo que permite dejar de preguntar por la combinación. Sobre USD 60 el neto va de **~8,56** (PayPal + salida en USDT) a **~13,20** (USDT + salida a CBU). Con 20% la peor caía a ~5,60 y había que mirar caso por caso; con 25% las cuatro cierran, y por eso puede ser una sola tarifa.
    - **Qué eliminó:** el gross-up entero (`paypalGrossUp` en las dos copias), el uso de `charged_amount` para sumar el recargo (ahora es igual a `amount`), el contador por par en los dos rieles internacionales, y la pregunta contractual sobre el recargo de PayPal — si no hay recargo, no hay cláusula que interpretar.
    - ⚠️ **El único costo que absorbe y que NO escala con la facturación sino con la cantidad de coaches** es la comisión de red de los pagos en USDT: es por pago, no por sesión, así que un coach de una sesión semanal cuesta lo mismo que uno de diez. Revisar si algún día la mayoría cobra en USDT.
    - 📝 **Descartado el encuadre de "10% de descuento con Mercado Pago"**: no hay un precio del cual descontar. El precio local está en pesos y el internacional en dólares, y son **dos números independientes** que el coach fija por separado (`price_usd` no se deriva de una cotización). Además el selector de medios aparece según lo que configuró el COACH y no según dónde está el cliente, así que alguien en Argentina vería las dos monedas sin forma de compararlas — y eso acerca el asunto a la cláusula de recargo de PayPal, que hoy no aplica porque los clientes del exterior no pueden usar Mercado Pago.
  - ⚠️ **Sigue abierto para el riel LOCAL:** pasar Mercado Pago a 15% plano eliminaría el tramo del 20% de la primera sesión. Borraría el contador por par y todo su andamiaje (`commissionPctFor`, el snapshot, el filtro de checkouts abandonados que ya causó dos bugs), pero **VIVE dejaría de capturar el valor de la presentación**, que es lo único que aporta en ese riel. Es una decisión con argumento propio y no debe viajar de arrastre con la del internacional.
  - ⚠️ **Un costo que no está en ningún cálculo del código: el cambio de moneda.** Aparece cuando lo que entra no es lo que sale — sacar dólares de PayPal a pesos y vender USDT por pesos son **el mismo costo**, no dos. No lo causa ninguna decisión sino que **dos decisiones independientes no coincidan** (el cliente elige cómo paga, el coach cómo cobra, y ninguno sabe lo que eligió el otro), así que no se le puede pasar a ninguno de los dos: va dentro de la comisión.
- **Comisión (server-side, snapshoteada, el cliente nunca la calcula):** 0% promo fundador (fin TBD) · **20% la PRIMERA sesión COMPLETADA del par coach-usuario** · **15% de la 2da en adelante**. Contador **por par (`user_id`+`coach_id`)**, solo `completada`, **nunca resetea**. El 20% es el costo de adquisición del cliente nuevo; el descuento cae en la 2da sesión, que es el momento de máxima fuga. Se calcula en `mp-create-payment` (COUNT completadas del par → 0/20/15) y se guarda en `platform_fee_pct`. El **IVA queda fuera del schema y del cálculo, y así se queda**: figura fiscal decidida el 06/08/2026 (Andre) — **persona humana en Monotributo**, factura tipo C sin IVA discriminado, así que la comisión retenida es exactamente lo que percibe Vita. Si algún día pasa a Responsable Inscripto hay que decidir si el coach pasa a pagar 24,2% o si el 20% se vuelve IVA incluido (ingreso real ~16,5%) — y cambiar a la vez `mp-create-payment`, el copy de `CoachProfileScreen` y el §8.4 de los T&C.
- 🔴 **`bookings.user_tz_observed` + `user_observation_source` + `user_observed_at`** (`scripts/add-observacion-pais.sql` — **CORRIDO y VERIFICADO el 25/08/2026**). Dónde estaba quien reservó. **Se guarda la observación, se deriva la clasificación** (D2): no se guarda el país —ya es una lectura de la zona— ni `es_internacional` —es una conclusión, y el criterio **todavía no está confirmado**: con un booleano habría que reescribir historia el día que el contador defina otro, con la observación se vuelve a derivar y no se toca ninguna fila. El criterio vive **solo** en la vista `clasificacion_de_operaciones`.
  - ⚠️ **La reserva es un PROXY del hecho fiscal**: el servicio se aprovecha en la sesión y quien reserva puede viajar en el medio. Se aceptó igual porque observar en la sesión exige capturar ubicación **durante una sesión de salud mental** — dato sensible que habría que declarar en una política de privacidad que todavía tiene placeholders.
  - 🔴 **`null` se trata como "sin clasificar", NUNCA como "mercado interno"**. Y el cliente usa `observedTz()` y no `deviceTz()`: aquella **cae a Argentina** cuando no puede leer la zona —correcto para mostrar horarios— y acá sería registrar un país que nadie observó. Hay un test que protege exactamente eso.
  - **Se puede INSERTAR pero no actualizar**: una observación que su propio observado puede editar no es una observación.
- **Vista `operaciones_de_dinero`** (`scripts/add-operaciones-de-dinero.sql` — **CORRIDO el 25/08/2026**). Las acciones del panel que mueven plata, con `monto`/`moneda`/`riel`/`referencia` sacados tipados del `details` de `admin_audit_log`. 📝 **No hizo falta una tabla nueva**: ese log ya existía, ya era append-only y ya guardaba quién hizo qué sobre qué y cuándo — la decisión D8 partía de una premisa falsa. Lo que faltaba era que `mark_coach_paid` **no guardaba cuánto se transfirió**.
- 🔴 **`bookings.payment_status = 'contracargo'`** (`scripts/add-chargebacks.sql` — **CORRIDO el 25/08/2026**). Hasta hoy `mp-webhook` mapeaba `charged_back` a `'reembolsado'`, **el mismo valor que un reembolso voluntario**. Los dos son "la plata volvió" y por eso comparten `refunded_at`; lo que los separa es **quién lo decidió** — el reembolso lo decide VIVE o el coach, el contracargo lo decide el comprador y su banco sin pedir permiso. ⚠️ Quien filtre por `'reembolsado'` tiene que decidir si el contracargo entra: en `lib/admin.ts` (la facturación) **entra**, porque los dos revierten la comisión y dejarlo afuera haría desaparecer una sesión disputada del material que se le lleva al contador.
- **`bookings.disputed_at` + `dispute_reason`** (mismo script). Una disputa abierta **no cambia `payment_status`**: la plata sigue siendo de VIVE hasta que el procesador resuelva, y adelantarse a marcarla perdida rompería la conciliación si la disputa se gana. Las escribe `paypal-webhook` desde los eventos `CUSTOMER.DISPUTE.*`. ⚠️ **`disputed_at` es CUÁNDO SE ABRIÓ y no se pisa**: PayPal manda `CUSTOMER.DISPUTE.UPDATED` varias veces por disputa, y sobrescribirla con `now()` en cada una la convertía en "cuándo nos avisaron por última vez" (corregido el 25/08/2026). `dispute_reason` sí se pisa: ahí interesa el estado más reciente. 🔴 **El mapeo a la reserva es best-effort a propósito**: sale de `resource.disputed_transactions[].seller_transaction_id`, y si no viene **no se adivina** — se loguea fuerte y se contesta 200, porque marcar la reserva equivocada dispara una investigación sobre alguien que no tuvo nada que ver.
- 🔴 **`bookings.dispute_resolved_at` + `dispute_outcome`** (`scripts/add-dispute-resolution.sql`, **CORRIDO el 26/08/2026**; `paypal-webhook` v18). Sin esto, **una disputa GANADA se quedaba para siempre** en `reversiones_despues_de_pagar` — la vista mira `disputed_at`, que no se limpia nunca ni debe. `dispute_resolved_at` se escribe cuando PayPal manda `status = 'RESOLVED'` y **se limpia si la disputa se reabre al escalar** (inquiry → chargeback → arbitraje), que es cuando más hay que mirarla.
  - 📝 **La lógica cuelga de `status` y NO de `dispute_outcome.outcome_code`.** El enum de `status` se pudo citar de la documentación de PayPal (`OPEN` / `WAITING_FOR_SELLER_RESPONSE` / `WAITING_FOR_BUYER_RESPONSE` / `UNDER_REVIEW` / `RESOLVED`); el de `outcome_code` no. Así que el outcome **se guarda crudo y no se interpreta** — mismo criterio que `session_attendance.raw`. Este proyecto ya se quemó con un dato recordado en vez de verificado.
  - 🔴 **Y no hace falta el outcome para nada.** Si la disputa se pierde, PayPal revierte la captura y `PAYMENT.CAPTURE.REVERSED` deja `payment_status = 'contracargo'`: la fila se queda en la lista **por esa condición**, que no se tocó. Si se gana, no hay reversión y sale. ⚠️ Los dos avisos son eventos distintos y PayPal no garantiza el orden: si `RESOLVED` llega antes que `REVERSED`, la fila desaparece un rato y vuelve. Es transitorio y se corrige solo.
- **Vista `reversiones_despues_de_pagar`** (mismo script, solo admin). Reservas donde la plata volvió —o está en disputa— **después** de habérsela transferido al coach. Cada fila es plata que hay que recuperar o dar por perdida. No lo arregla (eso necesita el registro de operaciones de D8) pero hace que se **pueda ver**: hasta hoy nada miraba `paid_out_at` cuando la plata se iba para atrás.
- **`bookings.payment_provider`** (text, NOT NULL default `'mp'`, CHECK IN `mp`/`paypal`/`usdt`, `scripts/add-payment-provider.sql` — **CORRIDO y VERIFICADO el 18/08/2026** contra el REST API: `select=payment_provider` resuelve, una columna inventada devuelve 42703). Qué procesador cobró la reserva. Hoy hay uno solo, así que es informativa; **existe como guarda para el día que haya un segundo rail** (cobro del exterior). `mp-process-refunds` seleccionaba TODA reserva en `'reembolso_pendiente'` con `payment_id` no nulo, sin mirar quién cobró: la primera reserva de otro rail cancelada haría que le pidiera a MP reembolsar un pago que no es suyo → falla → 5 intentos → dead-letter, con el usuario sin su plata y sin error visible. El filtro `.eq('payment_provider','mp')` ya está deployado. ⚠️ **El orden importa**: la columna va SIEMPRE antes que el filtro, o el cron consulta una columna inexistente y los reembolsos se frenan del todo. Índice parcial `bookings_refund_pending_idx` sobre la consulta del cron.
- **El costo de entrega se descuenta del pago al profesional** (`USDT_NETWORK_FEE_USD` en `lib/payout.ts`, **USD 1,50**, valor real de Andre del 20/08/2026). No hay columna: se calcula en el panel al agrupar, porque **se paga una vez por TRANSFERENCIA y no por sesión** — descontarlo por sesión multiplicaría el cobro por la cantidad de sesiones de la semana, que es lo contrario de cómo funciona el costo.
  - **Por qué lo paga el profesional y no VIVE:** pidió que le manden dólares por blockchain, controla la causa y paga el costo. Mismo criterio que Mercado Pago, donde la comisión del procesador sale de su parte. La transferencia bancaria no tiene costo de entrega: el archivo de lote cuesta lo mismo con una fila que con doscientas.
  - 🔴 **La propiedad que lo justifica: el costo NO escala con el monto.** Sobre una sesión de USD 50 pesa 3% si el profesional hizo una sola esa semana y 0,3% si hizo diez. No lo determina el precio, lo determina su volumen — así que el descuento cae fuerte solo sobre quien genera el costo desproporcionado.
  - **Sin el descuento, el mínimo de USD 20 no llegaba al 10% de margen** en el peor caso (6,2%); con él da 13,7% en todos.
  - ⚠️ **Sin mínimo de acumulación** (decisión de Andre): con montos chicos el costo puede superar lo que se le debe. El panel lo marca (`noAlcanza`) y **deshabilita el botón de marcar pagado** en vez de mostrar cero — que se leería como saldado.
- **`bookings.paid_out_at` + `bookings.payout_reference`** (`scripts/add-coach-payouts.sql` — **CORRIDO y VERIFICADO el 19/08/2026** contra el REST API: `select=paid_out_at,payout_reference` resuelve y una columna inventada devuelve 42703). Registro de lo que se le transfirió al coach, y **solo aplica a los rieles donde cobra VIVE** (`payment_provider <> 'mp'`): con Mercado Pago el split ya le pagó en el momento del cobro y no hay deuda que anotar. En el internacional la plata entra entera a la wallet de VIVE y se transfiere después, semanal y solo por sesiones ya realizadas — hasta ahora no existía ninguna fila que dijera a quién, cuánto, ni si ya se había pagado. `payout_reference` es texto libre a propósito (hash de tx si fue cripto, número de operación si fue banco: una transferencia bancaria no tiene hash, así que exigir 64 hex como en `mark_usdt_refunded` dejaría sin poder registrar el método más común). **Ninguna de las dos tiene `grant update` para `authenticated`** — las escribe solo `admin-actions` con service role, porque marcarse a uno mismo como cobrado no puede ser una acción del cliente. Índice parcial `bookings_payout_pending_idx`, que es exactamente la consulta del panel.
- 🔴 **`bookings_select_admin`** (mismo script, **CORRIDO el 19/08/2026**): `add-admin-flag.sql` creó políticas de SELECT de admin sobre `reports`, `guarantee_claims`, `coaches` y `profiles` — y **nunca sobre `bookings`**. La pestaña de Reembolsos del panel (sesión 104) consulta `bookings` con la sesión del admin, así que bajo RLS solo veía las reservas propias: **cualquier reembolso de USDT de otra persona salía como lista vacía, sin error**. No se notó porque el único reembolso probado con plata real fue por Mercado Pago, que no pasa por esa pestaña. Como todas las de admin: SELECT y nada más.
- **La comisión ahora también se calcula en el riel de USDT** (`usdt-create-payment`, 19/08/2026). Antes esa función escribía `payment_provider`, `payment_status`, `currency`, `amount` y `usdt_amount` — y **no `platform_fee_pct`**, así que toda reserva internacional se quedaba con el default 20 de la columna. En Mercado Pago el campo es casi decorativo (el split ya repartió); en el internacional **es el único número que dice cuánto se le debe al coach**, así que un par en el tramo del 15% —o en plena promo fundador de 0%— iba a cobrar de menos el día de la transferencia. El contador es el mismo y **no depende del riel**: es de la relación coach-usuario, así que una primera sesión pagada en pesos manda la segunda al 15% aunque se pague en dólares.
  - ⚠️ **El filtro de checkouts abandonados pasó a `_shared/commission.ts` como `PAIR_SESSION_FILTER`.** Vivía escrito inline en `mp-create-payment` y quedó desactualizado apenas apareció el segundo riel: seguía mirando solo `preference_id`, así que **un cobro de USDT abandonado que llegara a `completada` empujaba al par al tramo del 15%** — el mismo bug de las 16 reservas fantasma de agosto, por la otra puerta. Ahora el predicado SQL y `countsAsCompletedSession` viven en el mismo archivo, uno al lado del otro, y hay tests que fijan que los dos marcadores estén nombrados.
  - **La base del cálculo es `amount`, nunca `usdt_amount`**: este último trae el identificador del pago en los centavos, y pagárselo al coach sería regalarle hasta 0,99 USD por sesión de una plata que existe solo para reconocer la transferencia.
- **`bookings.charged_amount`** (numeric(12,2), `scripts/add-paypal-rail.sql` — **CORRIDO y VERIFICADO el 24/08/2026** con `scripts/verificar-riel-paypal.sql`: la columna existe, el CHECK `price_usd >= 20` está creado y `expire_unpaid_checkouts()` cubre los tres rieles. ⚠️ **La columna ya estaba en la base antes de esa corrida** y los dos documentos la daban por pendiente desde el 19/08 — un `add column if not exists` no distingue "la creé" de "ya estaba", así que el estado real solo se sabe preguntándole a la base.) Lo que se le cobró al cliente, en `currency`. 🔴 **Distinto de `amount`, y la diferencia es plata:** `amount` es el precio del profesional y **la base con la que se calcula su payout** (`amount × (1 − platform_fee_pct)` en `lib/admin.ts`). Hasta ahora coincidían; con PayPal dejan de coincidir, porque la comisión del procesador (5,40% + USD 0,30, confirmada en la tarifa oficial de PayPal para Argentina) **se suma al precio en vez de salir de la parte del coach** — si saliera de adentro la comisión real de VIVE sería 21-22% y el copy le promete 15%. Si alguien usa `amount` para guardar el total cobrado, **todos los coaches del riel internacional cobran de más y nada lo avisa**. No se aplica a USDT: ahí el total cobrado es `usdt_amount`, que además lleva el identificador del pago en los centavos.
- **`coaches.price_usd`: el CHECK pasa de 1..10000 a 20..10000** (mismo script, **PENDIENTE**). El piso existe porque la comisión **fija** de PayPal (USD 0,30) no escala: el recargo necesario para netear el precio del coach es +6,0% sobre USD 100, +7,3% sobre 20, +11% sobre 6 y **+37% sobre 1**. ⚠️ El script hace un `UPDATE` previo obligatorio — el coach de prueba tiene `price_usd = 6` (única fila con el campo cargado, verificado contra el REST API) y sin corregirla el CHECK no se puede crear. La regla está duplicada en `lib/pricing.ts` para mostrar el error mientras se escribe.
- **`expire_unpaid_checkouts()` cubre el tercer riel** (mismo script, **PENDIENTE**). Ya había pasado con USDT: la función busca los marcadores de los rieles que conoce, y una reserva del riel nuevo no tiene ninguno, así que **se queda `'pendiente'` para siempre ocupando el horario**. PayPal lleva ventana de 30 min como Mercado Pago (se paga dentro del propio flujo); los 60 de USDT existen porque hay que salir a otra app.
- **`bookings`** (columnas nuevas): `currency` (text, default 'ARS'), `payment_status` (text, NOT NULL default 'no_iniciado', CHECK IN `no_iniciado`/`pendiente`/`aprobado`/`rechazado`/`reembolso_pendiente`/`reembolsado`), `payment_id` (text, MP payment.id), `preference_id` (text — **identificador del checkout, del riel que sea**: la preferencia de Checkout Pro en Mercado Pago o el id de la orden en PayPal; `payment_provider` dice de cuál. ⚠️ Se comparte a propósito y no se agregó una columna nueva: `expire_unpaid_checkouts()` y el `PAIR_SESSION_FILTER` de la comisión ya tratan `preference_id is not null` como "arrancó un cobro", así que un riel nuevo entra solo en las dos protecciones. Con una columna aparte habría que acordarse de sumarla en los dos lados — y eso ya falló con USDT, que por no estar en ese filtro empujaba pares al tramo del 15% sin que se hubiera pagado nada), `platform_fee_pct` (numeric, default 20 — **snapshot de la comisión calculada por reserva**, no un valor fijo), `paid_at`, `refunded_at` (timestamptz). `amount` ya existía.
  - `refund_attempts` (smallint, NOT NULL DEFAULT 0) — dead-letter de `mp-process-refunds`: se incrementa en cada reembolso fallido; la función filtra `refund_attempts < 6` para dejar de reintentar un reembolso permanentemente roto (queda visible en `reembolso_pendiente` con `refund_attempts = 6` para intervención manual; reencolar = poner en 0). **Corrida — verificado 07/08/2026** sondeando la columna con la anon key (la nota anterior decía "FALTA correr" y estaba desactualizada).
- **`coach_mp_accounts`** (tabla nueva): tokens OAuth del coach — `coach_id` (PK, FK → `coaches.id`), `mp_user_id`, `access_token`, `refresh_token`, `public_key`, `expires_at`, `connected_at`, `updated_at`. **RLS habilitado SIN políticas a propósito** → ni anon ni authenticated leen/escriben; solo el service role (edge functions) accede. Los tokens NUNCA tocan el cliente. **Refresh implementado 06/08/2026** (`getFreshCoachToken` en `_shared/mp.ts`): `mp-create-payment` y `mp-process-refunds` obtienen el token por ese helper, que refresca vía `grant_type=refresh_token` si `expires_at` está a <24h y reguarda el nuevo par (el `refresh_token` de MP es de un solo uso). Antes nada usaba el `refresh_token` → a los ~180 días el token vencía y todo cobro/reembolso del coach fallaba sin recuperación.
- **`coaches.mp_connected`** (boolean, NOT NULL default false): flag NO secreto para la UI (¿el coach conectó MP?). Sin conectar, no puede recibir reservas pagas.
- 🔴 **D6 (`docs/decisiones-pagos.md`) implementada el 27/08/2026: sin ningún riel de cobro configurado, el coach no aparece en el catálogo.** `lib/coachesCache.ts` (`_doFetch`) y la consulta de respaldo de `app/search3.tsx` (la que corre con el caché frío) agregan `.or('mp_connected.eq.true,accepts_paypal.eq.true,accepts_usdt.eq.true')` a la query de `coaches`. No exige Mercado Pago puntual — exige un medio, cualquiera; es la regla espejo (D4) aplicada a la publicación en vez de al checkout. Antes de esto, "reserva confirmada sin cobro" era un estado alcanzable (el coach pasaba las 4 pantallas y recién en `BookingScreen_Confirm` se enteraba que no había ningún botón de pago para mostrar) — con el filtro, ese estado deja de ser alcanzable en vez de bloquearse después. **Impacto verificado**: de los coaches de prueba actuales, solo el que tiene MP conectado sigue visible; los otros tres (sin ningún riel) desaparecen del catálogo hasta que conecten alguno. Las dos consultas deben mantenerse en sync — es el mismo patrón ya documentado para `verified`/`availability_status`.
- **`expire_pending_bookings()`** (extendida): además de cancelar pendientes >24hs y notificar, ahora marca `payment_status = 'reembolso_pendiente'` si la reserva vencida estaba `aprobado`. El refund real lo hace el edge function `mp-process-refunds` (SQL no puede llamar a la API de MP) — agendar por pg_net cuando estén las creds. El cron `expire-pending-bookings` (cada 5 min) ya existe, solo se reemplaza el cuerpo.
- **`expire_unpaid_checkouts()` + cron `expire-unpaid-checkouts`** (`scripts/expire-unpaid-checkouts.sql`, **CORRIDA 09/08/2026**, cada 5 min, SQL puro): cancela reservas con `preference_id is not null AND payment_status = 'pendiente' AND status in ('pendiente','confirmada')` de más de **30 minutos**, y notifica al usuario. Cubre el checkout abandonado — cerrar la pestaña de MP sin pagar dejaba la reserva viva. **Al sondear la base había 27 así, y 16 habían llegado a `completada`**: `complete_confirmed_sessions()` las marcó como sesiones cumplidas al pasar el horario y el usuario recibió invitación a reseñar, sin que se hubiera pagado nada. Encima esas completadas falsas empujan al par al tramo del 15% en `mp-create-payment`, que cuenta `status='completada'`.
  - **Por qué no alcanzaba `expire_pending_bookings()`:** filtra `status='pendiente'` a las 24h. Si el coach acepta antes, pasa a `confirmada` y deja de ser candidata; y con `instant_booking` la reserva nacía `confirmada`, así que no la miraba nunca.
  - **El discriminador es `preference_id is not null`** — significa que el checkout arrancó, o sea que el coach tiene MP. Separa el abandono del caso legítimo "coach sin MP → reserva sin cobro" (ahí es null). El comentario viejo de `mp-webhook` decía que "confirmada sin pago" era indistinguible del abandono y por eso difería el barrido hasta que el pago fuera obligatorio: era falso, se distinguen por esa columna.
  - Los 30 min son holgados a propósito: Checkout Pro deja reintentar con otro medio tras un rechazo dentro del mismo flujo, y cancelar antes mataría una reserva que se está por pagar. `status='completada'` y `'cancelada'` quedan fuera del barrido — no se reescribe historia.
- 🔴 **La tardanza de la cancelación la calcula el TRIGGER, no el cliente** (`scripts/add-late-cancel-server-side.sql`, **CORRIDO el 19/08/2026**). `cancelled_late` la venía escribiendo la app y el trigger le creía. Dos problemas, los dos de plata: (1) **era incorrecta fuera de Argentina** — `isCancelLate` armaba el instante con `new Date(year, month-1, day, h, m)`, que interpreta una hora guardada en horario argentino según la zona **del dispositivo**, así que desde Madrid caía 5 horas antes y alguien cancelando con 29hs de anticipación quedaba marcado como tardío y perdía el reembolso; (2) **era falsificable** — ninguna política restringe su valor, así que contra la API directa se podía cancelar una hora antes mandando `cancelled_late = false` y cobrar igual. Ahora la función calcula el instante con `AT TIME ZONE 'America/Argentina/Buenos_Aires'` y **pisa** lo que mande el cliente; la columna pasa a ser un dato derivado y auditable contra `scheduled_date`/`scheduled_time`. ⚠️ **Se pisa el valor en vez de revocar el grant a propósito**: el flujo de cancelación escribe `status`, `cancelled_by` y `cancelled_late` en un solo UPDATE, y si una columna queda fuera del grant **falla el UPDATE entero** (lección de la sesión 104) — o sea que revocar dejaría sin poder cancelar a cualquiera con una build vieja. Como el trigger es BEFORE UPDATE, la fila que vuelve trae ya el valor corregido, así que el mensaje que ve la persona sigue la decisión del servidor.
- **Trigger `trg_mark_refund_on_cancel`** (`scripts/add-refund-on-cancel-trigger.sql`, **corrida** — verificado 18/07/2026 que el trigger existe en prod): BEFORE UPDATE OF status en `bookings`. Al pasar a `'cancelada'` un booking con `payment_status = 'aprobado'`, lo marca `'reembolso_pendiente'` — **salvo** una cancelación **tardía del propio usuario** (`cancelled_by = 'usuario'` AND `cancelled_late = true`), que pierde el reembolso como penalidad (política Andre 15/07). Centraliza el reembolso de los 5 caminos de cancelación (coach rechaza/cancela, competidores por slot ×2, expiry) que del lado cliente no tocaban el pago; sin esto, `mp-process-refunds` nunca los agarraba. El path de cancelación del usuario (`SalaScreen`) ahora setea `cancelled_late` (antes solo lo hacía el coach), que el trigger usa para distinguir temprana (reembolsa) de tardía (no). **Verificado funcionando con plata real el 09/08/2026.**
- **Cron `paypal-process-refunds`** (`scripts/add-paypal-refund-cron.sql` — **CORRIDO y VERIFICADO el 24/08/2026**, cada 5 min, jobid 8). 🔴 **Verificado por la RESPUESTA, no por `active = true`**: `net.http_post` a mano devolvió `200 {"processed":0,"failed":0}` (request 19405). Es la distinción que costó dos semanas y media de reembolsos sin procesar en julio — el job del placeholder también figuraba activo mientras daba 401.) Gemelo del de Mercado Pago. ⚠️ **Cada riel necesita SU procesador y SU cron**: `trg_mark_refund_on_cancel` marca `'reembolso_pendiente'` venga de donde venga el pago, y cada función filtra por su `payment_provider`. Si el de PayPal no corre, esas reservas se quedan marcadas para siempre y **nadie recibe su plata**, sin error visible. Reusa el secret `service_role_key` del Vault y trae el mismo chequeo anti-placeholder que costó dos semanas y media de reembolsos sin procesar en julio.
  - **Reembolsa `charged_amount`** (lo que pagó el cliente), no `amount`. 🔴 **PayPal se queda con su comisión aunque devuelvas**: esos ~USD 3,74 los pone VIVE. Es costo estructural del riel —el modelo es cobro-al-reservar con reembolso automático, así que los reembolsos son parte del diseño— y es una de las razones del markup en el precio.
  - Trata `CAPTURE_FULLY_REFUNDED` / `ALREADY_REFUNDED` como éxito: sin eso, una reserva devuelta a mano desde el panel de PayPal se quedaría reintentando hasta el dead-letter y figuraría como pendiente para siempre.
- **Cron `mp-process-refunds`** (`scripts/add-refund-cron.sql`, cada 5 min, agendado 24/07/2026) — ⚠️ **incidente del 09/08/2026, mismo patrón que el de `complete_confirmed_sessions()` del 01/07: el objeto existía y aun así no funcionaba nada.** El script se corrió **sin reemplazar el placeholder**: `vault.secrets['service_role_key']` contenía literalmente el texto `<PEGAR_SERVICE_ROLE_KEY>` (24 caracteres). El cron mandaba `Authorization: Bearer <PEGAR_SERVICE_ROLE_KEY>`, el gateway respondía `401 UNAUTHORIZED_INVALID_JWT_FORMAT` **antes de que la función corriera**, y `net._http_response` acumulaba un 401 cada 5 minutos desde el 24/07. **Dos semanas y media sin procesar un solo reembolso**, con el código de `mp-process-refunds` perfectamente sano. Corregido el 09/08/2026 con `vault.update_secret`; verificado disparando el command exacto del cron → `200 {"processed":0,"failed":0}`.
  - **La key que va en el Vault es la nueva `sb_secret_…`**, no la `service_role` legacy con formato JWT: es la que quedó en el secret `SUPABASE_SERVICE_ROLE_KEY` de las edge functions después de la rotación del 07/08/2026, y es contra ese valor exacto que `mp-process-refunds` compara el header. Pegar la legacy deja el mismo 401 con otro origen.
  - Moraleja repetida por tercera vez en este proyecto: **un cron agendado y activo no prueba que algo esté corriendo.** Para los que hacen HTTP saliente, la verificación real es `select status_code, content from net._http_response order by created desc`.

### `reviews`
- `id` (uuid, PK)
- `booking_id` (uuid, FK → `bookings.id`) — booking que originó la review; queda fijo en la creación aunque la review se edite en sesiones futuras (auditoría)
- `reviewer_id` (uuid, FK → `profiles.id`) — quien escribe la review
- `reviewed_id` (uuid, FK → `profiles.id`) — quien recibe la review
- `rating` (int, NOT NULL) — CHECK BETWEEN 1 AND 5
- `comment` (text, nullable) — comentario público por default
- `is_private` (boolean, NOT NULL DEFAULT false) — si true, solo visible para autor y destinatario
- `created_at` (timestamptz, NOT NULL DEFAULT now())
- `updated_at` (timestamptz, NOT NULL DEFAULT now()) — actualizado automáticamente por trigger
- UNIQUE(`reviewer_id`, `reviewed_id`) — una sola review por par, editable pero NO borrable
- Trigger `reviews_before_update` (BEFORE UPDATE): rechaza cambios en `reviewer_id`, `reviewed_id` y `booking_id` (inmutables); actualiza `updated_at`. Se usó trigger y no RLS porque `WITH CHECK` no tiene acceso a `OLD`.
- RLS: SELECT público para `is_private=false`; privadas solo visibles para autor o destinatario. UPDATE solo si `reviewer_id = auth.uid()`. Sin política de DELETE → borrado bloqueado por RLS.
- **INSERT endurecido 07/08/2026 (`scripts/harden-reviews-insert.sql`, CORRIDO por Andre — verificación del paso 3 sin confirmar contra output).** La política vieja era solo `reviewer_id = auth.uid()`: no validaba que el booking fuera del que reseña, ni que la sesión estuviera `completada`, ni que `reviewed_id` fuera el coach de esa reserva. `ReviewScreen.tsx` armaba todo eso bien, pero la pantalla no es la frontera de seguridad — contra la API directa, cualquier cuenta con sesión válida podía insertar una reseña de **cualquier** coach reusando un `booking_id` propio. Costo de fabricar reputación: N cuentas, no N sesiones pagadas. La política nueva (`reviews_insert_completed_session`) exige vía EXISTS que exista el booking, que sea del `auth.uid()`, que esté `completada` y que su coach sea el `reviewed_id` — mismo patrón que `session_notes_coach_all`, con el join a `coaches` que impone la lección 19 (`bookings.coach_id` es `coaches.id`, `reviews.reviewed_id` es `profiles.id`). Efecto lateral buscado: `booking_id` pasa a ser obligatorio de hecho (con NULL el EXISTS no matchea), sin un NOT NULL que rompería filas históricas. **Importa ahora** porque el deck v3 usa una barra de calidad sobre el rating, y una barra sobre estrellas falsificables es decorativa.

**Diseño:** el schema/trigger/RLS son genéricos y soportan bidireccionalidad por simetría técnica (`reviewer_id`/`reviewed_id` son ambos `profiles.id`, sin ninguna columna ni constraint que distinga dirección), pero **el producto es unidireccional a propósito: usuario reviewea coach, nunca al revés** (decisión de producto, 01/07/2026 — ver Notion "Decisiones estratégicas", sección "Reviews: unidireccionales"). Un coach no reviewea usuarios ni siquiera en formato liviano/interno — VIVE no es un marketplace bidireccional tipo Airbnb/Uber, y no existe ningún mecanismo downstream que use una calificación de usuario. El único INSERT a `reviews` en todo el código es `ReviewScreen.tsx` (usuario → coach); `ReviewScreen.tsx` además redirige a cualquier coach que llegue ahí (por notificación vieja o mal generada) a `/(coach)/reservas` en vez de mostrar el formulario. `reviewed_id` apunta siempre a `profiles.id` — para coaches: es `coaches.profile_id`, NO `coaches.id`. Una review por par: el UNIQUE sobre `(reviewer_id, reviewed_id)` permite editar pero no crear una segunda. `updated_at` deja rastro de ediciones — un coach no puede pedirle a un usuario que *borre* una review negativa (imposible por RLS), solo que la edite, lo cual queda registrado.

### `reports`
- `id` (uuid, PK), `reporter_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — quién reporta), `reported_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — a quién), `reason` (text, NOT NULL — slug de motivo, **sin CHECK**: la lista vive en `lib/reports.ts` `REPORT_REASONS`, mismo criterio que `coach_topics`), `details` (text, nullable — texto libre), `sala_id` (uuid, FK → `salas.id` ON DELETE SET NULL — contexto si el reporte salió del chat), `status` (text, NOT NULL default `'pendiente'`, CHECK IN `pendiente`/`revisado`/`accionado`/`descartado`), `created_at`.
- Reportes de moderación, **bidireccionales**: usuario→coach y coach→usuario (la dirección se deduce de `profiles.role`, no hay columna para eso). El equipo VIVE revisa a mano vía SQL (no hay panel admin todavía); **sin bloqueo automático** de la cuenta reportada.
- Entrada desde `components/ReportSheet.tsx` (hoja compartida): en `SalaScreen` (botón "⋯" del header, `reported_id` = la otra parte, `sala_id` seteado) y en `ProfesionalScreen` (link "Reportar" al pie, `reported_id` = coach, `sala_id` null; oculto en el propio perfil, pide login). Inserta vía `submitReport` (`lib/reports.ts`) + evento `registrarEvento('reporte_enviado', …)`.
- RLS: `reports_insert_own` (INSERT solo si `reporter_id = auth.uid()`), `reports_select_own` (SELECT solo los propios — para evitar spam desde la UI; nadie ve los reportes recibidos). **Sin UPDATE/DELETE** desde el cliente (el equipo gestiona `status` con el service role). Índice `reports_status_created_idx (status, created_at desc)`.
- **Corrida — verificado 07/08/2026** sondeando la tabla con la anon key (la nota anterior decía "FALTA correr" y estaba desactualizada).

### `blocked_users`
- `id` (uuid, PK), `blocker_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — quién bloqueó), `blocked_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — a quién), `reason` (text, nullable — slug opcional, reusa `REPORT_REASONS`), `created_at`. **UNIQUE(`blocker_id`, `blocked_id`)** + CHECK `blocker_id <> blocked_id`.
- Bloqueo entre usuarios (guideline **1.2** de Apple: una app con contenido generado por usuarios necesita filtrado + reporte + **bloqueo** + contacto publicado; `reports` era la mitad). **Direccional en la tabla, simétrico en el efecto:** se guarda quién bloqueó a quién, pero un bloqueo en cualquier sentido corta mensajes y reservas para los dos. Si fuera unidireccional, la persona bloqueada podría seguir escribiéndole a quien la bloqueó, que es justo lo que la guideline impide.
- RLS: `blocked_users_select_own` / `_insert_own` / `_delete_own`, las tres sobre `blocker_id = auth.uid()`. **Nadie puede consultar quién lo bloqueó a él** — si pudiera, el bloqueo sería una notificación. Sin UPDATE a propósito (un bloqueo no se edita: se borra y se rehace). Índices `blocked_users_blocker_idx` y `blocked_users_blocked_idx`.
- **`are_blocked(a uuid, b uuid) → boolean`** (`SECURITY DEFINER`, `stable`): ¿hay bloqueo entre estos dos en cualquier dirección? Es `SECURITY DEFINER` porque tiene que ver las dos direcciones y el RLS solo expone la propia — sin esto el lado bloqueado consultaría la tabla, no vería nada y concluiría que puede escribir. `REVOKE` explícito de `anon` además de `PUBLIC` (ver regla crítica 18).
- **Enforcement por TRIGGER, no por policy** (`trg_block_messages_between_blocked` en `messages`, `trg_block_bookings_between_blocked` en `bookings`, ambos BEFORE INSERT, ambos levantan `check_violation` con un mensaje que empieza con `blocked:`). Se eligió trigger porque las policies de `messages`/`bookings` se crearon a mano en el panel de Supabase y no están versionadas en ningún script: reescribirlas desde una migración es reescribir algo que no podemos leer. Un BEFORE INSERT es aditivo y aplica venga de donde venga el insert, service role incluido. ⚠️ El de `bookings` resuelve `coaches.profile_id` a partir de `bookings.coach_id` (que es `coaches.id`, no `profiles.id` — reglas 1 y 2).
- **Lo que el bloqueo NO hace:** no borra la conversación ni el historial de sesiones, **no cancela sesiones ya agendadas** (eso se cancela aparte, con su reembolso — dicho explícitamente en la UI y en T&C §14.3), y no le avisa nada a la persona bloqueada.
- Front: `lib/blocking.ts` (acciones + cache module-level de "a quién bloqueé yo", limpiado en `signOut`), `hooks/useBlockedFilter.ts` (filtra el catálogo — se aplica en las pantallas y **no** dentro de `coachesCache`, porque ese mismo cache lo usa `CoachVisibilityScreen` para calcular el standing del coach contra el pool COMPLETO de su puerta), `components/UserActionsSheet.tsx` (menú "⋯": reportar / bloquear), `screens/BlockedAccountsScreen.tsx` (`/cuentas-bloqueadas`, para deshacer).
- **Tabla nueva, agregada 13/08/2026 (`scripts/add-user-blocking.sql`) — CORRIDA y VERIFICADA el 13/08/2026 por Andre.** Confirmado con queries directas, no por asumir (regla crítica 10): `pg_trigger` devuelve las 2 filas esperadas (`trg_block_messages_between_blocked` → `messages`, `trg_block_bookings_between_blocked` → `bookings`) y `pg_get_functiondef('public.are_blocked(uuid,uuid)'::regprocedure)` devuelve la definición. Las queries quedaron al pie del script para poder repetirlas.
  - ⚠️ **Nota operativa:** el SQL editor de Supabase muestra solo el resultado de la ÚLTIMA sentencia cuando se corren varias juntas. Las dos verificaciones hay que correrlas por separado o la primera se pierde en silencio y parece que no devolvió nada.

### `guarantee_claims`
- `id` (uuid, PK), `booking_id` (uuid, FK → `bookings.id` ON DELETE CASCADE, **UNIQUE**), `user_id` (uuid, FK → `profiles.id`), `coach_id` (uuid, FK → `coaches.id` ⚠️ — `coaches.id`, no `profiles.id`, igual que `bookings.coach_id`), `status` (text, default `'pedida'`, CHECK IN `pedida`/`aprobada`/`rechazada`), `requested_at`, `resolved_at`, `resolved_by` (text — mail del equipo, no hay panel admin), `notes`.
- Mecanismo de la **garantía de primera sesión** (T&C §9.3). `scripts/add-guarantee-claims.sql`.
- **Hallazgo: el reembolso ya existía y no hacía falta reescribir historia.** `mp-process-refunds` selecciona **solo** por `payment_status = 'reembolso_pendiente'` y **nunca mira `status`** (`index.ts:39`), así que una sesión `'completada'` marcada así la reembolsa el cron que ya corre, con su `status` intacto. La nota anterior —que esto exigía reescribir el `status` histórico— partía de una lectura equivocada de esa función. Lo que faltaba era permiso para poner la marca (el RLS de usuario solo deja UPDATE hacia `status='cancelada'`) y alguien que validara §9.3.
- **Por qué tabla y no columna en `bookings`:** la solicitud tiene ciclo propio y §9.3 se reserva poder **denegar por abuso**. Una columna no sabe expresar "pedida y rechazada", y perder ese registro haría imposible detectar a quien reincide.
- RLS: `guarantee_claims_select_own` (SELECT, `user_id = auth.uid()`). **Sin INSERT/UPDATE/DELETE desde el cliente a propósito** — la única vía de escritura es la edge function. Si algún día se agrega un botón en la app, ese botón llama a la misma función; abrir una policy de INSERT dejaría al cliente crear solicitudes salteándose las validaciones. Índices `guarantee_claims_user_idx (user_id, status)` y `guarantee_claims_status_idx (status, requested_at)` — este último ascendente, al revés que `reports`, porque es una promesa con reloj.
- ⚠️ **`'pedida'` es el default de la columna y NO lo escribe nadie.** `guarantee-claim` crea la fila recién **al resolverla**, ya en `'aprobada'` o `'rechazada'` — consecuencia directa de que el intake sea el mail: no hay solicitud en la base hasta que alguien la contesta. Un contador de "pendientes" sobre esta tabla siempre da 0 (`AdminScreen` tenía uno así hasta el 15/08/2026). Por eso la pestaña de Garantías del panel arranca pegando el ID de la reserva y no eligiendo de una lista.
- **Edge function `guarantee-claim`** (service role **o admin logueado**): valida las 5 condiciones de §9.3 y, si pasan, escribe el claim y marca `payment_status = 'reembolso_pendiente'`. **No llama a MP**: el refund lo hace `mp-process-refunds`. Soporta `dry_run` (valida sin escribir) y `reject` (registra la denegación, incluso si además no calificaba). El intake sigue siendo el mail, como dice §9.3 — ver `docs/garantia-runbook.md`.
  - ⚠️ **`resolved_by` se deriva del JWT cuando llama un admin y el body no puede pisarlo** (15/08/2026). Es el único rastro de quién resolvió una garantía; si saliera del body sería un campo de auditoría que elige su propio actor — el mismo problema que un `is_admin` que viniera del cliente. Para el runbook (service role) no hay identidad que derivar y ahí sí vale lo que mande el `curl`.
  - "Primera sesión del vínculo" se cuenta con el **mismo criterio que la comisión** en `mp-create-payment` (`'completada'` excluyendo checkouts abandonados) y comparando **fecha Y hora**: comparar solo por `scheduled_date` dejaba pasar dos sesiones el mismo día.
  - "Una sola vez por Cliente" cuenta claims **`aprobada`**, no `pedida`: si contara pedidas, un rechazo por abuso le quemaría el único intento a alguien legítimo.
  - La **comisión se resuelve sola**: el refund va sin `amount` con el token del coach, así que MP revierte el pago entero incluido el `application_fee` — que es exactamente lo que §9.3 promete.
- ⚠️ **Riesgo no resuelto por esto:** si el coach ya retiró los fondos, el refund contra MP puede fallar y caer al dead-letter de `refund_attempts`. Es política de *money release*, no de esta función.
- **Tabla nueva, `scripts/add-guarantee-claims.sql` — CORRIDA y VERIFICADA el 13/08/2026 por Andre**: `information_schema.columns` devuelve las 9 columnas y `pg_policies` una sola fila (`guarantee_claims_select_own` / SELECT / `{authenticated}`), o sea que no hay INSERT/UPDATE abiertos al cliente. **La edge function `guarantee-claim` fue deployada por Andre y verificada el mismo día**, sin CLI, sondeando el endpoint (este entorno no tiene el CLI de Supabase logueado): sin auth devuelve el `401 UNAUTHORIZED_NO_AUTH_HEADER` del gateway (la ruta existe; una función inexistente da `404 NOT_FOUND`), y **con la anon key devuelve `401 Unauthorized` en texto plano**, que es la respuesta del propio código. Esa última es la que prueba las dos cosas juntas: que el código corre y que el guard de service role rechaza la anon key. Vale como patrón para verificar cualquier edge function desde acá.

### Panel de administración (`profiles.is_admin`, 14/08/2026)
- **`profiles.is_admin`** (boolean, NOT NULL DEFAULT false) — habilita el panel (`/admin`). Se asigna **a mano por SQL**; el cliente no puede escribirla. `scripts/add-admin-flag.sql`.
  - ⚠️ **Nace protegida gratis, por cómo quedó `lock-privileged-columns.sql`**: como ahí se revocó el UPDATE de tabla completa a `authenticated` y se otorgaron columnas NOMBRADAS, una columna agregada después **no queda otorgada**. Nadie puede auto-nombrarse admin sin que hubiera que hacer nada extra.
- **Por qué una columna y no un tercer valor de `role`:** `role` decide el enrutamiento entre `/(tabs)` y `/(coach)` en `app/_layout.tsx`; un tercer valor obliga a tocar ese árbol. El admin es un usuario normal que además ve una entrada más en su perfil.
- **`is_admin()`** (`SECURITY DEFINER`, `stable`) — se usa dentro de políticas de otras tablas, así que no debe depender de qué ve el invocador sobre `profiles`. `REVOKE` explícito de `anon` (regla crítica 18).
- **Lectura por política, escritura por edge function.** Cuatro políticas de SELECT (`reports_select_admin`, `guarantee_claims_select_admin`, `coaches_select_admin`, `profiles_select_admin`) para que el panel liste; **ninguna de UPDATE**. Todas las escrituras pasan por **`admin-actions`**, que resuelve la identidad desde el JWT —lo único que el cliente no puede falsificar—, confirma `is_admin` **con service role** (si consultara con el cliente del invocador, una política mal puesta lo haría fallar ABIERTO) y recién ahí escribe. Si el panel escribiera directo habría que reabrir justo las columnas que cerró `lock-privileged-columns.sql`.
- Acciones: `set_coach_verified` (aprobar/revocar una postulación — es lo que publica o despublica a un profesional, porque `coachesCache` filtra por `verified`), **`reject_coach_application`** y `resolve_report`. Las garantías **no** se resuelven por `admin-actions`: reusan `guarantee-claim`, que acepta service role **o** un admin logueado, para no duplicar las validaciones de §9.3.
  - ⚠️ **Revocar no es rechazar, y `set_coach_verified(false)` no toca `application_status`.** La postulación fue efectivamente aprobada en su momento; reescribirla a `'rechazada'` borraría por qué el coach está afuera del catálogo. `reject_coach_application` tiene un guard `.eq('verified', false)` justamente para que un tap en la pantalla equivocada no pueda despublicar a un coach activo.
  - `reject_coach_application` **exige motivo** (400 sin él). El motivo le llega al coach por notificación y queda en `application_notes`: como puede volver a postularse, sin motivo la segunda vuelta sería idéntica a la primera.

### `admin_audit_log` (15/08/2026)
- `id` (uuid, PK), `admin_id` (uuid, FK → `profiles.id` **ON DELETE SET NULL**, nullable), `admin_email` (text — **desnormalizado a propósito**), `action` (text NOT NULL), `target_type` (text NOT NULL — `coach`/`report`/`booking`), `target_id` (uuid), `details` (jsonb), `created_at`. Índices `(created_at desc)` y `(target_type, target_id)`.
- Cierra el ⚠️ de "sin auditoría": antes, quién aprobó a quién quedaba solo en el `console.log` de la edge function, que rota y no se puede consultar. Con dos cuentas `is_admin` eso ya no alcanzaba.
- **Por qué `admin_id` es nullable con `ON DELETE SET NULL` y además se guarda el mail:** un log de auditoría tiene que sobrevivir a que se borre la cuenta de quien actuó. Con un FK `NOT NULL`, dar de baja a un admin borraría su historial o bloquearía la baja.
- RLS: `admin_audit_log_select_admin` (SELECT con `is_admin()`). **Ninguna policy de INSERT/UPDATE/DELETE** y `revoke all … from authenticated, anon` + `grant select`. La única vía de escritura es `admin-actions` con service role: un log que su propio actor puede editar no es auditoría.
- La escritura se hace **después** de la acción y su fallo **no la deshace** — se devuelve como `warning` en la respuesta en vez de tragarse, porque una auditoría que falla en silencio promete un rastro que no existe.
- **Corrido y deployado el 14/08/2026, verificado.** `add-admin-flag.sql` corrido; dos cuentas marcadas `is_admin`. `admin-actions` deployada (v1) y `guarantee-claim` re-deployada (v1 → **v2**, cambió su chequeo de auth). Sondeo del endpoint: sin auth da el 401 del gateway, una función inexistente da 404, y **`admin-actions` con la anon key devuelve `{"error":"token inválido"}`** — respuesta del propio código, que prueba que corre y que la anon key no sirve para hacerse pasar por un usuario. ⚠️ El sondeo **no** distingue v1 de v2 de `guarantee-claim` (ambas contestan igual sin un token de admin real): eso lo prueba el número de versión en `functions list`, no la prueba.

### `session_notes`
- `id` (uuid, PK), `booking_id` (uuid, FK → `bookings.id` ON DELETE CASCADE — la sesión), `coach_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — autor, = `coaches.profile_id` / auth uid del coach), `user_id` (uuid, FK → `profiles.id` ON DELETE CASCADE — el cliente), `content` (text, NOT NULL), `shared` (boolean, NOT NULL default false — false: privada solo coach · true: la ve el usuario), `created_at`, `updated_at`. **UNIQUE(`booking_id`, `shared`)** → 1 privada + 1 compartida por sesión, editables (upsert con `onConflict: 'booking_id,shared'`).
- Notas de sesión del coach (medida anti-fuga #4, sesión pegajosa — ver memoria `project_vive_anti_disintermediation`). El coach escribe desde `components/SessionNotesSheet.tsx` (pill "Notas" en el header del chat, lado coach = `!recipientIsCoach`, tied a `activeBooking`). **Desde el 31/08/2026 las notas se leen INTERCALADAS EN EL HILO del chat, todas y ordenadas por `created_at` junto a los mensajes** (`getRelationshipNotes`, filtrando por el par `user_id`+`coach_id` de la sala en vez de por un `booking_id`): el usuario ve las compartidas, el coach ve las compartidas **y las suyas privadas**, estas últimas con tratamiento visual propio (punteada + candado + "Solo la ves vos"). Antes se mostraba UNA sola, la de `activeBooking`, y como `activeBooking` prioriza la sesión próxima sobre la terminada, **la nota de la sesión anterior desaparecía del chat apenas se reservaba la siguiente**, aunque siguiera en la base. ⚠️ `session_notes` NO está en la publicación de realtime: una nota escrita por el coach le aparece al usuario recién al reabrir el chat. `lib/sessionNotes.ts` (`getSessionNotes`/`getSharedNote`/`saveSessionNote` — vaciar el campo borra esa fila).
- **Por qué tabla aparte y no columnas en `bookings`:** el RLS de Postgres es por FILA, no por columna — el usuario ya lee su booking, así que una columna de nota privada le quedaría visible. Acá el RLS filtra la privada por completo.
- RLS: `session_notes_coach_all` (el coach FOR ALL sobre lo suyo). `USING (coach_id = auth.uid())` para leer/editar/borrar lo propio; **`WITH CHECK` más estricto** en insert/update: además de `coach_id = auth.uid()`, exige (vía EXISTS) que la reserva exista, que su coach sea quien inserta (`coaches.profile_id = auth.uid()`) y que `user_id` sea el cliente real del booking (`bookings.user_id = user_id`) — así un coach no puede escribir notas sobre reservas ajenas ni marcar una compartida para un usuario que no es su cliente. `session_notes_user_read_shared` (el usuario SOLO SELECT de compartidas propias, `user_id = auth.uid() AND shared = true`). La nota privada nunca entra en el filtro del usuario. Índice `session_notes_booking_idx (booking_id)`.
- ⚠️ **Tabla nueva, agregada 06/08/2026 (`scripts/add-session-notes.sql`), corrida por Andre — pero el `WITH CHECK` estricto se agregó DESPUÉS (hardening de seguridad). RE-CORRER el script (es idempotente, `drop policy if exists`) para aplicar la policy nueva.**

### `analytics_events`
- `id` (uuid, PK)
- `user_id` (uuid, FK → `auth.users.id`, nullable)
- `event_name` (text), `properties` (jsonb), `created_at`
- 🔴 **La RLS rechazaba TODA la analítica sin sesión** (encontrado en dispositivo el 01/09/2026): `new row violates row-level security policy for table "analytics_events"`. La política de INSERT que había pedía sesión, y la mitad más importante de lo que hay que medir pasa antes de que exista una cuenta — las cuatro pantallas del onboarding y, sobre todo, `muro_cuenta_visto`, que por definición se emite cuando la persona NO está registrada. Todo caía en el `console.warn` de `registrarEvento` sin llegar a la tabla. **Arreglo: `scripts/add-analytics-anon-insert.sql`** (**CORRIDO el 01/09/2026**, pendiente de verificar en dispositivo que ya no aparezca el warn), que agrega una política de INSERT para `anon` con dos candados: solo INSERT (no puede leer nada) y `user_id` obligatoriamente NULL, para que nadie pueda escribir eventos atribuidos a otra persona.
  - ⚠️ Se acepta a conciencia que con la anon key se puedan insertar filas basura: es inherente a medir desde el cliente, el daño máximo es ensuciar una tabla de métricas, y `properties.sesion` permite descartar un origen entero si aparece ruido.
- 🔴 **`user_id` es NULL en todo el onboarding**, y no es un bug: `registrarEvento` lo saca de la sesión, y durante el onboarding **no hay sesión**. Por eso los eventos del onboarding llevan **`properties.sesion`**, un id anónimo por dispositivo (`lib/onboardingAnalytics.ts`) que es lo único que permite hilar el recorrido de una persona. **Para cualquier embudo del onboarding hay que agrupar por `properties->>'sesion'`, no por `user_id`.**
  - El puente entre las dos mitades es **`onboarding_registro`**: lo emite `AuthContext` cuando por fin aparece la cuenta, y es la única fila que lleva `user_id` **y** `sesion`. Con él se responde "de los que contestaron X, cuántos terminaron creando una cuenta"; sin él el embudo se corta justo antes de la conversión. Se emite una sola vez por dispositivo y solo si hubo recorrido previo.
  - Eventos del onboarding (01/09/2026): `onboarding_pantalla_vista` · `onboarding_opcion_tocada` (con `orden`, qué número de toque es) · `onboarding_respuesta` (con `toques` y `segundos`) · `onboarding_fin` (con `destino`) · `onboarding_registro`. Y en Profesionales, `conexiones_puerta_abierta` (con `sugerida`), que es lo que permite comparar la puerta que el onboarding sugiere contra la que la persona abre de verdad.
  - **`muro_cuenta_visto`** (01/09/2026) — con `motivo`: qué intentaba hacer la persona cuando se le pidió la cuenta (`reservar_sesion`, `guardar_recurso`, `ver_mensajes`, …, 16 motivos). Se emite en **`requestAuth`**, un solo lugar, y por eso no puede quedar una llamada sin medir. Es el punto de fricción más grande del producto: cruzado con el alta, dice qué acción vale la pena para registrarse y cuál solo espanta.
  - **`recurso_iniciado` / `recurso_completado`** (01/09/2026) — el par que da la **tasa de abandono**. `iniciado` sale de `hooks/useRecursoAbierto.ts` (una línea por pantalla, sin sesión: mide también a quien explora sin cuenta) y `completado` de **`lib/resourceCompletions.ts`**, el cuello por el que pasan las once pantallas. ⚠️ Antes `completado` estaba suelto en solo 4 de las 11, así que **siete recursos se completaban sin dejar rastro**.
    - 📝 `recurso_completado` lleva **`con_cuenta`**: separa la completación de quien tiene cuenta (que además deja fila en `resource_completions`) de la de quien está explorando sin registrarse, que solo existe como evento.
  - ⚠️ `properties.sesion` **no identifica a nadie**: es un número al azar guardado en el dispositivo, sin relación con el mail, el nombre ni el teléfono.

### `notifications`
- `id` (uuid, PK)
- `recipient_id` (uuid, FK → `profiles.id`)
- `type` (text) — CHECK constraint activo. Valores válidos: `'reserva_nueva'` | `'reserva_confirmada'` | `'reserva_rechazada'` | `'reserva_cancelada'` | `'recordatorio_sesion'` | `'invitacion_review'` | `'recurso_feedback_umbral'` (agregado 03/07/2026, `scripts/add-notifications-recurso-feedback-umbral.sql` — el script busca el nombre real de la constraint en `pg_constraint` en vez de asumirlo, por eso no rompe si no coincide con el nombre por convención. **Todavía no hay ningún código que dispare este tipo** — el schema está listo pero la detección de umbral en `resource_feedback` y el envío real quedan para un paso posterior) | `'propuesta_publicada'` | `'propuesta_ajustes'` (agregados 05/07/2026, `scripts/add-notifications-propuesta-types.sql` — resultado de la revisión de una propuesta de recurso, insertados **a mano por VITA** como parte del protocolo de `docs/revision-recursos.md`, nada automático; tapeables en `CoachNotificationsScreen`, navegan a `/resource-proposals`. A propósito NO hay tipo para `descartada` — una push de descarte sería punitiva; el coach lo ve solo en su historial con las notas). `'invitacion_review'` se inserta automáticamente por `complete_confirmed_sessions()` cuando un booking pasa a `'completada'` — **solo para `bookings.user_id`, nunca para el coach** (reviews son unidireccionales, ver punto 15). Ver punto 10 para el hallazgo de que esta función no existió realmente hasta el 01/07/2026 pese a estar documentada como corrida desde el 23/06.
- `booking_id` (uuid, nullable)
- `title` (text), `body` (text)
- `read` (boolean, DEFAULT false)
- `created_at` (timestamptz)
- 🔴 **`'reserva_nueva'` está en el CHECK y NADIE LO INSERTA — verificado el 08/09/2026 con un grep sobre todo el repo.** El tipo existe desde el principio y el hueco ya estaba anotado en el CHANGELOG, pero quedó ahí. El coach **sí** recibe la push cuando entra una solicitud (`supabase/functions/_shared/booking-effects.ts:159`, *"Nueva solicitud de sesión 📅"*), así que el camino no está muerto — pero **la push es efímera y no deja fila**: sin permisos de notificación, descartada, o con el teléfono cambiado, no queda rastro de la reserva en ningún lado y **la campana muestra cero**. A las 24hs `expire_pending_bookings()` la cancela y devuelve la plata. ⚠️ No confundir con `'reserva_confirmada'`, que sí se inserta ahí mismo (línea ~188) pero **para el usuario, no para el coach**. El arreglo es un insert al lado del push. Ver `docs/inicio-del-coach.md` §0.2.
- **`'postulacion_aprobada'` y `'postulacion_rechazada'`** agregados 15/08/2026 (`scripts/add-application-status-and-audit.sql`, mismo patrón defensivo de buscar la constraint en `pg_constraint`). Los inserta `admin-actions` con service role al resolver una postulación, junto con el push por `exp.host`. Se agregaron **los dos** resultados y no solo el rechazo: aprobar sin avisar deja al coach descubriendo de casualidad que ya puede trabajar. Es la excepción al criterio de `propuesta_descartada` (donde una push sería punitiva) — acá el rechazo trae el motivo y la postulación se puede corregir y reenviar, así que el aviso es accionable, no una condena.

### `journal_entries`
- `id` (uuid, PK), `user_id`, `content` (text), `mood` (text — ⚠️ pese al tipo, `app/diario.tsx` inserta un número 1-5; Postgres lo castea a texto al vuelo, funciona pero es una inconsistencia pre-existente, no introducida en el rediseño de sesión 76), `created_at`
- **⚠️ `mood` CAMBIÓ DE SIGNIFICADO (sesión 164).** Era una copia del check-in del día; ahora es **el ánimo de esa entrada**, elegido en el propio Diario. Son dos preguntas distintas y no hay que volver a mezclarlas:
  - `mood_entries` = "¿cómo venís **hoy**?" — una fila por día (`UNIQUE user_id+entry_date`). La leen Inicio, la recomendación de recursos y `mood_trend_for_client`, o sea **lo que ve el profesional**.
  - `journal_entries.mood` = "¿cómo estás **ahora**?" — una fila por entrada. **Solo la lee el Diario.** No sale de la pantalla, y a propósito: el diario no alimenta la vista del profesional.
  - Motivo: el ánimo cambia durante el día. Se puede venir bien y querer anotar un mal momento de la tarde; el check-in del día no puede representar eso sin mentir. Por eso también **se pueden escribir varias entradas el mismo día con ánimos distintos** (esta tabla no tiene unique por día), y el historial muestra fecha **y hora**.
- **El Diario puede CREAR el check-in del día, y nunca pisarlo (sesión 164).** Al guardar una entrada, si no existe `mood_entries` de hoy, se inserta con el ánimo de esa entrada. Es un `insert` y **no** un `upsert` a propósito: el `UNIQUE(user_id, entry_date)` es lo que garantiza que no se sobreescriba, así que ni una carrera con el check-in de Inicio puede romper la regla. Con un upsert, un mal momento de las 23:00 redefiniría el día entero y movería la tendencia que ve el profesional. La pantalla avisa cuando lo crea ("También quedó registrado cómo venís hoy") — si no, la carita aparecería sola en Inicio al día siguiente.
- Rediseño de herramientas (sesión 76): el historial de Diario decodifica el número contra `ViveMoodColors` (`constants/theme.ts`) para el punto de color, sin emoji.
- 📝 Los 5 niveles (id + etiqueta) viven en `ViveMoods` (`constants/theme.ts`), compartidos por el check-in de Inicio y el selector del Diario. Eran una const local de `MoodCheckIn.tsx`; una segunda copia habría sido el tercer mapeo de ánimo del proyecto.

### `mood_entries`
- `id` (uuid, PK), `user_id` (FK → `profiles.id`), `mood_id` (smallint 1-5), `mood_label` (text — "Bajón"/"Cansado"/"Normal"/"Bien"/"Brillando"), `entry_date` (date). UNIQUE(`user_id`, `entry_date`) — un check-in por día, `upsert` con `onConflict: 'user_id,entry_date'`.
- Check-in de mood de Inicio ("¿Cómo venís hoy?", `components/MoodCheckIn.tsx`). Se lee con el hook `hooks/useMoodHistory.ts`. Colores de los 5 niveles centralizados en `ViveMoodColors` (`constants/theme.ts`), reusados en el check-in y ahora también en el resumen de mood de Diario (`app/diario.tsx`, rediseño sesión 76) — no redefinir esos hex en otro lado.
- Esta tabla ya existía (usada por Inicio) pero no estaba documentada acá — gap cerrado el 29/07/2026 al hacer que Diario también la lea. Sin migración nueva.

- 🔴 **`mood_trend_for_client(p_user_id, p_days)` — el coach ve la TENDENCIA, nunca las filas** (`scripts/add-mood-para-coach.sql`, ✅ **CORRIDO y VERIFICADO DE PUNTA A PUNTA el 26/08/2026** — ⚠️ **a la espera del abogado, pregunta A.10 de `docs/paquete-abogado.md`**). Los cuatro chequeos: (1) 🔴 **la puerta bloquea a un ajeno** — 0 filas, que es lo único que podía estar mal; (2) **deja pasar a un cliente propio** — 1 fila, 6 registros, promedio 2,83, dirección `sube`; (3) la función existe y **anon no puede ejecutarla** (401 `42501`, contra el 404 `PGRST202` de una inexistente — el control que le da valor a la prueba); (4) `mood_entries` sigue devolviendo 0 filas con la anon key.
  - 📝 **Cómo se prueba, porque no es obvio.** En el SQL Editor `auth.uid()` es NULL, así que el `exists` falla siempre y **cualquier chequeo da 0 filas por el motivo equivocado**. Hay que suplantar al coach con `set_config('request.jwt.claims', json_build_object('sub', <profile_id>)::text, true)`, y **todo tiene que ir en una sola transacción** porque ese `set_config` es local a ella. Además el editor muestra **solo el resultado de la última sentencia**: los chequeos tienen que salir en un único `select` final o corren sin que nadie los vea.
  - 📝 **Y hay que elegir bien a quién preguntarle.** El primer intento tomó un cliente cualquiera del coach, le tocó uno sin check-ins y dio 0 — indistinguible de "la función está rota". Los chequeos ahora eligen al cliente **con más check-ins** y avisan explícitamente cuando no hay ninguno con datos.
  - ⚠️ **Dos bugs los encontró esta verificación**, no la revisión de código: `42804` (la subconsulta escalar devuelve `integer` donde la firma decía `smallint`, y `RETURN QUERY` compara estricto) y `42P13` (`create or replace` no puede cambiar tipos de salida — el script dropea siempre antes de crear, y por eso rehace también el `grant`). Sin correrla, la feature salía muda y eso se leía como "esta persona no hizo check-ins".. Las policies de `mood_entries` siguen siendo own-only y **no se tocaron**: el coach no puede leer una sola fila. La función es `security definer`, valida adentro que exista una reserva `completada`/`confirmada` entre ese coach y ese usuario, y devuelve solo `dias_con_registro`, `promedio`, `ultimo`, `direccion` — con piso de 3 registros, porque con menos no hay tendencia sino ruido.
  - **Es función y no policy a propósito**: una policy daría las FILAS, y acá lo que se quiere entregar es un promedio y una dirección. Además se apaga con un `drop function`, mientras que una policy hay que acordarse de revertirla bien.
  - ⚠️ **El diario y la gratitud quedan afuera por completo**, y no por olvido: son texto libre donde la persona escribe lo que no le dice a nadie. El ánimo es una escala de 1 a 5.
  - **Doble interruptor**: no correr el script (o borrar la función) del lado de la base, y `MOSTRAR_ANIMO_AL_COACH` en `CoachHomeScreen`. El cliente trata la ausencia de la función como "no hay datos", así que apagar cualquiera de los dos alcanza.

### `mood_suggestions`
> ⚠️ **Nació como `resource_recommendations` y colisionó con la tabla homónima de Recursos v2** (recomendación coach → usuario). Renombrada el 06/08/2026 — ver "Colisión de nombres" abajo. **No confundir las dos.**
- `id` (uuid, PK), `user_id` (FK → `auth.users.id` ON DELETE CASCADE), `created_at` (timestamptz), `mood_id` (smallint 1-5), `mood_label` (text), `suggested_first`/`suggested_second` (text — resource_id/slug, mismo criterio que `resource_completions.resource_id`), `chosen` (text, nullable — CHECK que sea NULL o uno de los dos sugeridos). Índice `(user_id, created_at desc)`.
- Registro de la tarjeta "Para vos ahora" en Inicio (`components/ResourceSuggestionCard.tsx`): un evento por cada vez que se muestra con check-in real de hoy (no se inserta nada si no hay check-in), guardando el par de recursos sugerido y en qué orden (el orden se randomiza en cada visualización a propósito, para no sesgar el análisis por posición). Si el usuario toca uno de los dos, se hace `UPDATE` de esa misma fila con `chosen` — si no toca ninguno, queda `NULL`.
- RLS: 4 policies own-only (select/insert/update/delete, `user_id = auth.uid()`), mismo patrón que `mood_entries`.
- **`scripts/create-mood-suggestions.sql`, CORRIDO el 06/08/2026** — verificado: 4 policies en `pg_policy`. Es la primera vez que la tarjeta "Para vos ahora" registra de verdad (desde el 04/08 escribía a la tabla equivocada y fallaba en silencio).

#### Colisión de nombres `resource_recommendations` (incidente 04/08 → 06/08/2026)
El 04/08 se creó esta tabla con el nombre **`resource_recommendations`**, que **ya estaba tomado** por la recomendación coach → usuario de Recursos v2 (ver más abajo). El `CREATE TABLE IF NOT EXISTS` encontró la tabla existente y **no hizo nada**, pero los cuatro `CREATE POLICY` que venían después **sí se aplicaron, sobre la tabla de v2**. Verificado en prod el 06/08: la tabla tiene el schema de v2, 7 filas (todas recomendaciones reales de coaches) y tenía 7 policies.
- **Consecuencia 1 — el registro de la tarjeta de mood nunca funcionó** desde el 04/08: el INSERT mandaba `mood_id`/`mood_label`/`suggested_*` a una tabla sin esas columnas y con `resource_id`/`coach_id`/`room_id` NOT NULL. El error se tragaba en el `.then()`, así que no se notó. La verificación de entonces ("query de lectura contra la tabla real, RLS activo") fue inconclusa: leyó la tabla de v2.
- **Consecuencia 2 — se abrió el RLS de la tabla de v2.** `_delete_own` le dio DELETE al usuario sobre recomendaciones de su coach (v2 **no tenía** policy de DELETE, a propósito), y `_insert_own` le permitió insertar filas propias con cualquier `coach_id` (el INSERT era exclusivo del coach). `_select_own`/`_update_own` eran redundantes. Se dropearon las 4 con **`scripts/fix-resource-recommendations-policies.sql`, CORRIDO el 06/08/2026** — verificado en `pg_policy`: quedan exactamente las 3 originales de v2 (`_insert`, `_select`, `_update`) y ninguna de DELETE.
- **Lección:** antes de crear una tabla, grepear el nombre en SCHEMA.md. `CREATE TABLE IF NOT EXISTS` seguido de `CREATE POLICY` es una combinación traicionera — si la tabla ya existe, las policies terminan en la tabla equivocada sin un solo error.

### `gratitude_entries`
- `id` (uuid, PK), `user_id`, `item_1` (text, nullable), `item_2` (text, nullable), `item_3` (text, nullable), `content` (text, nullable — vestigial), `created_at`
- Diario de gratitud = **3 ítems por entrada** (decisión de producto 2026-07-12). La pantalla `/gratitud` (`app/gratitud.tsx`) escribe `item_1/2/3`; llenar solo 1 o 2 es válido (los vacíos van como string vacío). `content` quedó vestigial: la tabla nació con una sola columna de texto libre y el frontend nuevo no la lee. Registra racha vía `recordCompletion(user.id, 'gratitud', 300)`.
- Migración: `scripts/add-gratitude-items.sql` (agrega item_1/2/3, afloja NOT NULL de `content`). **CORRIDA por Andre el 07/08/2026 — verificado sondeando las tres columnas con la anon key.** Historia: la auditoría del 07/08 encontró que el script nunca se había corrido pese a estar escrito desde el 12/07, y que `app/gratitud.tsx:154-156` insertaba `item_1`/`item_2`/`item_3` — o sea que **la pantalla de Gratitud estuvo rota casi un mes** (todo guardado fallaba). Lección repetida: un script que existe en `scripts/` no implica que haya corrido; sondear la base antes de darlo por hecho.
- La racha ("N días seguidos", rediseño sesión 76) no vive en esta tabla — se calcula en `app/gratitud.tsx` sobre `resource_completions` filtrado a `resource_id='gratitud'` (mismo algoritmo de días consecutivos que `hooks/useResourceProgress.ts`, sin tabla ni migración nueva).

### `saved_resources`
- `id` (uuid, PK)
- `user_id` (uuid, nullable) — FK implícita a `auth.users.id`
- `resource_id` (text, NOT NULL) — ID del recurso guardado
- `pinned` (bool, nullable) — ⚠️ **columna vestigial, SIN uso** (ningún código la lee). El "pin al inicio" NO vive acá — se implementó como tabla aparte `pinned_resources` (ver abajo), decisión de producto 05/07/2026: pin es concepto distinto de guardar (curado, tope 4), no un flag sobre lo guardado.
- `created_at` (timestamptz, nullable)

### `pinned_resources`
- `id` (uuid, PK), `user_id` (uuid, NOT NULL, FK → `profiles.id` ON DELETE CASCADE), `resource_id` (text, NOT NULL — slug de tool de VITA, o uuid de `resources` / `coach_resources` serializado; mismo criterio text que `saved_resources`. **Sin FK a propósito**: la columna referencia tres orígenes distintos, por eso el resolver del inicio tiene que probar contra las dos tablas), `created_at`. UNIQUE(`user_id`, `resource_id`).
- Hasta **4 por usuario**, fijados a la pantalla de inicio (sección "Tus recursos pinneados" en `app/(tabs)/index.tsx`, que reemplazó a "Recursos útiles"). Concepto distinto de `saved_resources`: guardar es biblioteca ilimitada, pinnear es selección curada de 4.
- **El pin se ofrece al ABRIR el recurso, no desde la grilla de Recursos** (decisión de producto). Tres orígenes: (1) recursos de coaches del sistema viejo, desde la ficha `ResourceDetailScreen` (`/recurso`) — botón pin junto al bookmark; `resource_id` es el uuid de `resources`. (1b) recursos de **Recursos v2**, desde la ficha `app/coach-recurso.tsx` (`/coach-recurso`) — mismo `PinButton` con `icon="pin"` + `inline`, entre la campanita y el bookmark de `resource_saves`; `resource_id` es el uuid de `coach_resources` (agregado 31/08/2026: hasta entonces toda la biblioteca v2 —o sea todo lo que ve el usuario después del rediseño de la sesión 148— era impinneable, porque el pin había quedado anclado al sistema viejo). (2) tools hardcodeadas de VITA "por sesión" (con duración, se repiten en cualquier momento: `RespiracionScreen`, `RuidoScreen`, `AnclajeScreen`, `LecturasScreen`, `SuenoScreen`, `EscanerScreen`, `MeditacionScreen`, `RelajacionScreen`), desde **la pantalla propia de cada tool** vía el componente reutilizable `components/PinButton.tsx` en el header (reemplaza el spacer de ancho 60); `resource_id` es el slug de la tool (`'respiracion'`, `'ruido'`...). `PinButton` encapsula carga/toggle/tope-4/auth; además del `resourceId` acepta `icon` (`'bookmark'` por defecto en las tools, `'pin'` donde ya hay un bookmark de guardado al lado), `inline` (sin el slot de 60, para headers que arman su propia fila) e `inactiveColor`. Las tools "de registro diario" (`DiarioScreen`, `GratitudScreen` — una entrada por día) NO tienen `PinButton` en su header, llevan un pill de fecha en su lugar (rediseño sesión 76).
- ⚠️ **Ícono de `PinButton` migrado de `pin`/`pin-outline` a `bookmark`/`bookmark-outline`** (MaterialCommunityIcons, rediseño herramientas sesión 76) — unifica el vocabulario visual con el bookmark real de `saved_resources` en Recursos, aunque son conceptos distintos (pin = curado, tope 4; bookmark de Recursos = biblioteca ilimitada). Solo cambió el glyph, la tabla y la lógica de `pinned_resources` siguen intactas. ⚠️ **El glyph quedó dividido**: las tools usan `bookmark` (son la única marca de su pantalla), las dos fichas de recurso usan `pin` (ya tienen el bookmark de guardado al lado y dos glyphs iguales serían indistinguibles). Pendiente de decisión de diseño — el copy del vacío del inicio dice "tocá el marcador", que aplica al bookmark de las tools pero no al pin de las fichas.
- El inicio distingue ambos tipos por la forma del id: si está en el lookup `VITA_TOOLS` (índice de tools con íconos MaterialCommunityIcons y rutas propias, en `index.tsx`) es tool y navega a su ruta; si no, es uuid de recurso de coach → se busca **en paralelo en `resources` (filtrando `retired_at IS NULL`) y en `coach_resources` (filtrando `status = 'published'`)**, porque por la forma del id las dos son indistinguibles; según dónde aparezca navega a `/recurso` o a `/coach-recurso`, con `PINNED_TYPE_ICON` (por `resources.type`) o `PINNED_FORMAT_ICON` (por `coach_resources.format`). Si un uuid apareciera en las dos gana el mapeo viejo. Un recurso archivado/rechazado deja de resolver y su card desaparece del inicio, igual que un `retired_at` — la fila de `pinned_resources` queda huérfana pero es inofensiva. ⚠️ Los íconos de las tools se duplican por familia: las pantallas de tool y la grilla de Recursos usan Ionicons (`TOOLS` en `recursos.tsx`), el inicio usa MaterialCommunityIcons (`VITA_TOOLS` en `index.tsx`) — si se agrega/renombra una tool, tocar ambos.
- RLS: solo el dueño (SELECT/INSERT/DELETE, sin UPDATE — pin/despin es insert/delete). Tope de 4 por trigger `trg_pinned_resources_max_four` (RLS no cuenta filas) — el 5to INSERT lanza excepción; el frontend además avisa antes con conteo local, pero se defiende del error del trigger por si el conteo se desincroniza.
- Agregada 05/07/2026 (`scripts/add-pinned-resources.sql`). **Extensión a `coach_resources` el 31/08/2026: sin cambios de schema** — `resource_id` es text sin FK y el trigger del tope solo cuenta filas, así que aceptó los uuids nuevos tal cual. Todo el cambio fue de frontend.

### `resource_reminders`
- `id` (uuid, PK), `user_id` (FK → `profiles.id` ON DELETE CASCADE), `kind` (CHECK IN `tool`/`coach_resource`), `ref` (text — slug de tool o uuid de `coach_resources`, mismo patrón dual que `saved_resources.resource_id`), `title` (text, cacheado para la notificación y la lista sin resolver el recurso), `days` (smallint[], 0=Dom..6=Sáb), `hour` (smallint 0-23), `minute` (smallint 0-59), `enabled` (bool, default true), `created_at`, `updated_at`.
- **Recordatorios que el propio usuario configura** (feature en progreso, 16/07/2026) — reemplaza el botón del coach "recordarle que abra el recurso" (decisión Andre: era presión, no autonomía). Las notificaciones son **locales** (`expo-notifications`, no push) — esta tabla es la fuente de verdad que cada dispositivo re-agenda al abrir la app (las notis locales se pierden al reinstalar; la tabla las reconstruye).
- RLS: `FOR ALL USING/WITH CHECK (user_id = auth.uid())` — cada usuario ve/gestiona solo los suyos.
- Agregada 16/07/2026 (`scripts/add-resource-reminders.sql`, **corrida en Supabase**). **Solo el schema existe todavía** — falta codear el motor local (`lib/resourceReminders.ts`, RF1), la UI de configuración (campanita + chips de días/hora en tools y en `/coach-recurso`, RF2), y la pantalla "Mis recordatorios" (RF3). Nada en el frontend escribe o lee esta tabla todavía.

### `resource_completions`
- `id` (uuid, PK)
- `user_id` (uuid, NOT NULL, FK → `auth.users.id` ON DELETE CASCADE)
- `resource_id` (text, NOT NULL) — ID del recurso (ej. `"respiracion"`, `"gratitud"`), mismo criterio que `saved_resources.resource_id`
- `completed_at` (timestamptz, NOT NULL DEFAULT now())
- `duration_seconds` (int, nullable) — duración total del recurso; NULL para recursos libres (Diario, Ruido blanco)
- `progress_seconds` (int, nullable) — segundos completados; NULL = no empezado, igual a `duration_seconds` = terminado, entre medio = a medias
- Índices en `(user_id, completed_at DESC)` y `(user_id, resource_id)`
- RLS: SELECT/INSERT/UPDATE/DELETE solo si `user_id = auth.uid()`
- Agregada 02/07/2026 (`scripts/add-resource-completions.sql`). Alimenta: racha semanal (`StreakChip`) y "Continuar donde dejaste" (`ContinueCard`) en `app/(tabs)/recursos.tsx` vía `hooks/useResourceProgress.ts`, y **el check "hecho hoy" de los hábitos** en Progreso (`completedToday`, agregado 23/07/2026). **Las 10 herramientas de `constants/tools.ts` escriben acá al completarse** (las 9 de audio/ejercicio ya lo hacían vía `recordCompletion`; `app/diario.tsx` se sumó el 23/07/2026 — antes era la única que no registraba). La nota vieja de "ninguna herramienta escribe todavía" quedó desactualizada.

### `user_habits`
- `id` (uuid, PK, DEFAULT `gen_random_uuid()`)
- `user_id` (uuid, NOT NULL, FK → `auth.users.id` ON DELETE CASCADE)
- `tool_id` (text, NOT NULL) — clave del catálogo de `constants/tools.ts` (ej. `"respiracion"`), la misma que `resource_completions.resource_id`. Sin CHECK (la lista vive en el frontend, mismo criterio que `resource_completions.resource_id`)
- `sort_order` (int, NOT NULL DEFAULT 0), `created_at` (timestamptz, NOT NULL DEFAULT now())
- UNIQUE(`user_id`, `tool_id`) — una práctica aparece una sola vez en la rutina
- Índice en `(user_id, sort_order)`
- RLS: SELECT/INSERT/UPDATE/DELETE solo si `user_id = auth.uid()`
- Agregada 23/07/2026 (`scripts/add-user-habits.sql`). Guarda **solo la rutina** (qué prácticas VITA eligió el usuario) para la sección "Hábitos de hoy" de Progreso. El "hecho hoy" NO se guarda acá — se deriva de `resource_completions` (atado al uso real: completar la herramienta tilda el hábito). Rutina inicial (`respiracion`+`gratitud`) sembrada client-side la 1ª vez vía `lib/habits.ts` con guard en AsyncStorage (`vita_habits_seeded_<userId>`) para no re-sembrar si el usuario borra todo. Queries en `lib/habits.ts`, UI en `app/progreso.tsx`.

### `favorite_coaches`
- `id` (uuid, PK)
- `user_id` (uuid, NOT NULL, FK → `auth.users.id` ON DELETE CASCADE)
- `coach_profile_id` (uuid, NOT NULL, FK → `profiles.id` ON DELETE CASCADE) ⚠️ — mismo criterio que `salas.coach_id`: es `profiles.id` (= `coaches.profile_id`), **NO** `coaches.id`
- `created_at` (timestamptz, NOT NULL DEFAULT now())
- UNIQUE(`user_id`, `coach_profile_id`) — no se puede favoritear el mismo coach dos veces
- RLS: SELECT/INSERT/DELETE solo si `user_id = auth.uid()`. Sin política de UPDATE (no hay nada que actualizar, solo agregar/quitar filas)
- Agregada 01/07/2026 (`scripts/add-favorite-coaches.sql`) — reemplaza dos `useState` locales y desconectados entre sí (`conexiones.tsx` y `ProfesionalScreen.tsx`) que no persistían nada. Estado unificado vía `hooks/useFavoriteCoaches.ts`, usado en ambas pantallas y en la nueva `screens/FavoritosScreen.tsx` (`/favoritos`, accesible desde el ícono de estrella en el header de Conexiones).

### `resource_proposals`
- `id` (uuid, PK)
- `coach_id` (uuid, NOT NULL, FK → `coaches.id`) ⚠️ — mismo criterio que `coach_topics`/`coach_availability`/`coach_weekly_pattern` (tabla operativa del coach), NO `profiles.id`
- `type` (text, NOT NULL) — CHECK IN (`'audio'`, `'podcast'`, `'video'`, `'guia_pasos'`, `'lectura_breve'`). `'podcast'` y `'video'` agregados 09/07/2026 (`scripts/add-resource-types-podcast-video.sql`, **corrido en Supabase el 09/07/2026**). Decisión de producto: un **audio guía** (algo que se HACE, participativo) no es lo mismo que un **podcast/charla** (algo que se ESCUCHA, pasivo) → tipos separados aunque por debajo el podcast use el mismo bucket de audio; y el **video** se consume dentro de la app (no links a YouTube), con bucket propio. A propósito **no** incluye `'journaling'` ni `'gratitud'` — esos son recursos exclusivos de VITA, no proponibles por coaches.
- `topic` (text, nullable) — uno de los 28 subtemas de `AXES` (`constants/searchData.ts`), igual criterio que `coach_topics.topic`: sin CHECK contra la lista, vive en el frontend. Agregada 04/07/2026 (`scripts/add-resource-proposals-topic.sql`) — la migración original (03/07) no la incluía, pero el mockup ya existente de "Proponer recurso a VIVE" (`screens/CoachResourcesScreen.tsx`) ya tenía un selector de tema diseñado, así que se sumó para no perder esa UX. Es solo referencia textual para el revisor — no crea ningún vínculo a `resource_axes`/`resource_tags`.
- `axes` (text[], nullable) y `tags` (text[], nullable) — ejes (`'cuerpo'`/`'mente'`/`'alma'`, 1-3, obligatorio en el form) y tags (labels, máx. 3, opcional) que el coach propone. Agregadas 05/07/2026 (`scripts/add-resource-proposals-axes-tags.sql`). **Mismo criterio que `topic`: referencia NO vinculante para el revisor** — el vínculo real a `resource_axes`/`resource_tag_links` lo crea VITA a mano al publicar, usando esto como pista. Columnas array y no tabla puente a propósito (nunca se consultan relacionalmente). Sin CHECK contra los valores. Si el coach escribe un tag que no existe, el form además lo da de alta en `resource_tags` como `'propuesto'` (upsert best-effort con `ignoreDuplicates` por el UNIQUE de `label`; la RLS de esa tabla ya lo permite solo a coaches). ⚠️ Los dos caminos del submit (INSERT de creación y UPDATE de re-envío) incluyen `axes`/`tags` — si se agrega un campo al form, va en ambos (asimetría creación/edición ya mordió con `topic`).
- `title`, `description` (nullable), `duration_min` (int, nullable, CHECK > 0 si no es null — sin rango distinto por `type`, quedó pendiente definir si hace falta), `content` (jsonb, NOT NULL, estructura libre según `type`)
- `status` (text, NOT NULL, DEFAULT `'enviada'`) — CHECK IN (`'enviada'`, `'necesita_ajustes'`, `'aprobada'`, `'descartada'`)
- `reviewer_notes` (text, nullable)
- `created_at`, `updated_at` (timestamptz, NOT NULL DEFAULT now())
- RLS: el coach dueño (`coach_id IN (SELECT id FROM coaches WHERE profile_id = auth.uid())`) puede SELECT/INSERT/UPDATE solo sus propias propuestas. Sin política de DELETE. Nadie más tiene acceso — tabla nunca leída por un usuario final.
- Trigger `trg_resource_proposals_protect` (BEFORE UPDATE, función `fn_resource_proposals_protect_review_fields`): si la sesión viene de un usuario autenticado (`auth.uid() IS NOT NULL`), rechaza cambios a `status` y `reviewer_notes` — RLS no puede restringir por columna, así que un coach con permiso de UPDATE sobre su propia fila no podría auto-aprobarse ni borrar notas de revisión editando directo. Mismo patrón que `reviews_before_update`. También mantiene `updated_at` al día. Cuando se corre una `UPDATE` desde el SQL Editor/Dashboard (sin JWT de usuario, `auth.uid()` es NULL), el trigger no bloquea nada — así el equipo VITA aprueba manualmente sin fricción. **Excepción desde 05/07/2026** (`scripts/fix-resource-proposals-resubmit.sql`): la transición `necesita_ajustes → enviada` sí está permitida con sesión — es el re-envío del coach tras ajustar (botón "Ajustar y reenviar" en `ResourceProposalsScreen`, que abre `ProposeResourceScreen` en modo edición vía param `proposalId`; UPDATE sobre la misma fila, no INSERT). Cualquier otro cambio de status y todo cambio de `reviewer_notes` siguen bloqueados (las notas no se borran al reenviar — son historial, y quedan visibles arriba del form mientras el coach ajusta).
- El pase de propuesta aprobada a `resources` publicado es **manual, vía Supabase Dashboard** — no hay trigger ni automatismo. Decisión de producto, no limitación técnica. **El protocolo completo de revisión (query de cola, checklist editorial, SQL de las 3 acciones con las notificaciones, plantillas de copy) vive en `docs/revision-recursos.md`** (05/07/2026) — revisar siempre desde ahí para que la experiencia del coach sea consistente sin importar quién revisa.
- **Funciones de revisión** (`scripts/add-review-functions.sql`, corrido y verificado en Supabase 05/07/2026 — se usaron para la primera revisión real): vista `cola_revision` (propuestas en `enviada`, `security_invoker = true`) + `revisar_aprobar(id, axes[] default null)` (publica en `resources` resolviendo `attributed_to_coach_id` vía `coaches.profile_id` internamente, crea `resource_axes` con los ejes propuestos o los pasados por parámetro, marca `aprobada`, notifica `propuesta_publicada`; devuelve el `resource_id`; todo transaccional), `revisar_ajustes(id, notas)` (exige notas, notifica `propuesta_ajustes`) y `revisar_descartar(id, motivo)` (exige motivo, **sin** notificación). Las tres rechazan propuestas que no estén en `'enviada'`. Permisos: `REVOKE` explícito de `anon` **y** `authenticated` (lección 18) — solo SQL Editor/service role. No son `SECURITY DEFINER`. Los tags siguen siendo manuales (promoción/linkeo por tag, decisión aparte).
- **Forma de `content` por `type`** (convención de frontend, no hay CHECK que la valide en la base — decidida en `screens/ProposeResourceScreen.tsx`, 04/07/2026):
  - `audio` y `podcast`: `{ url: string }` — **URL pública del bucket `resource-audio`** (comparten mecanismo, máx. 20MB, mime de audio): el coach sube el archivo desde el formulario y la app lo reproduce con expo-audio dentro del detalle. Bucket creado por `scripts/add-resource-audio-storage.sql` (público lectura; INSERT solo coaches en su carpeta `{auth.uid()}/...`; sin UPDATE/DELETE). Los links externos del formato anterior (YouTube/Spotify/Drive) siguen funcionando como fallback (el detalle los detecta por dominio y los abre con `Linking`). Podcast/audio se distinguen solo por `type` (modo de consumo), no por forma de `content`.
  - `video`: `{ url: string }` — **URL pública del bucket `resource-video`** (`scripts/add-resource-video-storage.sql`, **corrido el 09/07/2026** — público lectura, INSERT solo coaches en su carpeta, máx. **100MB**, mime de video, sin UPDATE/DELETE; mismo patrón que `resource-audio`). Se reproduce con expo-video (`VideoView` + `useVideoPlayer`, mismo patrón que el video de perfil en `ProfesionalScreen`) dentro del detalle. Sin fallback a links externos: los videos viven en la app a propósito (decisión 09/07/2026).
  - `guia_pasos`: `{ steps: { title: string; body: string }[] }`
  - `lectura_breve`: `{ pages: string[]; source?: string }` — desde 05/07/2026 la lectura se pagina y **el coach arma las páginas** (marca el ritmo, decisión de producto tomada mirando el patrón de `LecturasScreen`). El formato anterior `{ body: string; source?: string }` sigue siendo válido como fallback (se renderiza como una sola página); el form en modo edición migra `body` → `pages` de una página al reenviar.
- **Desde 05/07/2026 el formulario SÍ tiene selección de ejes y tags** (columnas `axes`/`tags` de arriba) — pero sigue siendo referencia textual, no relación a `resource_axes`/`resource_tags`: VITA crea el vínculo real a mano al publicar, usando la propuesta del coach como pista. El único efecto colateral real del form es el alta de tags nuevos como `'propuesto'` en `resource_tags`.
- UI: formulario en `screens/ProposeResourceScreen.tsx` (`/resource-proposal-new`), lista de propuestas propias con status y `reviewer_notes` en `screens/ResourceProposalsScreen.tsx` (`/resource-proposals`). **Entry point real: el botón "Proponer recurso a VIVE" en `screens/CoachResourcesScreen.tsx`** (pestaña Recursos del coach, `app/(coach)/recursos.tsx`) — ese botón ya existía como mockup (modal con `handleSend` que solo hacía `console.log`, nunca escribía a la base) y se rewireó para navegar a `/resource-proposal-new` de verdad; el modal mock y sus estilos se borraron. Sin pantalla de edición de una propuesta ya enviada — solo alta y lectura de status.
- Agregada 03/07/2026 (`scripts/add-resource-proposals.sql`, corrido y verificado en Supabase el mismo día). Frontend agregado 04/07/2026, wireado al botón real y columna `topic` sumada el mismo día tras encontrar el mockup preexistente.

### `resources`
- `id` (uuid, PK)
- `proposal_id` (uuid, nullable, UNIQUE, FK → `resource_proposals.id`) — de qué propuesta salió; nullable porque VITA también puede cargar recursos propios sin propuesta de coach de por medio. UNIQUE para que una propuesta aprobada no termine duplicada en dos filas por error de carga manual.
- `attributed_to_coach_id` (uuid, nullable, FK → `profiles.id`) ⚠️ — a diferencia de `coach_id` de arriba, este va a `profiles.id`, mismo criterio que `favorite_coaches.coach_profile_id`/`salas.coach_id` (atribución/navegación a perfil), NO el criterio operativo de `coach_topics`. Es atribución, no implica ownership de edición — el coach nunca escribe acá directo.
- `type` (text, NOT NULL) — CHECK IN (`'audio'`, `'podcast'`, `'video'`, `'guia_pasos'`, `'lectura_breve'`, `'journaling'`, `'gratitud'`). `'podcast'`/`'video'` agregados 09/07/2026 (mismo script que `resource_proposals`, corrido el 09/07/2026). `'journaling'`/`'gratitud'` existen para que VITA pueda cargar sus propios recursos exclusivos en esta misma tabla si algún día lo decide — **el catálogo hardcodeado actual de Diario/Gratitud en `app/(tabs)/recursos.tsx` NO se migró acá, sigue viviendo aparte, sin tocar.**
- `title`, `description` (nullable), `duration_min` (int, nullable, CHECK > 0), `content` (jsonb, NOT NULL)
- `retired_at` (timestamptz, nullable) — baja editorial. Agregada 05/07/2026 (`scripts/add-resources-retired-at.sql`). **Retirar es `SET retired_at = now()`, NUNCA `DELETE`** — el DELETE arrastra por CASCADE los votos de `resource_feedback`. Todas las queries de consumo filtran `retired_at IS NULL` (`ProfesionalScreen`, biblioteca en `app/(tabs)/recursos.tsx`, y "Mis recursos" en `CoachResourcesScreen`). Republicar: `SET retired_at = NULL`. Sin notificación automática al coach.
- `created_at` (timestamptz, NOT NULL DEFAULT now())
- RLS: SELECT público (`USING (true)`), cualquiera lee, con o sin sesión. **Sin ninguna política de INSERT/UPDATE/DELETE** — con RLS activado y sin esas políticas, toda escritura desde la app queda bloqueada por defecto. La única forma de escribir es a mano vía SQL Editor/Dashboard (bypasea RLS). Verificado: INSERT como anon → rechazado por RLS.
- Nace vacía — no hay ninguna fila todavía, ni de coach ni de VITA.
- **Primer consumidor real de la SELECT pública**: `screens/ProfesionalScreen.tsx` (perfil público del coach visto por un usuario) — sección "Recursos de {nombre}" que lista lo que ese coach tiene publicado (`attributed_to_coach_id = profileId`). **Desde 05/07/2026 las tarjetas son solo listado (ícono/título/tipo/duración) y navegan a `/recurso?id=...`** — el render inline expandible por `type` se eliminó cuando el detalle del recurso pasó a ser el lugar único de consumo (la query ya ni trae `content`). Sin contadores de feedback ni de guardados — regla de negocio ya documentada ("la confianza se resuelve con curación editorial y atribución, no con cifras"). Agregado 04/07/2026.
- **Tercer consumidor (05/07/2026)**: "Mis recursos" en `screens/CoachResourcesScreen.tsx` (pestaña Recursos del coach) — el coach ve sus propios recursos publicados, leyendo con `attributed_to_coach_id = user.id` (el `user.id` del AuthContext ES `profiles.id`, verificado — no confundir con `coaches.id`; el INSERT de aprobación del protocolo escribe `c.profile_id`, misma referencia por construcción). Reemplazó al mock `MY_RESOURCES`. Muestra además "A {n} personas les sirvió" vía `get_my_resource_feedback_summary()` **solo cuando n > 0** (nunca "A 0 personas" — regla anti-ansiogénica) y solo el conteo positivo (el negativo es insumo de VITA, no del coach).
- **Segunda sección agregada el mismo día**: `app/(tabs)/recursos.tsx` (biblioteca general que ve cualquier usuario) suma "Recursos de nuestros coaches" — scroll horizontal con lo publicado por cualquier coach (`attributed_to_coach_id is not null`), tarjeta con tipo/duración/nombre del coach. **Desde 05/07/2026 el tap navega al detalle del recurso (`/recurso?id=...`, `screens/ResourceDetailScreen.tsx`), no al perfil del coach** — decisión de producto: el recurso vive en la pantalla Recursos (ficha con tipo/duración/descripción, autor con acceso opcional al perfil, y la experiencia de uso según tipo); el perfil del coach queda como vidriera secundaria (su sección "Recursos de {nombre}" sigue igual). El detalle filtra `retired_at IS NULL` y muestra "Este recurso ya no está disponible" si el id no resuelve. Sección nueva y separada — no se tocó `TOOL_GROUPS` ni el mock `CoachSection`/`COACH_RESOURCES` que ya existía en ese archivo (ese es "recomendaciones de tu coach personal" sobre las tools de VITA, un concepto distinto que no se tocó). **Desde 05/07/2026 los recursos de coaches son guardables vía `saved_resources`** (el `resource_id` text acepta el uuid serializado, sin migración): bookmark en la tarjeta del carrusel y en el detalle (`/recurso`), conviviendo con las tools hardcodeadas en la misma tabla y el mismo `savedIds`. **El acceso a los guardados cambió el mismo día**: se eliminaron los chips toggle "Todos/Guardados" de la pantalla Recursos; ahora hay un botón bookmark arriba a la derecha del header que navega a `/recursos-guardados` (`screens/RecursosGuardadosScreen.tsx`) — mismo patrón que el botón de favoritos de Conexiones (`/favoritos`). Esa pantalla lista tools de VITA (slugs, resueltos por `VITA_TOOL_MAP` de `constants/vitaTools.ts`) y recursos de coaches (uuids, desde `resources` filtrando `retired_at IS NULL`) mezclados por orden de guardado, con bookmark para des-guardar. El catálogo de tools de VITA se centralizó en `constants/vitaTools.ts` (íconos Ionicons + MaterialCommunityIcons); lo usan el inicio (pinneados) y la pantalla de guardados — `recursos.tsx` mantiene su propio array `TOOLS` para la grilla.
- Agregada 03/07/2026 (`scripts/add-resources.sql`, corrido y verificado en Supabase el mismo día).

### `resource_axes`
- `resource_id` (uuid, FK → `resources.id` ON DELETE CASCADE), `axis` (text, CHECK IN `'cuerpo'`/`'mente'`/`'alma'`)
- PK compuesta (`resource_id`, `axis`) — un recurso puede tener más de un eje
- ⚠️ Esta taxonomía (Cuerpo/Mente/Alma) es **nueva y separada** de `AXES` en `constants/searchData.ts` (Bienestar físico/emocional-mental/Crecimiento, la que alimenta `coach_topics`) — no son la misma cosa, no se unificaron en este trabajo. Ver punto 17 de Reglas críticas para la otra confusión de taxonomías ya documentada.
- RLS: SELECT público. Sin INSERT/UPDATE/DELETE para authenticated — se escribe a mano junto con el `resource`, vía Dashboard.
- Agregada 03/07/2026 (`scripts/add-resource-axes.sql`, corrido y verificado en Supabase el mismo día).

### `resource_tags`
- `id` (uuid, PK), `label` (text, NOT NULL, UNIQUE), `status` (text, NOT NULL, DEFAULT `'propuesto'`, CHECK IN `'oficial'`/`'propuesto'`), `created_at`
- RLS: SELECT público. INSERT solo para coaches autenticados (`EXISTS (SELECT 1 FROM coaches WHERE profile_id = auth.uid())`), con `WITH CHECK (status = 'propuesto')` — un coach nunca puede crear un tag ya `'oficial'` directo. Promover a `'oficial'` o fusionar con uno existente es manual, como parte de la revisión de la propuesta que lo trajo — decisión separada de aprobar el recurso en sí.
- Agregada 03/07/2026 (`scripts/add-resource-tags.sql`, corrido y verificado en Supabase el mismo día).

### `resource_tag_links`
- `resource_id` (FK → `resources.id` ON DELETE CASCADE), `tag_id` (FK → `resource_tags.id` ON DELETE CASCADE), PK compuesta
- RLS: SELECT público. Sin INSERT/UPDATE/DELETE para authenticated — se escribe a mano junto con la aprobación de la propuesta.
- Agregada 03/07/2026 (`scripts/add-resource-tag-links.sql`, corrido y verificado en Supabase el mismo día).

### `resource_topics`
- `resource_id` (FK → `resources.id` ON DELETE CASCADE), `topic` (text, NOT NULL), `created_at`. PK compuesta (`resource_id`, `topic`). Índice en `topic`.
- **Espejo de `coach_topics`**: `topic` es uno de los **28 subtemas de AXES** (`constants/searchData.ts`), **sin CHECK** contra la lista a propósito (mismo criterio que `coach_topics` / `resource_proposals.topic` — la lista vive en el frontend). Es el nivel **fino** de taxonomía de un recurso; el nivel grueso cuerpo/mente/alma sigue en `resource_axes` (son cosas distintas, no se unificaron — ver regla 17).
- **Por qué existe**: para filtrar la biblioteca de recursos por "el problema de la gente" con la **misma taxonomía** con la que los coaches ya se etiquetan, en vez de inventar una 4ta. Primer consumidor: `screens/ExploreResourcesScreen.tsx` (`/explorar-recursos`), chips de los 28 subtemas agrupados por los 3 ejes; query `resources ⋈ resource_topics!inner` filtrando `topic` + `retired_at IS NULL`. Entry point: botón "Explorar todo →" en la sección "Recursos de nuestros coaches" de `app/(tabs)/recursos.tsx`.
- RLS: SELECT público (`resource_topics_public_read`, `USING (true)`). **Sin INSERT/UPDATE/DELETE para authenticated** — se escribe a mano al publicar (vía `revisar_aprobar`), nunca desde la app (igual que `resource_axes`/`resource_tag_links`).
- **Poblado**: `revisar_aprobar` (extendida, `scripts/update-revisar-aprobar-resource-topics.sql`) materializa `resource_proposals.topic` (que antes era solo "pista" textual del revisor) como fila real en `resource_topics` al aprobar. Nuevo param opcional `p_topics text[]` para pisar/extender; la firma vieja `(uuid, text[])` se DROPea para no dejar sobrecarga ambigua (las llamadas de 1/2 args siguen andando por los DEFAULT).
- Agregada 09/07/2026 (`scripts/add-resource-topics.sql`, **corrido en Supabase el 09/07/2026**, junto con `scripts/update-revisar-aprobar-resource-topics.sql` que la puebla al aprobar).

### `resource_feedback`
- `id` (uuid, PK), `resource_id` (FK → `resources.id` ON DELETE CASCADE, NOT NULL), `user_id` (FK → `profiles.id` ON DELETE CASCADE, NOT NULL), `sirvio` (boolean, NOT NULL), `created_at`
- UNIQUE(`resource_id`, `user_id`) — un usuario puede cambiar de opinión (UPDATE) pero no votar dos veces
- Señal binaria post-uso, **nunca expuesta como número público en la UI**. Alimenta privadamente el perfil del coach.
- RLS: el usuario ve/crea/edita solo su propia fila (`user_id = auth.uid()`). **A propósito no existe ninguna política que le dé al coach acceso de fila a esta tabla** — una policy de SELECT solo filtra filas, no columnas: si el coach tuviera acceso de fila a sus recursos atribuidos, podría pedir `select user_id` y ver el detalle de quién votó qué, violando la regla de negocio. El coach tiene **cero acceso directo** a esta tabla vía API.
- Función `get_my_resource_feedback_summary()` (`SECURITY DEFINER`, `SET search_path = public, pg_temp`) — único camino que tiene un coach para ver esta señal: filtra internamente por `resources.attributed_to_coach_id = auth.uid()` y devuelve solo `sirvio_count`/`no_sirvio_count` agregados por recurso, nunca `user_id`. Permisos: `REVOKE EXECUTE FROM anon` explícito (⚠️ hallazgo: `REVOKE ALL ... FROM PUBLIC` no alcanza para bloquear `anon` — Supabase otorga EXECUTE a `anon` en todo `public` vía default privileges a nivel de base, no vía el pseudo-rol `PUBLIC`; hubo que revocarlo de `anon` explícitamente, ver `scripts/fix-resource-feedback-summary-grant.sql`), `GRANT EXECUTE TO authenticated`.
- Detección de umbral (cada 10/25/50 "sirvió"): trigger `trg_resource_feedback_milestone` → función `fn_resource_feedback_milestone()` (SECURITY DEFINER). Dispara AFTER INSERT OR UPDATE OF sirvio; cuenta votos positivos para el resource_id; en hitos exactos 10/25/50 inserta una notificación `recurso_feedback_umbral` al `attributed_to_coach_id` del recurso. Agregado 07/07/2026 (`scripts/add-resource-feedback-milestone.sql`).
- Agregada 03/07/2026 (`scripts/add-resource-feedback.sql`, corrido y verificado en Supabase el mismo día. Fix de permisos en `scripts/fix-resource-feedback-summary-grant.sql`, mismo día).

### Recursos v2 — `coach_resources`, `resource_recommendations`, `resource_saves`, `resource_events`
Sistema **paralelo e independiente** al de `resource_proposals`→`resources` de arriba (ese sigue existiendo y en uso — alimenta "Recursos de nuestros coaches" en `app/(tabs)/recursos.tsx`). Este es el nuevo pipeline de sesión 62 (13/07/2026): el coach sube y publica directo (sin cola de revisión de VITA), consumido por la sección "Explorar por tema". `scripts/recursos-v2-migration.sql`, **corrido y verificado en Supabase el 13/07/2026**. Seed de prueba: `scripts/seed-recursos.sql`.

- **`coach_resources`**: `id` (uuid, PK), `coach_id` (FK → `coaches.id` ON DELETE RESTRICT), `title`, `description`, `format` (CHECK IN `audio`/`podcast`/`video`/`lectura`), `source` (CHECK IN `native`/`external` — native = Storage, external = link), `url` (podcast/video), `storage_path` (audio nativo, bucket `resource-audio`, hasta 30MB), `body_md` (lectura), `topic_id` (text, una de las 10 puertas de `constants/conexionesDoors.ts`, sin CHECK — vive en el frontend), **`wellness_goal`** (ver abajo), `duration_seconds`, `status` (CHECK IN `pending`/`published`/`rejected`/`archived`, default `pending`), `rejection_rule` (smallint 1-6), `is_author_declared` (bool), `created_at`. **`video` siempre usa `url` de YouTube — a diferencia de `resources` (el sistema viejo), acá NO hay bucket propio para video.**
  - **`wellness_goal`** (text, nullable, CHECK IN `calmar_ansiedad`/`dormir_mejor`/`mejorar_animo`/`ganar_foco`/`construir_habitos`/`entender_emociones`/`mover_el_cuerpo`/`alimentacion`). Agregada el 29/08/2026 (`scripts/add-resource-wellness-goal.sql`, **CORRIDO y VERIFICADO** — 4 chequeos: columna nullable, CHECK con los 8, las 11 filas viejas en NULL sin romper, valor inválido rebota con 23514). Es el OBJETIVO del recurso (para qué sirve), distinto de `topic_id` (de qué tema es). 🔴 **La columna va sin su feature**: el descubrimiento por intención que la usaría es de una fase futura (Recursos v3), pero el campo se captura AHORA porque etiquetar contenido viejo después es inviable — ningún coach vuelve a editar lo que ya subió. Nullable a nivel base (las filas anteriores al 29/08 quedan en NULL, el CHECK admite NULL), obligatoria en el CLIENTE solo para recursos nuevos. TEXT + CHECK y no ENUM nativo, misma convención que `format`/`source`/`status` — agregar un valor es un ALTER de una línea. El selector vive en `app/coach-recurso-nuevo.tsx` (`WELLNESS_GOALS`, cuyos ids están atados a este CHECK).
  - **Trigger `trg_notify_resource_status`** (`scripts/add-resource-status-notification.sql`, **CORRIDO y VERIFICADO el 29/08/2026** con una prueba real de aprobar/rechazar). AFTER UPDATE OF status: cuando el status pasa a `published` o `rejected` —lo hace el admin a mano desde el editor de tablas, ya que la moderación no tiene panel—, inserta (a) una fila en `notifications` para el `profiles.id` del coach (traducido desde `coach_resources.coach_id` = `coaches.id`) con los tipos nuevos `recurso_publicado`/`recurso_rechazado` (el rechazo menciona la `rejection_rule`), y (b) una fila en `analytics_events` (`recurso_aprobado`/`recurso_rechazado`, `{regla}` en el rechazo) — la ÚNICA vía de capturar esos eventos, porque la aprobación no pasa por código de cliente. `security definer`. Solo `published`/`rejected` avisan (archivar es silencioso). 📝 **In-app solamente, sin push** (decisión de Joaquín 29/08): mandar push desde el trigger requeriría `pg_net` a exp.host y no se puede observar/testear el envío — se deja para si hace falta que el coach se entere con la app cerrada. `notifications` está en realtime, así que con la app abierta el aviso aparece solo.
  - RLS SELECT: `status = 'published' OR coach_id IN (mis coaches)` — pública para published, sin restricción de rol (aplica a `anon` y `authenticated` por igual). INSERT solo el coach dueño (`is_author_declared = true`, arranca en `pending`). UPDATE solo el coach dueño. **Sin política de DELETE** — se archiva, no se borra. El pasaje `pending → published/rejected` es manual vía Supabase Dashboard (service role, no pasa por RLS de `authenticated`).
  - ⚠️ **Trampa encontrada 16/07/2026**: la RLS de SELECT depende de que el JOIN embebido `coaches!inner(profiles!inner(name))` pueda resolver `profiles` — y la política de `profiles` ("Perfiles de coaches visibles para todos") exige `role = 'coach'`. Si `coach_resources.coach_id` apunta a una fila de `coaches` cuyo `profiles.role` es `'user'` (pasa con cuentas de test que tienen fila en `coaches` sin haber cambiado de rol — ver `coaches` arriba), el join falla bajo RLS para cualquier otro usuario y **la fila entera desaparece de los resultados**, sin error visible. El seed ahora filtra `WHERE p.role = 'coach'` al elegir el coach de prueba (antes hacía `LIMIT 1` a ciegas).
- **`resource_recommendations`**: `id` (uuid, PK), `resource_id` (FK → `coach_resources.id`), `coach_id` (FK → `coaches.id`), `user_id` (FK → `profiles.id`), `room_id` (FK → `salas.id`), `note` (≤200 chars), `opened_at` (null = sin abrir), `created_at`. Es la recomendación que un coach manda a un usuario por chat (botón "+" en `SalaScreen`) — alimenta la sección "De tus coaches" y la card embebida en el chat vía `messages.metadata`.
  - RLS: SELECT el usuario receptor o el coach que la envió. INSERT solo el coach. UPDATE (marcar `opened_at`) solo el usuario receptor.
- **`resource_saves`**: PK compuesta (`user_id`, `resource_id` → `coach_resources.id`). Guardados, **separada de `saved_resources`** (que usa `resource_id` TEXT y acepta slugs de tools de VITA) — esta es solo para UUIDs de `coach_resources`, sin mezclar tipos. `RecursosScreen.toggleSave()` decide la tabla según `isCoachResource`.
  - RLS: `FOR ALL USING/WITH CHECK (user_id = auth.uid())`.
- **`resource_events`**: `id`, `user_id`, `resource_id` (→ `coach_resources.id`), `event` (CHECK IN `view`/`play`/`complete`/`coach_profile_visit`/`booking_started`), `created_at`. Métrica flywheel. RLS: INSERT solo el propio usuario; **sin política de SELECT** — solo legible por service role/SQL Editor (mismo patrón que `coach_mp_accounts`).
  - **Funnel completo instrumentado (18/07/2026)** vía `lib/resourceEvents.ts` (`logResourceEvent`, fire-and-forget): `view` al cargar el recurso; `play` al primer play de audio, primer `playing` del video (YoutubeIframe), o al tocar "Abrir en {fuente}" de un podcast; `complete` al terminar el audio (currentTime ≥ duration) o al `ended` del video (lectura no dispara `play`/`complete` — su señal de consumo es `view`); `coach_profile_visit` al tocar el nombre del coach (ahora tappable) → navega a `/profesional` con `resourceId` como param; `booking_started` en `ProfesionalScreen`, al tocar "Reservar sesión" — **se dispara ahí y no se hila más allá** (no hace falta pasar el `resourceId` a `BookingScreen`, "started" es la intención de reservar, no la reserva confirmada).
  - **`get_my_resource_counts()`** y **`get_my_resource_stats_month()`** (`scripts/add-resource-events-stats-functions.sql`, **corrida** 18/07/2026): funciones `SECURITY DEFINER` (mismo patrón que `get_my_resource_feedback_summary`) para que el coach lea agregados de sus propios `coach_resources` — ni `resource_events` ni `resource_saves` tienen SELECT para `authenticated`, así que sin esto no hay forma de leer las stats. La primera devuelve `(resource_id, plays, saves)` por recurso (histórico); la segunda `(plays, saves, profile_visits)` del mes calendario en curso. `REVOKE` explícito de `anon`, `GRANT EXECUTE` a `authenticated`. Alimentan el tab F4 "Tus recursos" (`CoachResourcesScreen.tsx`) — antes mostraba todo en `0` hardcodeado.
- Bucket `resource-audio`: límite subido de 20MB a 30MB el 13/07/2026 (mismo bucket que usa el sistema viejo de `resources`, compartido entre ambos pipelines).
  - ⚠️ **Otra trampa `coach_id` vs `auth.uid()` encontrada 16/07/2026**: la policy `resource_audio_coach_insert` exige `(storage.foldername(name))[1] = auth.uid()::text` — la carpeta tiene que ser el `profiles.id` del coach, NO `coaches.id`. `app/coach-recurso-nuevo.tsx` armaba la ruta con el `coach_id` (`coaches.id`) que le llega por param, y el upload fallaba siempre con "new row violates row-level security policy". Fix: usar `user.id` (de `useAuth`) para la carpeta, `coach_id` solo para el INSERT en `coach_resources`. Mismo tipo de mezcla que la trampa de arriba — dos ids distintos con nombres parecidos es una fuente de bugs recurrente en este módulo.

### `ai_usage` (09/09/2026) — ✅ **CORRIDO y VERIFICADO el 10/09/2026**

> `scripts/add-ai-usage.sql` corrido contra la base (vía `supabase db query --linked`)
> y verificado con sus 4 chequeos: la tabla existe con RLS prendido, **0 políticas**,
> **0 permisos** para `anon`/`authenticated`, y `registrar_uso_ia()` suma y corta
> —`(true,1)` en la 1ra llamada, `(false,2)` en la 2da con tope 1— con la fila de
> prueba borrada. `weekly-reflection` **v28 deployada** después del SQL (el orden
> importa: la función sin la RPC daría 503).

- `user_id` (uuid, FK → `auth.users.id`, on delete cascade) · `dia` (date) · `feature` (text, CHECK IN `weekly_reflection`) · `llamadas` (integer). **PK compuesta** `(user_id, dia, feature)`.
- **Para qué**: el tope de gasto por persona y por día de las features que llaman a un modelo. Hoy solo `weekly-reflection`.
- 🔴 **Por qué existe.** `weekly-reflection` llama a la API de Anthropic y no tenía **ningún** tope por usuario. Exige token de usuario real (no la anon key), pero el único freno de FRECUENCIA vivía en un caché de `AsyncStorage` — o sea **en el teléfono de quien llama**. Un script con un token válido la llamaba las veces que quisiera, y cada llamada es plata. 📌 El CAPTCHA del alta lo encarece de rebote, pero no lo cierra: una sola cuenta legítima alcanza.
- **`dia` es la fecha de ARGENTINA, no UTC**: es la ventana que ve la persona, y con UTC el día se cortaría a las 21:00 hora local. Mismo supuesto transversal que el resto del esquema.
- **`registrar_uso_ia(p_user, p_feature, p_tope)`** — `SECURITY DEFINER`, devuelve `(permitido, usadas)`. Suma y contesta **en una sola sentencia**: leer y después escribir dejaría una ventana entre las dos, y dos llamadas simultáneas pasarían las dos. ⚠️ **Suma antes de preguntar**, así el intento rechazado también cuenta y quien se pasa no puede seguir golpeando gratis.
- **El tope NO vive en la base**: es `REFLECTION_TOPE_DIARIO` en la edge function (default 20). Se ajusta con `supabase secrets set` y sin migración.
- **RLS prendido y SIN políticas**: ni `anon` ni `authenticated` leen ni escriben nada; entra solo el service role, o sea la edge function. Que el titular tampoco lea es deliberado — saber cuánto le queda del tope solo le sirve a quien lo quiere agotar.
- ⚠️ **La edge function falla CERRADO** si esta consulta falla (503), al revés que `lib/emailVerificado.ts`. La diferencia es el costo de equivocarse: acá degradar significa que la tarjeta muestre el texto determinístico de `buildReflection()`, que es bueno; allá significaba que nadie pudiera reservar.
- 📌 **No es analítica.** Si hace falta medir uso de la IA, esa pregunta va a `analytics_events`. Acá vive solo el dato que la decisión de cortar necesita.

## Reglas críticas

1. **`coaches.id` ≠ `profiles.id`** — son valores distintos. El dato que conecta es `coaches.profile_id`.
2. **`salas.coach_id` → `profiles.id`** (= `coaches.profile_id`). **`bookings.coach_id` → `coaches.id`**. Son FKs distintas — mismo nombre de columna, tablas distintas. No asumir que se puede usar el mismo valor para ambas.
3. **`messages.content` está encriptado** — nunca guardar texto plano en esa columna.
4. **`room_url` vive en `salas`**, generado por trigger al hacer INSERT. Leerlo siempre desde la sala, no desde `bookings` (la columna en `bookings` es redundante).
5. **Scripts SQL en `/scripts` pueden no estar corridos** — este archivo es la verdad sobre qué existe HOY. Confirmado contra `information_schema` el 20/06/2026.
6. **Cualquier cambio estructural se revisa y corre entre Andre y Joaquín** — hay datos reales de testing en `salas`, `messages` y `bookings`.
7. **RLS en `coaches`**: existe la política `coaches_insert_own` (FOR INSERT, WITH CHECK `profile_id = auth.uid()`) — permite que un usuario autenticado cree su propia fila de coach al postularse. Sin políticas de INSERT, la tabla bloqueaba todo insert por RLS activado sin excepciones. Hasta el 30/06/2026 NO existía política de UPDATE — cualquier `update()` a `coaches` desde el cliente (precio, video, bio, etc.) afectaba 0 filas en silencio, sin que Postgrest devolviera error (solo se nota si se encadena `.select()` y se chequea que `data` no esté vacío). Fix: `scripts/add-coaches-update-policy.sql` agrega `coaches_update_own` (FOR UPDATE, USING + WITH CHECK `profile_id = auth.uid()`).
8. **Cancelación automática de horarios conflictivos**: cuando un coach acepta una reserva, todas las demás reservas 'pendiente' para el mismo `coach_id` + `scheduled_date` + `scheduled_time` se cancelan automáticamente (`status='cancelada'`), con notificación al usuario afectado. Esta lógica vive en `CoachReservasScreen.tsx`, función `accept()`.
9. **Sistema de disponibilidad real**: `coach_availability` almacena slots puntuales por coach+fecha. La gestión (agregar/eliminar slots) vive en `CoachAvailabilityScreen`, la lectura en `BookingScreen_Calendar` y `BookingScreen_Time`. Un slot deja de estar disponible en dos casos distintos (agregado 01/07/2026): (a) para **cualquier usuario**, si ya tiene una reserva `'confirmada'` de otro; (b) para **ese mismo usuario únicamente**, si él ya tiene una reserva `'pendiente'` ahí — evita que un usuario mande dos solicitudes al mismo horario, sin bloquear que usuarios distintos compitan por el mismo slot 'pendiente' (ver punto 8, que resuelve esa competencia cuando el coach acepta). `BookingScreen_Calendar` también excluye horarios de hoy ya pasados (comparación contra la hora local del dispositivo, sin manejo de zona horaria explícito).
10. **`bookings.status = 'completada'` es automático, nunca manual.** La función `complete_confirmed_sessions()` (pg_cron, cada 5 minutos, ver `scripts/complete-confirmed-sessions.sql`) marca como completado cualquier booking `'confirmada'` cuyo `scheduled_date + scheduled_time` haya superado 20 minutos (timezone `America/Argentina/Buenos_Aires`), e inserta una notificación `'invitacion_review'` **solo para el usuario** (nunca para el coach — reviews son unidireccionales, ver punto 15). Se descartó deliberadamente un botón manual del coach: si pudiera marcar sesiones como completadas, tendría incentivo para omitir las que salieron mal y así evitar la invitación a review. El umbral de 20 minutos es corto a propósito — invitar a reviewear mientras la sesión está fresca.
    - ⚠️ **Hallazgo del 01/07/2026:** esta función se documentó el 23/06/2026 como "✅ completada y verificada en Supabase" junto con el cron job (`complete-sessions`, ya agendado y activo desde esa fecha), pero la función en sí **nunca quedó persistida en la base** — confirmado con `pg_get_functiondef` (no existía), búsqueda por nombre en `pg_proc` (no existía) y búsqueda por contenido `'invitacion_review'` en cualquier función de la base (no existía en ningún lado). Resultado: desde que se agendó el cron, cada corrida fallaba en silencio durante más de una semana — ningún booking se completó automáticamente nunca, y no se generó ninguna invitación a review para nadie. El CHECK constraint de `notifications.type` sí incluía `'invitacion_review'` (esa parte del 23/06 sí corrió). Corregido el 01/07/2026 corriendo `scripts/complete-confirmed-sessions.sql` — mismo patrón de incidente que el del 19/06 (documentado en Notion), vale la pena, ante cualquier función/trigger "documentado como corrido", confirmarlo con una query directa a `pg_proc`/`pg_get_functiondef` antes de asumir que existe.
11. **Solicitudes pendientes vencen automáticamente a las 24hs.** La función `expire_pending_bookings()` (pg_cron, cada 5 minutos — ver `scripts/expire-pending-bookings.sql`, corrida en Supabase el 30/06/2026) pasa a `status='cancelada'` cualquier booking `'pendiente'` cuyo `created_at` supere 24hs, e inserta una notificación `'reserva_rechazada'` para el usuario. Antes de esto, `CoachReservasScreen.tsx` calculaba el countdown "Xhs para responder" solo en el cliente y nunca actualizaba la base — la tarjeta de pendiente quedaba viva para siempre, clampeada en "0hs". No manda mensaje a la sala ni push notification (mismo alcance que el rechazo manual del coach, que tampoco manda mensaje a sala).
12. **Bucket de Storage `coach-videos`** (público, 50MB, solo mime types de video — ver `scripts/add-coach-video-upload.sql`, corrido en Supabase el 30/06/2026). Path convención: `{auth.uid()}/video.mp4` (carpeta por usuario, filename fijo, siempre `upsert:true` para no dejar huérfanos al re-subir). RLS: SELECT público para cualquiera; INSERT/UPDATE/DELETE solo si `(storage.foldername(name))[1] = auth.uid()::text` — como `auth.uid() = profiles.id = coaches.profile_id`, un coach solo puede escribir su propia carpeta, sin necesidad de join a `coaches`. Es el primer uso de Supabase Storage en el proyecto.
13. **Modalidad de reserva instantánea (`coaches.instant_booking`)**: si está en `true`, `BookingScreen_Confirm.tsx` inserta el booking directo con `status='confirmada'` y replica ahí mismo los efectos que normalmente dispara `CoachReservasScreen.tsx → accept()` (notificación `reserva_confirmada` al usuario, mensaje `system_confirmed` en la sala, y cancelación automática + notificación `reserva_cancelada` de otras reservas `'pendiente'` que compitan por el mismo coach+fecha+horario). Es lógica duplicada a propósito — no se extrajo a un helper compartido para no tocar `accept()`, que ya tiene datos reales de testing (ver punto 6). Columna agregada vía `scripts/add-coach-instant-booking.sql`, **aplicada — verificado 11/07/2026 vía `scripts/audit-schema.sql`** (la columna existe en Supabase; el flag "pendiente" anterior estaba obsoleto).
14. **Bucket de Storage `avatars`** (público, 5MB, solo mime types de imagen — ver `scripts/add-avatar-upload.sql`, **aplicado — verificado 11/07/2026 vía `scripts/audit-schema.sql`**). Mismo patrón que `coach-videos` (punto 12), pero apuntando a `profiles.avatar_url` en vez de `coaches.video_url` — sirve para cualquier rol, coach o usuario, mismo bucket y misma política. Este script también crea `profiles_update_own` (FOR UPDATE, `id = auth.uid()`) **solo si no existe ya una política de UPDATE en `profiles`** — no había ninguna documentada, así que es la primera vez que se confirma explícitamente. Desde el 01/07/2026 la foto real reemplaza el ícono/iniciales genérico en todas las pantallas que muestran un perfil (ver detalle en la entrada de `profiles.avatar_url` más arriba) — no queda ningún placeholder sin actualizar salvo que el usuario/coach nunca haya subido foto (fallback intacto en todos lados).
15. **Reviews son unidireccionales (usuario → coach) por decisión de producto, no por limitación técnica** (01/07/2026 — ver Notion "Decisiones estratégicas", sección "Reviews: unidireccionales", y punto 10 más arriba para el hallazgo de `complete_confirmed_sessions()`). El schema/trigger/RLS de `reviews` son genéricos y soportarían la dirección coach→usuario sin cambios, pero nunca se construyó esa UI, y ahora está bloqueada explícitamente: `ReviewScreen.tsx` chequea `role` de `useAuth()` al montar y redirige cualquier coach a `/(coach)/reservas` en vez de mostrar el formulario — protección contra cualquier notificación `invitacion_review` vieja, mal generada, o alcanzada por una vía no prevista (ej. tap de push, deep link futuro), no solo contra el flujo normal. Antes de este fix, `app/_layout.tsx` (listener global de tap-en-push) y `ReviewScreen.tsx` no chequeaban rol — un coach que llegara a esa pantalla habría insertado una review con `reviewer_id = reviewed_id` (se reviewea a sí mismo), sin que la constraint UNIQUE ni las RLS lo impidieran. En la práctica nunca se disparó porque el mecanismo que generaba la notificación (`complete_confirmed_sessions()`) tampoco existía — ver punto 10.
16. **El chat de la Sala está congelado mientras la reserva esté `'pendiente'`** — decisión de producto ya documentada en Notion ("Mensaje previo a la aceptación del coach": *"Es unidireccional — no hay chat antes de aceptar, lo que evita que coach y usuario intercambien contactos y se salgan de VIVE"*), implementada recién el 01/07/2026 (antes, `SalaScreen.tsx` permitía escribir y mandar mensajes libremente sin importar el estado de la reserva). `SalaScreen.tsx` deriva `isChatFrozen = confirmedBooking?.status === 'pendiente'`: mientras es `true`, el input y botón de enviar se reemplazan por un aviso (`"El chat se habilita cuando el coach acepte tu solicitud"` del lado usuario, `"Aceptá o rechazá la solicitud desde Reservas..."` del lado coach), y `sendMessage()` también corta temprano como defensa en profundidad (RLS de `messages` no valida esto — es un chequeo de UI/cliente, no de base). El único canal antes de la aceptación sigue siendo el mensaje unidireccional opcional de `bookings.user_message`, mostrado en `CoachReservasScreen.tsx`. La comunicación se destranca sola apenas el booking pasa a `'confirmada'` o `'cancelada'` (deja de haber un booking `'pendiente'` activo).
17. **Dos taxonomías de temas resueltas el 08/07/2026:** `AXES` en `constants/searchData.ts` (3 ejes, 28 subtemas granulares) y los chips de Conexiones (9 categorías amplias). Solución aplicada: cada chip tiene un campo `searchTopics: string[]` que mapea sus 28 subtemas reales de AXES (ej. "Ansiedad y estrés" → `['Ansiedad', 'Estrés físico']`). Los chips ya NO navegan a `search3.tsx` — filtran in-place con `coaches.filter(c => chip.searchTopics.some(t => c.topics.includes(t)))`. Los 4 subtemas que faltaban en `TOPIC_TO_AREA` (Progreso) y `QUIZ_TOPIC_MAP` (Conexiones "Para vos") también se completaron: `Hábitos mentales → trabajo`, `Sexualidad → salud`, `Espiritualidad → proposito`, `Soledad → emocion`. Los 28 subtemas de AXES están ahora cubiertos en ambos mapas.
18. **`REVOKE ALL ... FROM PUBLIC` no basta para bloquear `anon` en una función `SECURITY DEFINER`** (hallazgo 03/07/2026, `get_my_resource_feedback_summary()`): Supabase le otorga `EXECUTE` a `anon`/`authenticated` sobre todo objeto nuevo del schema `public` vía *default privileges* a nivel de base, no a través del pseudo-rol `PUBLIC` — así que revocar de `PUBLIC` no revoca ese grant directo. Hubo que hacer `REVOKE EXECUTE ... FROM anon` explícito (`scripts/fix-resource-feedback-summary-grant.sql`). Ante cualquier función nueva pensada para "solo `authenticated`", verificar el permiso real llamándola con la anon key sin sesión, no asumir que `REVOKE FROM PUBLIC` alcanza — mismo espíritu que el punto 10 (no asumir que algo "documentado como restringido" lo está de verdad).
19. **Dos convenciones de FK a coach conviven a propósito, y el criterio para elegir cuál usar es "operativo vs. atribución"**: tablas donde el coach opera datos de negocio propios (`coach_availability`, `coach_weekly_pattern`, `coach_topics`, `bookings`, y ahora `resource_proposals`) usan `coach_id → coaches.id`, con RLS tipo `coach_id IN (SELECT id FROM coaches WHERE profile_id = auth.uid())`. Tablas que solo atribuyen/enlazan a un perfil para mostrarlo (`salas.coach_id`, `favorite_coaches.coach_profile_id`, `reviews.reviewed_id`, y ahora `resources.attributed_to_coach_id`) usan `→ profiles.id` directo. Ver punto 1-2 para el error clásico de confundir ambas.
20. 🔴 **El RLS es por FILA, no por COLUMNA — y por eso `coaches.verified` era auto-asignable** (hallazgo 13/08/2026, `scripts/lock-privileged-columns.sql`). Las dos policies de UPDATE que existían son de la forma "esta fila es tuya" (`coaches_update_own`: `profile_id = auth.uid()`; `profiles_update_own`: `id = auth.uid()`), lo que autoriza a escribir **cualquier columna** de la propia fila. Como `coachesCache` filtra `.eq('verified', true)`, con la anon key alcanzaba con `update profiles set role='coach'` → `insert into coaches` (`coaches_insert_own` ya existía) → `update coaches set verified=true` para **quedar publicado como profesional verificado sin que nadie revisara la postulación**. Cerrado con **privilegios por columna**: `revoke update on <tabla> from authenticated` + `grant update (col, col, …)` con la lista exacta que el cliente escribe de verdad, auditada contra todos los `.update()` del código. La GRANT dice qué columnas, la policy dice qué filas; hacen falta las dos. `service_role` no se toca — las edge functions siguen escribiendo todo. **CORRIDO y VERIFICADO el 13/08/2026**: `information_schema.column_privileges` devuelve exactamente 15 filas (5 de `coaches` + 10 de `profiles`) y **ni `verified` ni `role` figuran**.
    - **Regla que se desprende:** ante cualquier policy de UPDATE nueva del tipo "la fila es tuya", preguntarse qué columnas de esa tabla NO debería poder tocar su dueño. Si hay alguna (flags de verificación, roles, estados de moderación, montos), la policy sola no alcanza.
    - ⚠️ **Queda abierto:** las 4 columnas de aceptación (`accepted_terms*`, `age_confirmed`) siguen escribibles por el cliente porque las escribe el alta desde `AuthContext`. Son falsificables por su propio titular, lo que debilita su valor probatorio. Cerrarlo exige moverlas a una edge function.
21. 🔴 **Cerrar UPDATE no cierra INSERT: la misma escalada quedó abierta seis meses más por el verbo que faltaba** (hallazgo 15/08/2026, `scripts/add-application-status-and-audit.sql`). `lock-privileged-columns.sql` (punto 20) revocó el UPDATE de tabla completa sobre `coaches` y re-otorgó 5 columnas nombradas dejando `verified` afuera — pero **nunca tocó el privilegio de INSERT**, que siguió siendo de tabla completa. Verificado en prod: `information_schema.column_privileges` devolvía `INSERT | verified` para `authenticated`. La cadena de tres pasos que el punto 20 documenta se colapsaba en una sola sentencia que el arreglo anterior no cubría: `insert into coaches (profile_id, verified) values (auth.uid(), true)`. `coaches_insert_own` valida la FILA, no las columnas — exactamente la misma distinción, aplicada a INSERT. Cerrado igual: `revoke insert` + `grant insert (…)` con las 6 columnas que la postulación escribe de verdad, y `verified` con `DEFAULT false` para que el cliente ya no necesite mandarla (`CoachApplicationScreen` dejó de hacerlo — si la mandara, el alta entera fallaría con 42501).
    - **Regla que se desprende:** un privilegio por columna hay que revocarlo **por verbo**. Al blindar una columna, recorrer los cuatro (`SELECT`, `INSERT`, `UPDATE`, `DELETE`) y preguntarse por cada uno, no solo por el que motivó el hallazgo. La query de auditoría **no debe filtrar por `privilege_type`** o repite el mismo punto ciego.
22. 🔴 **`new Date().toISOString().split('T')[0]` NO es la fecha de hoy: es la fecha UTC** (hallazgo 15/08/2026, `lib/dates.ts`). Argentina es UTC-3, así que ese "día" salta a las **21:00 hora local**. El patrón estaba copiado en cinco lugares y producía tres bugs visibles, todos en la franja de 21:00 a 00:00 — justo el horario en que más se usa una app de bienestar:
    - **La racha desaparecía todas las noches.** `computeMoodStreak` comparaba la última entrada contra un "hoy" que ya era mañana, cortaba en el primer paso y devolvía 0. El badge se caía solo.
    - **El check-in de hoy se veía como no hecho.** `todayMoodEntry` en Inicio buscaba la entrada de mañana.
    - **La sugerencia de hablar con alguien dejaba de aparecer.** `detectMoodDrop` exige que la entrada más nueva sea la de hoy.
    - Y un cuarto, silencioso: **dos check-ins del mismo lunes** (20:00 y 22:00) se guardaban con `entry_date` distinto, así que el `UNIQUE(user_id, entry_date)` de `mood_entries` **no los dedupeaba**.
    - **Regla:** `toISOString()` es correcto para **instantes** (timestamps) y nunca para **días de calendario**. Todo lo que represente "el día del usuario" sale de `localDayKey()` / `localDayKeyMinus()` en `lib/dates.ts`. Ante cualquier comparación de fechas, preguntarse en qué huso está cada lado.
    - ⚠️ **El arreglo es hacia adelante y no se puede backfillear:** `mood_entries` no guarda cuándo se escribió la fila, así que no hay forma de saber cuáles quedaron corridas. El desfasaje máximo es de un día y solo afecta a filas escritas entre las 21:00 y las 00:00. Caso borde de una sola vez: quien haya hecho check-in después de las 21:00 la noche antes de actualizar, al registrar al día siguiente pisa esa fila por el UNIQUE.
    - ⚠️ **Queda uno abierto a propósito:** `dayKey` del deck de Conexiones (`lib/coachDeckRanking.ts`) sigue usando UTC, así que el deck rota a las 21:00. Ahí **no es un bug** —la rotación es pareja para todos y una vez cada 24hs— y hay un test que fija ese comportamiento. Si algún día se alinea, ese test falla a propósito.
